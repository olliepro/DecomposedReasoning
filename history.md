# History

## 2026-06-24

- Added ANSI highlighting to the side-by-side streaming CLI for the tokenizer-added control tokens: `<steer>`/`</steer>` render cyan and `<exec>`/`</exec>` render yellow, with `--no-color` and `NO_COLOR` support to disable it. Validation passed with formatting, compile, direct highlighter checks, and a short 8-epoch SFT smoke request.

- Fixed the side-by-side streaming CLI scrollback renderer so generated newlines terminate rows without adding extra blank spacer rows, and so one-sided rows are only forced when backlog grows or at stream end. Verified with formatting/compile checks and a short 8-epoch SFT side-by-side smoke request.

- Switched the added-token Qwen3.5 4B side-by-side vLLM pair to the completed 8-epoch final models: job `6121191` serves the instruct run as `qwen35-4b-instruct-addedtok-sft-8ep` on `a0223:18020`, and job `6121192` serves the base run as `qwen35-4b-base-addedtok-sft-8ep` on `a0246:18021`. Updated the side-by-side script defaults and verified `/v1/models` plus a short streaming smoke request. Also fixed the Ascend SFT wrapper so configs whose DeepSpeed path contains `zero3` select the ZeRO-3 Accelerate config instead of silently falling back to ZeRO-2.

- Relaunched the added-token Qwen3.5 4B SFT side-by-side vLLM pair on `preemptible-nextgen`: job `6121131` serves the instruct model as `qwen35-4b-instruct-addedtok-sft` on `a0127:18020`, and job `6121132` serves the base model as `qwen35-4b-base-addedtok-sft` on `a0232:18021`. Updated the side-by-side script defaults to these job IDs and verified `/v1/models` plus a short streaming smoke request.

## 2026-06-15

- Changed malformed steer-decision termination to preserve the invalid generated steer text before stopping. Direct single-steer, inline selected-candidate, and branch-selected malformed paths now append the rejected steer text/token IDs to the terminal outcome and record its steer-phase token span, so steer-only masking can still account for all text generated during that phase. Added regressions for malformed non-branch and branch-selected terminal leaves.

- Fixed the Qwen3.5 RL reward-decode crash from job `5611097`: `</s>` is plain generated text for this tokenizer, not EOS, so chat-EOS handling now only treats `<|im_end|>` and `<|endoftext|>` as textual EOS markers. This prevents ordinary split `.</`/`s`/`>` text from being truncated in text space and later turning into `-1` response IDs during rollout consumption. Added parser and legacy stop-reason regressions that preserve `</s>` as normal text.

- Added scheduled-root regression coverage for the initial Qwen `<think>` steer decision boundary: verified the historical `batch_0332_step_000333/doc_4/node_root` artifact did issue a `decode_chunk` from `<think>\n` and stored `<steer>...</steer><exec>...` as one decode chunk, then added a `_decode_state_outcome_async` root-path test that requires the first scheduled `node_root` request to be `steer_single_candidate` and keeps the generated steer text out of `decode_chunk` events. Validation passed: targeted boundary pytest (`4 passed`), steer span/normalization pytest (`22 passed`), full branch-executor pytest (`90 passed`), and `git diff --check`.

## 2026-06-10

- Launched four 4B Qwen3.5 branching-DAPO jobs from `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/RLTraining/qwen35_branching_dapo/merged_hf/qwen35_4b_branching_5496401_global_step_50_hf`, matching the last 4B 16K/n16 reference shape (`5516410`/`5516411`): quad A100 x4, 72h, 400 steps, batch 8, prompt 1024, response 16384, max model len 17408, save every 20, clip 0.2/0.28, exec temp 0.7, steer temp 1.0, W&B online. Jobs: `5529148` structured baseline / `RL_UPDATE_MODE=all`, `5529149` branching / `RL_UPDATE_MODE=all`, `5529150` structured baseline / `RL_UPDATE_MODE=steer_only`, `5529151` branching / `RL_UPDATE_MODE=steer_only`.
- Fixed the branching SQLite viewer’s leaf token reconstruction so unpaired `think_close_continuation` tokens are deferred to the end of the trajectory instead of being spliced inline, which removes the unnatural duplicated final-answer insertion the user was seeing in leaf detail views.
- Added a regression for the terminal think-close ordering bug in `Analysis/tests/branching_eval/test_visualize_branching_run.py`, then verified the visualization tests and pyright on the touched files.
- Fixed node-detail trajectory panels to hydrate the latest leaf from token rows instead of showing only the preview text, so clicked rollout nodes include the final exec/answer block when the leaf event itself has the complete reconstruction.
- Made repeat-forced think-close logging explicit for future runs: `repeat_forced_think_close` now records the synthetic forced-close text as a typed generated-text event, so the viewer can show the `"I am repeating now. Time to stop."` closure directly instead of only implying it via the next vLLM prefix tail.

## 2026-06-03

- Fixed the Qwen3.5 branching-DAPO launcher to build Hydra overrides as a bash array instead of a fragile long backslash command, and wired trainer logger/save/step/KV-util defaults through env variables. This avoids the post-training shell `127` failures seen after otherwise-valid two-step runs.
- Confirmed 2B structured-baseline job `5470634` and corrected 2B epsilon-greedy job `5470691` both reached step 2 with `rollout_n=16`, `adv_estimator=grpo`, `realized_leaf_count=64`, nonzero advantages, and nonzero actor grad norm; skipped the slow no-branching comparison per user request.
- Submitted structured-baseline and epsilon-greedy two-step validation runs for both Qwen3.5 sizes on `quad`, 4x A100 each: `5471033` 2B structured, `5471034` 2B epsilon, `5471035` 4B structured, and `5471036` 4B epsilon.
- Verified those four corrected jobs completed with Slurm `0:0` exit and valid step-2 metrics: all had `prompt_group_count=4`, `realized_leaf_count=64`, nonzero advantages, and nonzero actor grad norm.
- Submitted the matching 400-step comparison jobs on `preemptible-quad`, 4x A100 each, skipping no-branching: `5471120` 2B structured-baseline, `5471121` 2B epsilon-greedy, `5471122` 4B structured-baseline, and `5471123` 4B epsilon-greedy. Each uses `rollout_n=16`, GRPO, 32K response length, vLLM scheduler defaults, and `save_freq=25`.
- Cancelled those four comparison jobs before any training step because they were submitted with console-only logging, cleaned their partial scratch artifact/checkpoint directories, and resubmitted W&B-enabled replacements: `5471129` 2B structured-baseline, `5471130` 2B epsilon-greedy, `5471131` 4B structured-baseline, and `5471132` 4B epsilon-greedy.

## 2026-05-27

- Changed baseline and structured-baseline branching-engine rollouts to score each completed leaf immediately instead of waiting for all sibling rollouts in the doc to finish; added regression tests that prove `leaf_scored` is emitted before the final delayed rollout response.
- Converted the branching event source from `tree_events.jsonl` to `tree_events.sqlite`, with legacy JSONL import for older runs and SQLite-backed doc progress snapshots.
- Added the dynamic stdlib branching visualization server and per-run `viz/start_viz.sh` launcher; browser reloads now refresh from the run SQLite DB without starting servers inside Slurm jobs.
- Analyzed the epsilon-greedy branching path using saved Qwen3.5 AIME24 run artifacts. The dominant slowdown is that inline epsilon-greedy independently builds full candidate pools per rollout path, then runs the embedding/diverse selector to keep one candidate; the inspected 4B eps33 run issued about 3,192 candidate-pool responses and 2.81M candidate-pool tokens versus about 514 responses and 0.46M candidate-pool tokens for tree-random.
- Fixed the sync fallback in `Analysis/branching_eval/run_matrix.py` so epsilon-greedy also disables true tree branching when constructing `BranchExecutor`.
- Fixed `RLTraining/branching_dapo/agent_loop_manager.py` instrumentation so inline epsilon selector overrides are forwarded instead of raising on unexpected keyword arguments.
- Added regression tests for sync epsilon executor construction and RL selector-override forwarding; verified with the focused Analysis/RLTraining pytest targets and targeted pyright.
- Added `BRANCHING_EVAL_MAX_ASYNC_INFLIGHT_REQUESTS` to `BranchExecutor` so eps-greedy request pressure can be tuned separately from doc and branch semaphores.
- Added `manual_runs/20260513/qwen35_eps_tuning/` with a 4B vLLM sweep that keeps vLLM permissive while measuring sustained eps-greedy load without relying on `tree_events.jsonl`.
- The sweep showed `d8/b32/request16` was the best stable low-queue point, while request limit 32 hit vLLM prefill activation OOM at the 262K batched-token cap despite moderate KV usage.
- Restarted Qwen3.5 eps33 AIME jobs after cleaning partial slow/failed run dirs: final active setting is 16 concurrent docs, 64 branch tasks, request limit 16; 0.8B uses 262K batched tokens at 0.90 GPU memory utilization, while 2B/4B use 131K batched tokens at 0.86 to avoid prefill OOM.
- Extended `manual_runs/20260513/rescore_qwen35_aime_answer_markers.py` to recognize structured-baseline run directories, then reran the answer-marker rescore on the latest completed missing-marker Qwen3.5 AIME runs. Wrote unweighted per-problem score summaries to `manual_runs/20260513/qwen35_aime24_25_benchmark/reports/qwen35_aime24_25_unweighted_problem_scores_20260527.{csv,json}`.

## 2026-05-26

- Checked git state before changes; worktree was already dirty on `main` with unrelated modified and untracked files.
- Added `manual_runs/20260526/serve_qwen3_8b_sft_tp2.sbatch` to serve the local Qwen3 8B SFT `checkpoint-472` with vLLM tensor parallelism 2 and a 16K max model length.
- First launch attempt `5420888` failed on an exported tokenizer config incompatibility; cancelled the job and removed the scratch run directory.
- Updated the launcher to use the checkpoint weights plus the compatible local base Qwen3 8B tokenizer, while preserving the checkpoint chat template.
- Submitted corrected job `5420982`; vLLM came up on `a0024:38005`, reported `max_model_len=16384`, and accepted a 16K `max_tokens` chat completion request.
- Found the lowest recorded SFT `eval_loss` at step 118 (`0.77704877`) and switched the vLLM launcher from `checkpoint-472` to `checkpoint-118`.
- Cancelled the previous `checkpoint-472` server job and submitted `5421413`; verified `checkpoint-118` on `a0024:44539` from the login node with a 16K `max_tokens` request.
- Switched the launcher back to the previous served model, `checkpoint-472`, at user request.
- Cancelled the `checkpoint-118` server and submitted replacement job `5421815`; verified `checkpoint-472` on `a0003:58525` from the login node with `/v1/models` and a chat completion using `max_tokens=16000`.
- Added manual-run artifacts for the Qwen3.5 AIME24/AIME25 benchmark matrix:
  `manual_runs/20260513/prepare_qwen35_aime_benchmark.py`,
  `manual_runs/20260513/run_qwen35_aime_benchmark_one.sbatch`, and
  `manual_runs/20260513/submit_qwen35_aime_benchmark.sh`.
- Generated 15 configs covering Qwen3.5 0.8B/2B/4B across HF direct, SFT baseline, SFT epsilon-greedy 50%, SFT tree random, and SFT tree cluster modes with 32K max generation.
- Exported or reused the last SFT checkpoints as vLLM-ready scratch models, including the newly created 2B `checkpoint-480` official-layout export.
- The first benchmark submission failed before readiness because vLLM IPC socket paths exceeded ZeroMQ's 107-character Unix socket limit; cleaned the failed logs, changed job `TMPDIR` to `/tmp/q35_<jobid>`, and resubmitted.
- Resubmitted jobs `5422307` through `5422335`; the first seven servers reached readiness and began writing AIME24 output under `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/Analysis/output/manual_20260513/qwen35_trained_aime24_25_five_modes_32k`.
- Updated the Qwen3.5 AIME24/AIME25 matrix to 32 rollouts/leaves per doc at user request: cancelled jobs `5422307` through `5422335`, removed their superseded scratch outputs/logs, regenerated configs with `baseline_rollouts=32` and `max_branch_points_per_rollout=5`, and resubmitted jobs `5422381` through `5422410`.
- Prepared Qwen3.5 checkpoint-480 NoveltyBench scripts for 0.8B, 2B, and 4B official-layout vLLM exports, including Qwen3 reasoning-field parsing in the local vLLM client.
- Initial NoveltyBench array `5422339` failed during vLLM startup due to overlong ZeroMQ IPC paths; cleaned partial outputs/logs, shortened `TMPDIR` to `/tmp/q35_<job>_<task>`, and resubmitted.
- Replacement NoveltyBench array `5422354` exposed a serving context mismatch: benchmark requests kept the intended 16K generation cap but vLLM was served with an 8K context. Cancelled the array/scoring job, cleaned partial outputs/logs, and raised Qwen3.5 `MAX_MODEL_LEN` to 32K while preserving `MAX_GEN_TOKS=16384`.
- Submitted corrected Qwen3.5 NoveltyBench generation array `5422481` on `preemptible-quad` and dependent 2048-token-filtered scoring job `5422482`.
- `5422481` reached readiness and began completing rows without validation/disconnect errors, but progress showed the 2-hour wall clock would likely time out before completion. Slurm would not extend the active array in place, so cancelled it, cleaned partial outputs/logs, raised the generation wall clock to 8 hours, and submitted replacement array `5422618` with dependent scorer `5422619`.
- `5422618` ran without errors but showed very low KV cache use because vLLM was explicitly capped with `--max-num-seqs=4`. Removed the default `MAX_NUM_SEQS` setting so vLLM uses its own scheduler default unless explicitly overridden, cancelled/cleaned the partial capped run, and submitted replacement array `5422855` with dependent scorer `5422856`.
- `5422855` still showed low KV cache use and slow row completion despite the uncapped vLLM scheduler. Cancelled it and the dependent scorer, raised Qwen3.5 NoveltyBench per-task pressure to `max_concurrent_prompts=20`, `max_concurrent_docs=20`, `max_concurrent_branches=96`, and raised vLLM GPU memory utilization to `0.90` for all Qwen3.5 sizes before rerunning.
- Submitted the higher-pressure Qwen3.5 NoveltyBench generation array as `5423620` with dependent filtered scoring job `5423627` after cleaning the previous partial Qwen3.5 barrage outputs, generated configs, and logs.
- `5423620` confirmed higher request pressure and larger vLLM KV allocation on healthy tasks, but task `5423620_1` failed during startup on `a0001` with CUDA device unavailable. Cancelled the array/scorer, cleaned the partial artifacts again, and submitted replacement generation array `5423642` with `--exclude=a0001`; dependent scorer is `5423643`.
- Prefix cache hit rate remained `0.0%` because the Qwen3.5 vLLM launcher still explicitly disabled prefix caching. Updated the launcher to pass `--enable-prefix-caching`, added `#SBATCH --exclude=a0001` to the barrage script, and prepared to restart from clean Qwen3.5 barrage artifacts.
- Cancelled no-prefix-cache array `5423642` and scorer `5423643`, removed its partial Qwen3.5 barrage outputs/configs/logs, and submitted prefix-cache-enabled replacement generation array `5423745` with dependent scorer `5423746`.
- Raised live array `5423745` to `ArrayTaskThrottle=33` to run the Qwen3.5 barrage with full Slurm-side parallelism; all remaining tasks except the final resource-pending task started immediately while retaining the `a0001` exclusion.
- For the Qwen3.5 AIME24/AIME25 benchmark, cancelled the nine non-baseline jobs from the `5422381`-`5422410` matrix after observing prefix caching was disabled and eps/tree queues were moving slowly.
- Updated the non-baseline restart path to use eps-greedy `0.33`, restore vLLM prefix caching, and return to the historical AIME resource/concurrency shape: 2 A100s, TP=2, GPU memory utilization 0.90, max 128 vLLM sequences, 4 concurrent docs, and 32 concurrent branch requests.
- Restarted missing Qwen3.5 0.8B tree-random AIME job after the prefix-cache-enabled TP=2 launch failed on `a0001` with an NCCL unhandled CUDA error during worker initialization; resubmitted as job `5423729` with `--exclude=a0001`.
- Rendered the Qwen3.5 4B SFT eps33 AIME24 run `5423694` with the canonical `tree_events.jsonl` HTML replay visualizer, writing index/gallery/doc pages under the scratch run's `viz/` directory.
- During the full-throttle Qwen3.5 NoveltyBench array `5423745`, task `27` failed during vLLM startup on `a0010` with `CUDA-capable device(s) is/are busy or unavailable`. Removed that failed task's log artifact and requeued only `5423745_27` so the dependent scorer can still run after the array completes.
- Task `5423745_31` hit the same vLLM startup CUDA device-unavailable failure after it had left Slurm's requeueable active set. Removed its failed artifacts, submitted replacement one-element array `5424018` for task `31`, cancelled the stale scorer `5423746`, and submitted replacement scorer `5424019` with dependencies on the original generation array and the task-31 replacement.
- Fixed AIME answer parsing so plain answer-marker completions such as `Answer: 45<|im_end|>` and `The answer is 033.` normalize correctly, while preserving boxed-answer handling.
- Added `manual_runs/20260513/rescore_qwen35_aime_answer_markers.py` and used it to rescore saved Qwen3.5 AIME baseline/direct artifacts from stored vLLM response tokens, updating current `doc_progress`, `lm_eval_aggregates`, and baseline verification-variance files for 12 saved runs. The rescore report is `manual_runs/20260513/qwen35_aime24_25_benchmark/answer_marker_rescore_report.json`.
- Extended the AIME answer-marker rescorer to cover Qwen3.5 eps33/tree-random/tree-cluster runs by reconstructing bounded terminal decode tails from `tree_events.jsonl`; rescored 28 saved non-baseline runs and 9,317 choices. The tree/eps rescore report is `manual_runs/20260513/qwen35_aime24_25_benchmark/answer_marker_rescore_tree_eps_report.json`.
- Added companion Qwen3.5 SFT structured-baseline configs for 0.8B, 2B, and 4B under `manual_runs/20260513/qwen35_aime24_25_benchmark/configs/`, with `include_structured_baselines=true`, steer triggering enabled, 32 rollouts/doc, AIME24/AIME25, 32K max generation, 8 concurrent docs, and 128 branch/decode tasks.
- Wrote `structured_baseline_manifest.json`, validated the three configs with `load_branching_eval_config`, and submitted quad jobs `5424044`, `5424046`, and `5424048` through the existing vLLM AIME benchmark sbatch wrapper.
- Cancelled structured-baseline job `5424044` before readiness because it landed on previously problematic node `a0001`; moved its startup logs to `logs/cancelled_structured_exclude_a0001_5424044/` and resubmitted the 0.8B structured baseline with `--exclude=a0001` as job `5424051` on `a0016`.
- Ran a one-shot `Analysis/scripts/visualize_branching_run.py` pass over all six completed Qwen3.5 plain SFT baseline AIME24/AIME25 `tree_events.jsonl` files, writing each HTML replay to the corresponding scratch run's `viz/` directory.
- Ran the same one-shot HTML replay visualizer over all six completed Qwen3.5 HF direct baseline AIME24/AIME25 `tree_events.jsonl` files, with verified `index.html`, `gallery.html`, and `summary.json` outputs in each scratch run's `viz/` directory.
- Fixed AIME answer extraction priority to use boxed answers first, then immediate `Answer:`/`final answer` marker answers, then loose dollar-delimited fallback; added Eval parser regressions and reran the completed Qwen3.5 HF direct rescore report at `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/Analysis/output/manual_20260513/qwen35_hf_direct_answer_priority_rescore_20260527_report.json`.
- Reran the corrected answer-priority rescorer over all 38 fully completed Qwen3.5 AIME benchmark runs, skipping 11 partial runs; report written to `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/Analysis/output/manual_20260513/qwen35_all_completed_answer_priority_rescore_20260527_report.json`.
- Tightened the Qwen3.5 AIME answer-marker rescorer so it can target only fully completed runs and assert every complete doc has saved responses; reran it over all 38 completed benchmark run directories, fixing stale zero scores in the completed 4B HF direct AIME25 docs 14-29.
- Rendered the completed Qwen3.5 4B SFT eps33 AIME24 run with the tree-events HTML visualizer; the replay covered 365,392 events, 30 attempts, and 30 selected docs, writing 1,053 files under that run's `viz/` directory.
- Investigated malformed empty/double tags in that eps33 visualization. The doubled `<exec>` is present in raw vLLM decode chunks when the model emits `<exec>` after the evaluator has already injected an exec-open prefix; the empty `<steer></steer></exec>` blocks are then introduced by boundary normalization while repairing the resulting unbalanced exec tag stack.
- Patched steer-to-exec normalization to match the structured SFT data boundary `</steer>\n<exec>` instead of injecting an out-of-distribution newline after `<exec>`, and updated focused branch executor / visualizer / normalization tests.
- Investigated answer truncation at `</think>`: runtime control flow was treating a bare `</think>` as terminal even though the cleaned structured SFT dataset almost always continues with answer text after the close marker. Patched branch decoding so bare think-close prefixes run one no-stop continuation for the answer phase, while prefixes that already contain post-think answer text still terminate normally.
- Converted the branching event source to SQLite-backed `tree_events.sqlite`, removed legacy `tree_events.jsonl` import/read fallback, and optimized the dynamic local visualization server so summary/index/gallery use SQLite aggregate queries while doc graph and node-detail endpoints query only the requested attempt instead of rebuilding a full static snapshot.
- Reworked the dynamic branching visualization hot path onto typed SQLite tables for nodes, edges, leaves, node events, candidate pools, selector flags, vLLM summaries, and progress rows. The server no longer replays attempts or parses full event JSON for graph/node interactions; live-sized doc graph and node-detail requests measured under 0.6s after one-time typed-table backfill.
- Patched steer candidate insertion to use the cleaned SFT boundary `</exec>\n<steer>` instead of inserting candidates after two newlines, while still tolerating legacy/model-emitted double-newline boundaries.
- Submitted Qwen3.5 4B SFT structured-baseline rerun `5428055` on Ascend `quad` after validating a new one-newline-boundary config; vLLM reached readiness on `a0007:8332` and began AIME24 evaluation.
- Cancelled rerun job `5428055` at user request after 23:41 elapsed; Slurm reported `CANCELLED` on `a0007`.
- Restarted the same Qwen3.5 4B SFT structured-baseline rerun as job `5428241` on `a0018`; vLLM reached readiness on `a0018:8332` and AIME24 evaluation began with SQLite events and vLLM throughput logs active.
- Fixed the live SQLite branching viewer so attempt pages auto-select the first eventful rollout node, root nodes expose their rollout children, and lazy node-detail clicks query only the requested node; restarted the job `5428241` viewer on `127.0.0.1:51127` with node-detail requests measured around 0.1s.
- Updated the live viewer graph payload to include compact intermediate generation markers (`vllm_step`, `decode_chunk`, and `steer_block_generated`) from typed SQLite rows, while keeping heavyweight per-event details lazy-loaded by node click.
- Fixed the SQLite writer path after rerun `5428241` failed on a `database is locked` doc-progress write: event rows and doc-progress snapshots now share the same writer queue/connection, detail JSON backfill was removed, decode chunk clicks reuse typed vLLM token rows for highlighted probability chips, the failed scratch run was removed, and corrected replacement job `5428377` was submitted on `quad` after an immediate relative-path launch failure in `5428375`.
- Matched the dynamic SQLite viewer to the old static graph semantics: graph rows show compact `vllm_step`/runtime events rather than every raw chunk, clicked events lazy-load highlighted token probability rows, clicked final nodes include the full generated trajectory, and the stdlib server now uses read-only viewer connections plus stale-while-refresh payload caching. Cancelled old logging job `5428377`, removed its partial run directory, and restarted the same Qwen3.5 4B structured-baseline rerun as `5428412` on `a0024`; the viewer is serving the fresh run at `127.0.0.1:51127`.
- Fixed the live SQLite viewer hang caused by repeatedly reloading already-fetched nodes with no final leaves, kept final-token trajectories lazy, forced trajectory SQL to start from the clicked decode stream index, and restarted the viewer for replacement job `5429267` on `127.0.0.1:51127`.
- Removed the live-run backfill path at user request, restored the old static graph's contiguous visible-event step placement, added typed generated-chunk node columns for normalized token trajectories, cancelled and cleaned partial job `5429267`, and restarted the fresh no-backfill Qwen3.5 4B structured-baseline rerun as `5429360` on `a0021` with the viewer on `127.0.0.1:51127`.
- Fixed final trajectory token coloring in the live viewer by pairing generated chunks with vLLM responses by stream order instead of raw event-index recency, preserving probabilities for decode and steer tokens while rendering normalization-only insertions as neutral `n/a` probability tokens.
- Submitted fresh Qwen3.5 4B SFT epsilon-greedy 33 run `5429424` on `a0007` using SQLite/no-JSONL logging, request in-flight limit 16, and the prior stable 4B eps vLLM shape (`gpu_memory_utilization=0.86`, `max_num_batched_tokens=131072`, `max_num_seqs=1024`); also cleaned the sbatch wrapper so these local vLLM override env vars are unset before `vllm serve` to avoid unknown-env warnings.
- Fixed epsilon-greedy selector decision rendering in the dynamic SQLite viewer: once selection exists, candidate-pool/selector/continuation rows collapse to one visible selector decision, and clicked decisions join typed selector flags to the matching candidate pool to show the selected candidate first, then shortlisted candidates, then all remaining candidates.
- Fixed the selector-event click path so lightweight node events force lazy event-detail hydration before rendering candidate sections; this prevents selector decisions from displaying an empty candidate warning when the event endpoint already has candidate rows.
- Assigned distinct live-viewer graph colors for exec/decode chunks and steer-generated chunks so steer events no longer blend into exec events.
- Fixed final trajectory reconstruction for terminal `think_close_continuation` responses that do not emit a `generated_chunk` row; the live viewer now appends unpaired terminal vLLM response tokens so final answers appear in the complete token trajectory.
- Added an AIME prompt suffix instructing models to put final answers in LaTeX `\boxed{...}` form, validated the adapter test/config loads, and submitted fresh Qwen3.5 4B SFT tree-random (`5432547`) and tree-cluster (`5432548`) quad jobs with the SQLite/no-JSONL runtime and recent stable vLLM overrides.
- Fixed dynamic viewer trajectory reconstruction for branched nodes so complete token views stitch ancestor decode chunks, selected edge candidates, and the clicked node's decode stream instead of only node-local chunks.
- Added typed selector cluster assignments to the SQLite event store and exposed grouped cluster payloads for selector-applied detail views, letting cluster runs show expandable candidate groups by cluster.
- Removed `trigger_fired` from graph-visible tree events while keeping the raw event logged; focused visualization tests pass and the login2 viewers for jobs `5432547`/`5432548` were restarted on ports `51129`/`51130`.
- Preserved terminal evaluation emoji suffixes in truncated tree node labels so correct/incorrect/stop-state markers remain visible even when the node name is shortened for space.
- Added an index-page `avg_problem_passrate` pill and matching summary JSON fields, computed as the equal-weight average of selected per-problem passrates with unscored problems excluded.
- Added AIME25-only Qwen3.5 HF-direct and SFT eps33 configs/manifests for 0.8B, 2B, and 4B; submitted six 12-hour `preemptible-quad` jobs (`5432920`, `5432922`, `5432924`, `5432926`, `5432928`, `5432930`) and repointed login2 viewers `51129`/`51130` to the fresh 0.8B AIME25 HF-direct/eps33 SQLite runs.
- Fixed clicked HF-direct baseline leaf events in the dynamic viewer by resolving `leaf_baseline_N` to the matching baseline vLLM stream and reconstructing the full completion plus highlighted token trajectory from `vllm_choice_token` rows; restarted login2 viewers `51129`/`51130`.
- Removed the failed partial 2B SFT eps33 AIME25 run from job `5432926` after an OpenAI embedding 403, verified a fresh embedding smoke call succeeds, and resubmitted that one `preemptible-quad` job as `5433065`.
- Updated the dynamic viewer to render non-branching baseline rollouts as separate synthetic display nodes (`node_leaf_baseline_N`) under `node_root`, with each leaf node lazily loading its own full token trajectory and avoiding duplicate preview-only completion cards; restarted login2 viewers `51129`/`51130`.
- Repointed the login2 `51129` viewer from the 0.8B HF-direct run to the fresh live AIME25 Qwen3.5 4B HF-direct run `5432928`; verified doc 0 renders 32 synthetic baseline leaf nodes.
- Added AIME24-only Qwen3.5 2B/4B HF-direct configs and manifest, then submitted 12-hour `preemptible-quad` jobs `5433145` and `5433147` with distinct serve ports `8326`/`8328`.
- Added current SQLite/dynamic-viz rerun configs for Qwen3.5 0.8B/2B SFT structured baselines and submitted 12-hour `preemptible-quad` jobs `5433203`/`5433205` on serve ports `8334`/`8336`.
- Added `--run-root` multi-run support to the dynamic SQLite viewer, serving each discovered run under `/runs/<run-id>/`; restarted login2 port `51129` as a fast run picker over the benchmark output root.
- Removed external font fetches and moved dynamic-viewer CSS/JS to local `/assets/...` endpoints so forwarded browser pages do not inline the 79k tree script or wait on Google fonts; restarted `51129`.
- Replaced the stale login2 single-run `51129` server with the multi-run viewer, using an explicit SQLite run-dir list to avoid slow first-touch scratch root scans on login2; added parsed task/size/variant/filter columns to the picker.
- Simplified the `51129` run picker: removed source/gallery columns and all-doc gallery routes, added model and average-doc-passrate columns, moved variant last, humanized start times, and hid older duplicate task/model/size/selector rows behind a checkbox.
- Fixed run-picker duplicate hiding and ordering: latest embedded run timestamps now stay visible within duplicate model/size/task/selector groups, and rows/columns sort as model, size, task, selector.
- Added pivot-style row merging to the SQLite run picker so adjacent identical model, size, task, and selector cells are merged after filtering or duplicate toggling.
- Submitted fresh SQLite/plain SFT-baseline reruns for Qwen3.5 0.8B, 2B, and 4B on 12-hour `preemptible-quad`; each size job covers both AIME24 and AIME25.
- Submitted the missing fresh SQLite Qwen3.5 2B complement: AIME24 eps33 plus both-task tree-random and tree-cluster on 12-hour `preemptible-quad`.
- Retried the 2B AIME24 eps33 complement as Slurm job `5434735` after the first attempt failed in vLLM NCCL initialization on `a0010`; the retry excludes `a0010`.
- Confirmed the 2B AIME24 eps33 retry reached vLLM readiness and began serving/eval; added its fresh run directory to the login2 `51129` visualization list.
- Added a distinct Qwen3.5 2B HF-direct temp-0.7 config for both AIME24 and AIME25 (`qwen35_2b_hf_direct_temp07.yaml`), validated it with the Analysis config loader, and submitted 12-hour `preemptible-quad` job `5437165` with 2 A100s on port `8340` while excluding recent bad vLLM startup nodes `a0001,a0010`; vLLM reached readiness on `a0007`, and eval began writing the AIME24 run under `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/Analysis/output/manual_20260513/qwen35_trained_aime24_25_five_modes_32rollouts_32k/aime24_qwen35_2b_hf_direct_temp07_baseline_baseline_seed20260513_20260529T123944.593530Z`.
- Started a 30-minute one-A100 Qwen3.5 2B manual vLLM server as job `5437308` on `a0024` using `preemptible-quad` because non-preemptible `quad` test-only estimates were delayed until about 16:20 EDT; the server is on `http://a0024:8000`, uses the requested `vllm serve Qwen/Qwen3.5-2B --port 8000 --tensor-parallel-size 1 --max-model-len 1024 --language-model-only` command, and returned `hello world` to the smoke curl.
- Replaced job `5437308` with job `5437318` on `a0024` using `--max-model-len 2048` after vLLM rejected a 1000-token math prompt under the prior 1024-token context; the thinking-mode text-task request used `temperature=1.0`, `top_p=0.95`, `top_k=20`, `min_p=0.0`, `presence_penalty=1.5`, `repetition_penalty=1.0`, `chat_template_kwargs.enable_thinking=true`, and was accepted with 47 prompt tokens plus 1000 completion tokens.
- Cancelled the earlier Qwen3.5 2B HF-direct temp-0.7 AIME24/AIME25 job `5437165`, removed its 1.9 GB partial AIME24 scratch output, added decode config/client plumbing for `top_k`, `min_p`, `presence_penalty`, and decode-level `repetition_penalty`, then submitted separate parallel thinking-mode HF-direct jobs for AIME24 (`5437356` on `a0007:8342`) and AIME25 (`5437358` on `a0003:8344`) with `temperature=1.0`, `top_p=0.95`, `top_k=20`, `min_p=0.0`, `presence_penalty=1.5`, and `repetition_penalty=1.0`; both reached vLLM readiness and began writing SQLite events with those decode params.
- Restarted the dynamic SQLite multi-run visualization registry on `ascend-login02` at `127.0.0.1:51129`, using the explicit 36-run SQLite directory list under the Qwen3.5 AIME output root; verified `/` returns the `Branching Runs` page with HTTP 200 and logs at `logs/branching_viz_51129.log`.
- Built a Qwen3.5 2B RL smoke path for the branching DAPO engine using the local checkpoint-480 vLLM export, the scratch `qwen35-rl-cu129` env, and vLLM `0.22.0` (latest PyPI at check time). The smoke wrapper now supports `branching`, `no_branching`, `structured_baseline`, and `epsilon_greedy` rollout modes with eval-style vLLM flags (`--language-model-only`, `--reasoning-parser qwen3`, prefix caching, priority scheduling, max logprobs). Fixed the RL adapter to read current Analysis `candidate_pool_id` branch metadata and added a regression test. Cleaned failed scratch artifacts/checkpoints from earlier smoke attempts, then verified all four modes reach `global_step_2`: branching `5439936`, no-branching `5439791`, structured-baseline `5439792`, epsilon-greedy `5439793`.
- Investigated live Qwen3.5 2B HF-direct thinking-text jobs `5437356`/`5437358`: Slurm and vLLM are still running, but progress is effectively stalled because 64 concurrent baseline requests per batch are generating near the 32,768-token cap. AIME24 is blocked on docs 4/5 after completing 4 docs; AIME25 is blocked on docs 6/7 after completing 6 docs plus 3 responses for doc 7.
- Diagnosed and fixed the slow Qwen3.5 2B HF-direct thinking-text restart path. Live jobs averaged only about 1k aggregate vLLM gen tok/s versus about 3.4k tok/s in the old fast HF-direct runs; the old run already used 32 `baseline_rollout_single` requests/doc, so the request-shape code change was not the slowdown. The regression was the Slurm wrapper no longer defaulting HF-direct vLLM to the old fast batching shape (`max_num_batched_tokens=131072`, `max_num_seqs=1024`, `gpu_memory_utilization=0.86`), leaving the new runs on the default 2048-token compile range. Also disabled HTTP request timeouts for baseline rollout request kinds so long 32k baselines do not fail/retry. Verified with focused pytest and pyright, cancelled stale `5437356`, removed partial thinking-text output dirs for `5437356`/`5437358`, and restarted AIME24/AIME25 as `5440390`/`5440392`; both new vLLM logs confirm the restored HF-direct serve args.
- Ran the more realistic Qwen3.5 2B branching-DAPO RL smoke matrix on `preemptible-quad` under Slurm account `pas2138`, using vLLM `0.22.0` after checking it was the current PyPI release. All four rollout modes completed 2 training steps with `max_response_length=16384`, `max_model_len=32768`, `train_prompt_bsz=4`, `n_resp_per_prompt=4`, and `train_prompt_mini_bsz=1`: no-branching `5440296`, epsilon-greedy `5440341`, structured-baseline `5440363`, and full branching `5440394`. Each run saved `global_step_2` under `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/RLTraining/qwen35_branching_dapo/checkpoints/`.
- For the RL smoke, kept NCCL enabled on quad and disabled only the cross-NUMA policy because the quad partition does not use that path. Fixed response padding to clamp structured/branching responses at the rollout response length, skipped the useless terminal `update_weights` wake-up after the final step, made the Qwen3.5 smoke wrappers env-overridable for realistic params/exclusive jobs/KV memory, and split the rollout-manager tests out of the near-1000-line branching test file. Cleaned failed scratch artifacts from the earlier branching retry.
- RL smoke tradeoffs: used exclusive quad nodes while the trainer used 2 GPUs, lowered vLLM KV memory utilization to `0.60` for structured-baseline and `0.45` for full branching to avoid CuMem wake-up OOM, and kept `train_prompt_mini_bsz=1` because the VERL actor mini-batch assertion requires divisibility after rollout expansion. No CPU offload was enabled. The remaining speed gap is that the actor-side Transformers path logs that optional FLA/causal-conv1d fast kernels are missing, so it falls back to the torch implementation.
- Fixed the Qwen3.5 RL fast-kernel path in the scratch `qwen35-rl-cu129` env by installing `flash-linear-attention==0.5.0` and rebuilding `causal-conv1d==1.6.2.post1` from source against `torch==2.11.0+cu129`; build job `5452621` completed on `preemptible-quad` in 5:03 and verified FLA/causal-conv1d imports. Added a reproducible source-build helper that targets both Ascend A100 `sm_80` and Cardinal H100 `sm_90` while skipping the huge Blackwell/future arch compile.
- Updated the Qwen3.5 branching-DAPO smoke defaults to a 4-GPU vLLM/actor shape with rollout KV utilization back at `0.80`, CUDA `12.9.1`, and configurable GPU GRES so Ascend can use `GPU_TYPE=a100` while Cardinal can use `GPU_TYPE=h100`. Released 4-GPU branching job `5452560`; it is pending only for a full quad node, with Slurm test-only estimating `2026-06-01T17:16:25` on `a0019`.
- Branching remains slower mostly because it trains many more realized leaves, not because vLLM alone is slow: the prior realistic 2-step smoke had full branching at 64 realized leaves and about 980k tokens versus about 4 leaves and 64-66k tokens in non-branching modes, making old-log-prob and actor-update time scale roughly with token volume.
- Finished the 4-GPU Qwen3.5 2B branching-DAPO speed pass. The stable non-fused 4-GPU baseline `5452560` completed 2 steps in 28:26 with `gpu_memory_utilization=0.80`, `max_num_batched_tokens=131072`, `max_num_seqs=1024`, and 64 realized leaves; step times were 617.7s and 470.0s, with old-log-prob at 104.4s/46.2s and actor update at 218.7s/140.3s.
- Enabled actor `use_remove_padding=true`, fused PPO kernels with the Triton backend, and SDPA actor attention in the Qwen3.5 smoke wrappers. Verified fused/no-padding eager-attention job `5454432` completed 2 steps in 22:03; old-log-prob dropped to 30.0s/28.1s, actor update to 89.0s/85.6s, and step time to 401.0s/393.7s. Verified the faster SDPA job `5454653` completed in 20:35 with old-log-prob 19.9s/19.0s, actor update 54.5s/55.5s, and step time 349.4s/353.3s; checkpoint saved at `qwen35_2b_branching_fusedtriton_sdpa_rmpad_4gpu_kv080_bt131k_realistic16k2step_5454653/global_step_2`.
- Current fastest smoke sacrifices: default Qwen3.5 smoke logging is console-only to avoid W&B offline shutdown noise/overhead, but W&B can be restored by overriding `TRAINER_LOGGERS`. No NCCL disable, CPU offload, reduced KV utilization, or reduced branching work was used in the final speed run; remaining wall time is dominated by rollout generation for 64 realized 16K leaves.
- Tightened the branching-DAPO reward structure check: answer reward is now gated on valid format, answers must appear after one non-empty `<think>` block, strict mode requires non-empty interleaved `<steer>/<exec>` pairs, and exactly one post-think `\boxed{...}` answer is allowed. The steer/exec requirement is configurable via `reward.custom_reward_function.reward_kwargs.require_steer_exec` and `REWARD_REQUIRE_STEER_EXEC`. Filtered the RL export to scalar/simple-fraction plus exact symbolic numeric answers, split dataset helper modules so `build_rl_dataset.py` is under 1000 LOC, regenerated `BuildRLDataset/output/train.parquet` with 9149 rows (`8451` scalar/simple fraction, `698` exact symbolic), and validated with focused pytest/pyright in BuildRLDataset and RLTraining.
- Started a filtered Qwen3.5 2B branching-DAPO 200-step run attempt (`5455232`) with 4 A100s, 16K responses, 4 prompts x 4 responses, full 4x2 branching, vLLM KV utilization `0.80`, console+offline W&B logging, and strict steer/exec reward. Cancelled it after 2 steps because both steps had zero reward/advantages: leaves produced `<think>/<steer>/<exec>` traces but usually stopped at `think_end` or max tokens without a post-think boxed answer. Removed the failed run's scratch artifacts.
- Fixed the branch executor post-think continuation gate so post-`</think>` text only counts terminal when it contains a complete `\boxed{...}` answer; otherwise decoding gets the no-stop answer continuation path. Verified with focused Analysis pytest (`70 passed`) and pyright. Submitted the fixed 200-step filtered run as `5455319` on `preemptible-quad`; Slurm scheduled it on `a0007` for `2026-06-01T20:25:54`.
- Checked `5455319` at `2026-06-01 17:52 EDT`: it remains pending for resources, and `a0007` is occupied by another 4-GPU job until the scheduled slot. Fresh test-only requests on `preemptible-quad`/`quad` were no earlier, and `nextgen` could not satisfy the exact 4-GPU shape. Added `RLTraining/scripts/monitor_slurm_rl_run.sh` and started monitor PID `3611265`, writing periodic snapshots to `RLTraining/logs/monitor_5455319.log`.
- Rechecked `5455319` at `2026-06-01 17:56 EDT`: still pending for resources with `SchedNodeList=a0007` and expected start `2026-06-01T20:25:54`; stdout/stderr logs do not exist yet because the job has not started. Fixed the monitor terminal-state check and restarted it as PID `3614660`; the monitor log has fresh pending snapshots.
- Rechecked `5455319` at `2026-06-01 18:00 EDT`: still pending for resources with the same `a0007` scheduled start at `20:25:54`; no stdout/stderr logs exist yet. Fresh 4-GPU `sbatch --test-only` checks on `preemptible-quad` and `quad`, including shorter/lower-memory shapes, all start later (`21:20`-`21:25`), so the existing reservation remains the fastest path.
- Updated `RLTraining/scripts/monitor_slurm_rl_run.sh` so each snapshot summarizes the latest trainer health line into compact fields: global step, score/reward/advantage means, response clip/abort ratios, realized leaves, branching final-adv magnitude, generation/old-log-prob/actor/step timing, throughput, and total tokens. Validated the parser against cancelled job `5455232`, then restarted the `5455319` monitor as PID `3629659`; it appended a fresh pending snapshot at `18:03 EDT`.
- Cancelled replacement run `5455319` after 2 completed steps because rewards/advantages stayed exactly zero; the executor fix was active, but the exported prompts still lacked a model-visible format contract for one post-think boxed answer. Cleaned its scratch artifact/checkpoint/W&B directories and stopped the stale monitor.
- Added a default system message to the filtered RL export requiring one `<think>` block, non-empty `<steer>/<exec>` content, and exactly one post-think `\boxed{...}` answer. Updated the RL rollout path to accept system+user prompts and seed the branch executor with chat-template prompt token IDs so the system message is actually used by vLLM completions. Regenerated `BuildRLDataset/output/train.parquet` with 9149 rows and verified prompt roles `['system', 'user']`.
- Validated the prompt/runtime fix with focused checks: BuildRLDataset pytest/pyright passed, Analysis branch-executor pytest (`70 passed`) and pyright passed, and RLTraining rollout-manager pytest (`19 passed`) plus pyright passed. Submitted fresh 200-step branching job `5455418` with 4 A100s, 16K responses, strict reward, console+offline W&B, vLLM KV utilization `0.80`, and a 24-hour limit; it is pending for `a0007` at `2026-06-01T20:25:54`. Monitor PID `3854246` writes to `RLTraining/logs/monitor_5455418.log`.
- Rechecked `5455418` at `2026-06-01 18:40 EDT`: still pending with the same `a0007` start estimate (`20:25:54`), and `a0007` has all 4 A100s allocated by an existing job. Fresh `sbatch --test-only` checks would start later (`preemptible-quad` at `21:25:54`, `quad` at `20:50:54`), so the existing reservation remains the best slot. Monitor PID `3854246` is still running and has logged the pending state.
- Rechecked `5455418` at `2026-06-01 18:43 EDT`: still pending for resources with the same `a0007` start estimate (`20:25:54`); stdout/stderr and W&B logs do not exist yet. Monitor PID `3854246` remains active. Fresh replacement checks remain worse (`preemptible-quad` at `21:25:54`, `quad` at `20:50:54`), so no queue change was made.
- Audited home-dir disk usage for cleanup candidates at `2026-06-01 18:49 EDT`: home quota was `359G/500G` and `922k/1,000k` files; biggest measured consumers were DecomposedReasoning run traces (`Analysis/output` 140G, `tree_events.jsonl` 136.2G), `~/.cache/huggingface` 93G, `~/.cache/uv` 24G, `~/.cache/vllm` 16G, and duplicate local virtualenvs.
- Cleared the Hugging Face cache entry `~/.cache/huggingface/hub/models--Skywork--Skywork-Reward-Gemma-2-27B-v0.2` at `2026-06-01 18:51 EDT`, removing the 51G Skywork reward model cache and reducing home quota usage to `308G/500G`.
- Ensured Qwen3.5 RL launchers use scratch-backed Hugging Face caches. The Slurm wrapper already exported `HF_HOME`, `HF_HUB_CACHE`, `HUGGINGFACE_HUB_CACHE`, and `HF_DATASETS_CACHE` under `/fs/scratch/...`; added `HF_ASSETS_CACHE` there and added matching scratch defaults plus cache directory creation to `RLTraining/scripts/run_branching_dapo_olmo3.sh` for direct/manual launches. Shell syntax validation passed. Existing queued job `5455418` remains pending and was not resubmitted because its submitted wrapper already had the core HF cache variables on scratch.
- Cleared the remaining `~/.cache/huggingface` directory at `2026-06-01 18:53 EDT`; home quota usage dropped to `267G/500G`, and total `~/.cache` usage is now 48G.
- Replaced pending 200-step filtered Qwen3.5 2B branching-DAPO job `5455418` with job `5455482` at `2026-06-01 19:11 EDT` to use the requested 8-leaf tree shape: `BRANCHING_BRANCH_FANOUT=2`, `BRANCHING_MAX_BRANCH_POINTS_PER_ROLLOUT=3`, and `BRANCHING_NUM_CANDIDATES=2`. The run keeps 4 A100s, 16K response cap, strict steer/exec reward, filtered train parquet, console+offline W&B, scratch caches/artifacts, and vLLM KV utilization `0.80`; monitor PID `4092229` writes `RLTraining/logs/monitor_5455482.log`.
- Replaced pending job `5455482` with `5455501` at `2026-06-01 19:27 EDT` for the requested cluster-across branching setup: `BRANCHING_BRANCH_PROB=0.2`, `BRANCHING_EPSILON_GREEDY_PROB=0.1`, `BRANCHING_SELECTOR_MODE=cluster_across`, and `BRANCHING_NUM_CANDIDATES=50`, while keeping fanout `2`, max branch points `3`, 4 A100s, 16K responses, strict reward, W&B+console logging, and scratch caches/artifacts. Slurm accepted the job on `preemptible-quad`; same-shape test-only estimated `2026-06-01T21:25:54` on `a0007`, and monitor PID `23795` writes `RLTraining/logs/monitor_5455501.log`.
- Made the requested cluster-across branching shape the default for Qwen3.5 branch-mode launches at `2026-06-01 19:53 EDT`: branch probability `0.2`, epsilon-greedy probability `0.1`, selector `cluster_across`, fanout `2`, max branch points `3`, and `50` candidates. Updated both the smoke matrix submitter and direct Qwen smoke wrapper; `bash -n` validation passed. Current Slurm queue is empty; the earlier `5455501` 4-GPU job was cancelled before validation, and the 2-GPU validation job `5455511` completed successfully with these requested overrides.
- Removed forced answer-shaping from the Qwen3.5 branching decode path at `2026-06-01 20:36 EDT`: the aborted max-branch forced-close experiment was backed out, synthetic post-think `\boxed{` prefix injection was removed, and the post-think continuation now uses the remaining response budget instead of a hidden 512-token answer cap. Focused branch-executor pytest passed (`8 passed`) and pyright reported `0 errors`.
- Split Qwen3.5 branching-DAPO sampling temperatures at `2026-06-01 20:47 EDT`: exec/decode uses `EXEC_TEMPERATURE` via `actor_rollout_ref.rollout.temperature` (default `0.7`), while steer candidate generation uses custom `STEER_TEMPERATURE` (default `1.0`). Wired the setting through `BranchingRolloutSettings` into `DecodingConfig.steer_temperature`, propagated it through the smoke wrappers/matrix submitter, and added focused rollout-manager coverage. Validation: shell wrapper `bash -n` passed, focused pytest passed (`1 passed, 5 deselected`), and pyright reported `0 errors`.
- Increased Qwen3.5 branching-DAPO rollout defaults to 32K generated tokens at `2026-06-01 20:53 EDT`: `MAX_RESPONSE_LENGTH` now defaults to `32768` in the Qwen wrapper, Qwen smoke matrix submitter, and delegated launcher. `MAX_MODEL_LEN` now defaults to `MAX_PROMPT_LENGTH + MAX_RESPONSE_LENGTH` so the prompt plus 32K response fits, and the shared launcher uses that value for `max_position_embeddings` instead of a fixed 32K override. Shell syntax validation passed; no Slurm job was submitted for this change.
- Diagnosed failed 32K 2-GPU validation job `5456150` at `2026-06-01 21:41 EDT`: it completed 2 steps but all rewards/advantages were zero because the Qwen chat template prefilled `<think>\n` in prompt token IDs while the branch executor and reward-visible response text started empty. Mirrored the prompt-prefilled think prefix into executor state and reward metadata without adding sampled response tokens or forcing answers, added focused reward/rollout-manager tests, and validated with RLTraining pytest (`26 passed`) plus pyright (`0 errors`). Removed the failed validation's scratch checkpoint, tree-event artifacts, and offline W&B directory.
- Ran replacement 32K 2-GPU validation job `5456201` at `2026-06-01 21:42 EDT`; it completed 2 steps with W&B+console, filtered train parquet, `max_response_length=32768`, vLLM TP=2, NCCL active, and 16 realized leaves/step, but reward and advantage stayed zero on both steps. SQLite events showed the empty-prefix bug was fixed, but the model still wrote `\boxed{...}` inside `<think>` and produced no valid post-think boxed answer. Strengthened the filtered dataset system prompt without forced decoding: no boxed/final answer inside `<think>`, and after `</think>` output only one boxed answer. Regenerated `BuildRLDataset/output/train.parquet` with 9149 rows, validated BuildRLDataset pytest/pyright and RLTraining reward/rollout pytest/pyright, and removed `5456201` scratch artifacts/checkpoint/W&B output.
- Ran stronger-prompt 2-GPU validation job `5456252` at `2026-06-01 22:00 EDT`; it still had zero reward/advantages on both steps (`15` realized leaves then `16`) and was cleaned from scratch after completion. Found the filtered parquet still wrapped source prompts as chat `user` messages whose content began with a redundant `user:` label. Updated the exporter to strip leading chat-role labels from chat message content while preserving raw `source_prompt_text`, regenerated `BuildRLDataset/output/train.parquet` with 9149 rows and verified zero `user:` prefixes in the first 1000 exported user messages. BuildRLDataset pytest (`6 passed`) and pyright (`0 errors`) pass.
- Ran clean-prompt 32K 2-GPU validation job `5456341` at `2026-06-01 22:15 EDT`; it completed 2 steps with W&B+console, vLLM TP=2, NCCL active, `max_response_length=32768`, `max_model_len=34816`, cluster-across branching, branch probability `0.2`, epsilon-greedy probability `0.1`, fanout `2`, max branch points `3`, and 50 candidates. Both steps still had zero rewards/advantages, but reconstructed step-2 trajectories showed 5/16 format-valid leaves, so the remaining issue is sparse/correctness reward rather than universal format rejection. The run used exec/decode temperature `0.7`, steer temperature `1.0`, and post-think continuation temperature `0.7`.
- Dug into the first-steer empty-block bug at `2026-06-01 22:33 EDT`: surviving SQLite events showed decode chunks like `<steer></steer><exec></exec>\n<steer>` were executor-injected `prompt_token_delta` rows before sampled steer text, caused by normalizing an excluded `<steer` stop before generating the steer string. Fixed steer prefix preparation so already-open `<steer>` tags sample steer content before structural normalization, while invalid open-exec prefixes still normalize first. Also confirmed the Qwen3.5 chat template defaults to a closed empty think block unless `enable_thinking=True`, patched RL prompt tokenization to request the open `<think>\n` tail, and verified the manager now detects `initial_assistant_prefix='<think>\n'`. Validation passed: Analysis branch-executor pytest (`67 passed`), focused normalization pytest (`27 passed`), Analysis pyright (`0 errors`), RL reward/rollout pytest (`27 passed`), and RL pyright (`0 errors`).
- Diagnosed the zero-reward 2-step validation job `5456534` at `2026-06-01 23:57 EDT`: the run did use the SFT export `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/SFTTraining/vllm_exports/qwen35_2b_lr7e6_checkpoint-480_official_layout`, but the async VERL reward loop only forwarded `extra_info` and did not merge the rollout `reward_scores` metadata. Embedded `rollout_reward_scores` into each leaf `extra_info` from the branching rollout manager so the active reward path can see logical reconstructed responses without patching VERL. Reconstructed step-2 SQLite leaves showed 5/16 nonzero rewards after the fix, including one exact doc-1 `\boxed{76}` leaf scoring `1.1`; the old reward-visible path scored all 16 as `0.0`. Validation passed: focused RLTraining pytest (`30 passed`) and pyright on the rollout manager/reward path (`0 errors`).
- Reverified the zero-reward diagnosis at `2026-06-02 00:10 EDT`: focused RLTraining pytest still passes (`30 passed`) and pyright reports `0 errors`; rerunning the SQLite rescore for job `5456534` still shows fixed reward visibility gives 5/16 nonzero leaves with max `1.1`, while the old reward-visible path gives 0/16 nonzero. Slurm currently returns `Unable to contact slurm controller` for a 4-GPU `preemptible-quad` post-fix test-only request, so no new trainer validation job was submitted.
- Submitted the post-fix 4-GPU Qwen3.5 2B branching-DAPO validation as Slurm job `5456823` at `2026-06-02 00:23 EDT` after `sbatch --test-only` accepted the shape for `a0019`. The run uses 4 A100s, SFT export `qwen35_2b_lr7e6_checkpoint-480_official_layout`, 32K responses, TP/FSDP 4, strict steer/exec reward, branch probability `0.2`, epsilon-greedy probability `0.1`, selector `cluster_across`, 50 candidates, KV utilization `0.80`, exec temperature `0.7`, steer temperature `1.0`, and 2 training steps. Corrected the monitor prefix for this job; monitor PID `2203688` writes `RLTraining/logs/monitor_5456823.log`. Startup is healthy so far: actor workers loaded the SFT export with Triton fused kernels, vLLM `0.22.0` started with TP=4/NCCL/FlashAttention/Triton FLA GDN, and generation is active with no recent errors; no trainer step metrics had landed yet at `00:40 EDT`.
- Confirmed nonzero reward in post-fix validation job `5456823` at `2026-06-02 00:44 EDT`: step 1 trainer metrics show `critic/score/mean=0.159375`, `critic/score/max=1.1`, `critic/rewards/mean=0.159375`, `critic/advantages/max=1.532688`, and `branching/realized_leaf_count=32`. This confirms the zero-reward/zero-advantage failure was the reward metadata plumbing bug, not the SFT model universally failing to produce rewardable leaves. The job is still running step 2, and monitor PID `2203688` remains active.
- Started a proper 200-step Qwen3.5 2B branching-DAPO run as Slurm job `5456869` at `2026-06-02 00:49 EDT`, after post-fix validation job `5456823` completed cleanly with nonzero rewards on both recorded steps. The full run uses 4 A100s on `preemptible-quad`, SFT export `qwen35_2b_lr7e6_checkpoint-480_official_layout`, 32K responses, TP/FSDP 4, strict steer/exec reward, branch probability `0.2`, epsilon-greedy probability `0.1`, selector `cluster_across`, 50 candidates, KV utilization `0.80`, exec temperature `0.7`, steer temperature `1.0`, W&B+console logging, and checkpoints every 25 steps. Slurm placed it on `a0019` with a 24-hour limit; monitor PID `2268486` writes `RLTraining/logs/monitor_5456869.log`.
- Checked `5456869` startup at `2026-06-02 00:56 EDT`: actor FSDP loaded the SFT export with NCCL `2.28.9+cuda12.9` and Triton fused kernels; vLLM `0.22.0` initialized with TP=4, FlashAttention, Triton/FLA GDN, max model length `34816`, KV cache capacity `9,396,682` tokens, and maximum concurrency `269.90x` for 34,816-token requests. W&B offline logging is writing under scratch, the trainer reached `Training Progress: 0/200`, and vLLM generation throughput logs show active requests. No trainer step metric has landed yet.
- Confirmed `5456869` completed the first two training steps at `2026-06-02 01:05 EDT` and continued into step 3. Step 1 had `critic/score/mean=0.1125`, max `1.1`, `critic/advantages/max=2.3174`, 32 realized leaves, and `timing_s/step=360.33`. Step 2 had `critic/score/mean=0.309375`, max `1.1`, `critic/advantages/max=2.3114`, 32 realized leaves, `response_length/clip_ratio=0.0`, `response/aborted_ratio=0.0`, `timing_s/step=249.73`, and throughput `497.21`.
- Started a detached branching SQLite viewer for the live 200-step RL run at `2026-06-02 01:48 EDT`: PID `2531678` serves `http://127.0.0.1:51130/` on `ascend-login02` with `--run-root /fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/RLTraining/qwen35_branching_dapo/artifacts/qwen35_2b_branching_32k200step-rewardfix-fullrun_5456869_20260602T045139Z`. Health check passed with `sqlite_runs=12`; the registry refreshes on request, so later `batch_*_step_*` directories should appear as the run advances. Existing port `51129` was left untouched because it is serving older eval runs.
- Fixed live RL tree scoring visibility at `2026-06-02 03:11 EDT`: the branching-DAPO rollout manager now carries tree event context and leaf runtime metadata into `reward_scores`, appends canonical `leaf_scored` events after the async reward loop, and flushes those rows into each step's `tree_events.sqlite`. The interactive visualizer also colors epsilon inline continuations distinctly (`selector_continued_inline` cyan) so epsilon behavior remains separate from branching selector behavior but is graph-visible. Validation passed: RLTraining rollout-manager pytest (`11 passed`) and pyright (`0 errors`), plus focused Analysis visualizer pytest and pyright (`0 errors`).
- Cleaned the canceled online investigation run `5457262` from scratch artifacts/checkpoints/local W&B, then submitted fresh online 4-GPU Qwen3.5 2B branching-DAPO run `5457317` at `2026-06-02 02:53 EDT` after `sbatch --test-only` accepted immediate placement on `a0019`. The run uses the SFT export, 32K responses, TP/FSDP 4, strict steer/exec reward, branch probability `0.2`, epsilon-greedy probability `0.1`, selector `cluster_across`, 50 candidates, fanout `2`, max branch points `3`, KV utilization `0.80`, exec temperature `0.7`, steer temperature `1.0`, W&B online, and checkpoints every 25 steps. W&B URL: `https://wandb.ai/oliverproudfoot-the-ohio-state-university/branching_dapo_qwen35_smoke/runs/rpp816hj`.
- Confirmed new run `5457317` has the requested live visualization/scoring behavior. Viewer PID `2744486` serves `http://127.0.0.1:51130/` from the new artifact root and shows the step DBs as they arrive. Step 1 wrote 32 `leaf_completed` and 32 `leaf_scored` rows, with 3 verified leaves and average score `0.153125`; step 2 wrote 32 `leaf_completed` and 32 `leaf_scored` rows, with 8 verified leaves and average score `0.30625`. The run reached step 3 after two full trainer updates; step 1/2 metrics had no response clipping or aborts, 32 realized leaves each, and step times `315.56s` / `251.89s`.
- Fixed the mid-trajectory `<|im_end|>` bug at `2026-06-02 11:20 EDT`: vLLM was returning EOS token `248046` in generated tokens with `finish_reason=stop` and `stop_reason=None`, so the harness consumed EOS as text and continued. The vLLM client now truncates generated chat EOS tokens/text before logging or prefix-chain updates, and steer/think continuation helpers apply the same guard. Validation passed: focused EOS tests, full `Analysis` vLLM client plus branch executor pytest (`103 passed`), and targeted pyright (`0 errors`).
- Cancelled invalid online job `5457317`, removed the transient `5457362` restart artifact, then started replacement online job `5457365` with binary fanout over four branch points (`2**4` leaves per prompt, 64 max leaves for batch size 4). The run is on `a0019` with 4 A100s, W&B online (`https://wandb.ai/oliverproudfoot-the-ohio-state-university/branching_dapo_qwen35_smoke/runs/xj8ds3hu`), checkpoints at steps 25/50, and viewer PID `1129024` serving `http://127.0.0.1:51130/` from the fixed artifact root. Latest checked step 61 had `branch_depth_max=4.0`, `branching/max_possible_leaf_count=64.0`, no aborts, nonzero reward, and the latest five SQLite DBs had zero EOS-token hits in vLLM token/chunk tables.
- Added prompt and golden-answer logging at `2026-06-03`: each attempt now writes a typed SQLite `prompt_context` row and graph-visible `prompt_logged` root event before generation events; the interactive viewer shows the gold answer immediately and lazily hydrates the full input prompt from the event detail endpoint.
- Disambiguated raw-answer correctness from reward correctness in the branching SQLite viewer at `2026-06-03`: leaf cards and graph badges now mark `raw_answer_acc=True`/`format_valid=False` leaves as answer-correct but format-invalid, while the run index headline uses answer-only accuracy from `leaf_metric.raw_answer_acc` when available. Restarted the updated live viewer on `http://127.0.0.1:51131/` because `51130` was blocked by an active local socket.
- Extended prompt/golden-answer logging to RL branching-DAPO artifacts at `2026-06-03`: each prompt group now emits the same graph-visible `prompt_logged` root event before rollout generation, with the gold answer sourced from `reward_model.ground_truth`. Validation: focused rollout-manager regression passed and targeted RL pyright reported `0 errors`.
- Updated the branching SQLite viewer index at `2026-06-03` so `correct/incorrect` columns also prefer answer-only correctness from `raw_answer_acc` instead of reward-gated verification. Reward correctness remains visible in leaf details/cards. Validation: focused visualizer pytest passed (`21 passed`) and targeted pyright reported `0 errors`; live viewer restarted on `http://127.0.0.1:51131/`.
- Fixed the multi-run branching viewer home page at `2026-06-03` so `avg doc passrate` falls back to selected-doc leaf answer accuracy from `leaf_metric.raw_answer_acc` when `doc_progress_typed` is absent. The live run-root page now shows real per-step values instead of broad `n/a` rows, with the full home page loading in about `5.8s`; validation passed with focused visualizer pytest (`21 passed`) and targeted pyright (`0 errors`).
- Fixed final-leaf inspector loading for older branching SQLite artifacts at `2026-06-03`: node/event detail reads now detect whether `prompt_context` exists and fall back to null prompt fields instead of crashing on old DBs. Added a regression that drops `prompt_context` and still hydrates leaf completions/details. Validation: visualizer pytest passed (`22 passed`) and targeted pyright reported `0 errors`; restarted the live viewer on `http://127.0.0.1:51131/` and verified the previously failing node plus event `20761` return 200.
- Reduced dynamic branching viewer request-path work at `2026-06-03`: run-root discovery is cached for 60s, the home page computes only the newest SQLite summary synchronously instead of all 183 step DBs, and run-local attempt/progress/key reads now use caches plus a controlled 503 on transient SQLite locks instead of empty replies. Validation: visualizer pytest passed (`22 passed`) and targeted pyright reported `0 errors`; restarted `http://127.0.0.1:51131/`. Cold root load dropped to about `5.1s`, while the selected step's first graph payload remains heavy because its SQLite DB is about `799M` and graph payload construction takes about `14.5s` outside HTTP.
- Wired requested Qwen3.5 branching-DAPO sampling defaults at `2026-06-03`: exec and steer top-p now default to `0.95`, presence penalty defaults to `1.5`, exec/decode repetition penalty defaults to `1.0`, steer repetition penalty follows that same `1.0` default, and DAPO actor clipping defaults to `clip_ratio_low=0.2` / `clip_ratio_high=0.28`. Added `steer_top_p` through the eval and RL rollout config path so steer generation can diverge from exec generation without changing global rollout top-p. Validation: shell syntax check passed, focused Analysis pytest passed (`3 passed`), focused RLTraining pytest passed (`2 passed`), and targeted pyright reported `0 errors` in both trees.
- Fixed late-rollout epsilon suppression at `2026-06-03`: maxed true-branch paths now resume with `branching_enabled=False` but preserve an explicit `inline_epsilon_enabled=True`, so additional tree fanout stays capped while epsilon-greedy inline candidate selection can still fire after the branch cap. Steer-boundary detection now stays active for inline epsilon after true branching is disabled. Validation: full branch-executor pytest passed (`72 passed`) and targeted pyright reported `0 errors`.
- Cleaned stale RLTraining saved outputs at `2026-06-03 15:04 EDT`: emptied old scratch roots `RLTraining/branching_dapo` (6.6G, April/May artifacts/checkpoints/W&B) and `RLTraining/branching_dapo_smoke` (8.1M), after earlier clearing old `qwen35_branching_dapo` contents. Preserved the active 400-step Qwen3.5 2B/4B quad run output under `qwen35_branching_dapo`; jobs `5470222` and `5470223` are running and have reached `Training Progress: 0/400`.
- Started live tree-event viewers for the active 400-step Qwen3.5 runs at `2026-06-03 15:15 EDT`: 2B viewer PID `3814150` serves `http://127.0.0.1:51131/` from the 2B artifact root and had step 1 plus step 2 DBs; 4B viewer PID `3814151` serves `http://127.0.0.1:51132/` from the 4B artifact root and had step 1. Health checks for the index pages and current step pages returned HTTP 200.
- Corrected the active 400-step Qwen3.5 branching setup at `2026-06-03 15:44 EDT`: changed branch-mode defaults to binary fanout over four branch points (`2**4` leaves per prompt) and branch probability `0.15`, cancelled the mistaken `2**3`/`0.20` jobs `5470222`/`5470223` and brief `0.20` restarts `5470321`/`5470322`, cleaned their scratch outputs, and submitted replacements `5470337` (2B) and `5470338` (4B) on `quad`. Live logs confirm `branch_prob=0.15`, `branch_fanout=2`, `max_branch_points_per_rollout=4`, `epsilon_greedy_prob=0.1`, `num_candidates=50`, and `total_training_steps=400`; live SQLite step-1 DBs reached `max(branch_points_used)=4`. Restarted viewers on `http://127.0.0.1:51131/` for 2B and `http://127.0.0.1:51132/` for 4B.
- Verified the non-branching comparison jobs at `2026-06-03 15:58 EDT`: submitted and started 2B/4B epsilon-greedy structured non-branching jobs `5470429`/`5470431` plus 2B/4B structured-only jobs `5470432`/`5470430`, all on `quad` with 4 GPUs, 32K responses, KV utilization `0.80`, top-p `0.95`, presence penalty `1.5`, repetition penalty `1.0`, and `total_training_steps=400`. Each job reached W&B, `Training Progress: 0/400`, vLLM KV initialization, and generation throughput. W&B runs: 2B eps `tv8bvut3`, 4B eps `y36r5rpc`, 2B structured `jkt6wgrp`, 4B structured `bzkwq9du`.
- Fixed `prompt_logged` inspector freezes for the live RL branching viewers at `2026-06-03`: prompt event detail payloads now expose only a bounded head/tail preview for large prompts, with the full prompt served through an explicit plain-text `/prompt.txt` link instead of being inserted into the page DOM. Validation passed with focused visualizer pytest (`23 passed`) and targeted pyright (`0 errors`); restarted the active 2B/4B viewers on `http://127.0.0.1:51131/` and `http://127.0.0.1:51132/`.
- Restored full event display for the live RL branching viewers at `2026-06-03` after a too-aggressive graph sampling attempt hid intermediate events. Kept the safer click path: event chips/buttons now schedule inspector hydration without redrawing the full tree, so prompt clicks do not synchronously rebuild thousands of SVG event chips. Validation passed with focused visualizer pytest (`23 passed`) and targeted pyright (`0 errors`); restarted `51131`/`51132`.
- Fixed the actual `prompt_logged` click crash at `2026-06-03`: the JS event-detail path treated `event_index=0` as missing because it used a truthy fallback, causing prompt hydration to loop without a detail URL. Event index zero is now preserved, missing detail paths fail visibly instead of looping, and a headless Chrome replay clicked `0:node_root:prompt_logged` successfully on `51131`. Validation: focused visualizer pytest `23 passed`, targeted pyright `0 errors`; restarted `51131`/`51132`.
- Removed the explicit Qwen3.5 branching-DAPO vLLM `max_num_batched_tokens=131072` cap at `2026-06-03`: the Qwen/OLMo launchers can now pass `ROLLOUT_MAX_NUM_BATCHED_TOKENS=null`, the rollout config accepts `None`, and the async vLLM server omits the arg so vLLM chooses its own batching behavior. Cancelled capped jobs `5470337`/`5470338` and the first bad no-cap restarts `5470754`/`5470755` after they exposed VERL's `8192` default, then submitted replacements `5470766` (2B on `a0012`) and `5470767` (4B on `a0014`). Logs confirm active actor rollout `max_num_batched_tokens=None` and vLLM `0.22.0` starts with `compile_ranges_endpoints=[2048]`, not the old `131072` compile range.
- Removed the explicit Qwen3.5 branching-DAPO vLLM running-request cap at `2026-06-03`: `ROLLOUT_MAX_NUM_SEQS=null` now passes through as actor rollout `max_num_seqs=None`, the async vLLM server omits `max_num_seqs`, and the Ray vLLM server actor omits the `max_concurrency` override when `max_num_seqs` is unset. Cancelled jobs `5470766`/`5470767`, cleaned their specific scratch artifacts/checkpoints, and submitted replacements `5470837` (2B on `a0012`) and `5470838` (4B on `a0014`) with both `max_num_batched_tokens` and `max_num_seqs` unset. Startup logs confirm vLLM `0.22.0` is generating with no explicit max-seqs/batched-token args; KV capacity is `10,022,331` tokens for 2B and `7,055,075` tokens for 4B.
- Investigated doc 3 in the 2B no-vLLM-caps step-2 viewer at `2026-06-03 18:29 EDT`: mixed grades came from two causes. Reward intentionally gates answer credit on valid structure, so leaves with multiple boxed answers or broken steer/exec score zero even if the raw extracted answer is equivalent. Separately, the string fallback treated `\dfrac{7\pi}{24}` and `\frac{7\pi}{24}` as different because Math-Verify parsed the gold in string mode; canonicalized common LaTeX fraction aliases in the string matcher. Validation: focused reward pytest passed (`21 passed`), direct job-env probe scores both aliases as correct with valid structure, and targeted RLTraining pyright reported `0 errors`.
- Fixed the branching slowdown regression at `2026-06-03 21:50 EDT`: the late-rollout inline-epsilon fix accidentally used probability `1.0` for steer-boundary detection after true branch capacity was exhausted, instead of `epsilon_greedy_prob`. This made current 2B/4B branching runs resolve about 600-700 candidate pools per step versus about 100 in the previous fast branching run, swamping prefix-cache reuse. Boundary detection now uses `branch_prob` for true branching, `epsilon_greedy_prob` for inline epsilon, and `0.0` otherwise. Validation: branch-executor pytest passed (`73 passed`) and targeted Analysis pyright reported `0 errors`.
- Restarted the Qwen3.5 comparison matrix at `2026-06-03 22:07 EDT` after the branching slowdown fix: active jobs are `5471305`/`5471306`/`5471307` for 2B branching, structured baseline, and epsilon-greedy, plus `5471312`/`5471313`/`5471314` for the matching 4B modes. All six are running on `quad` with 4 A100s, 48 CPUs, 256G memory, and 72-hour limits. The vLLM batch/request caps are intentionally unset (`max_num_batched_tokens=None`, `max_num_seqs=None`) while actor rollout KV utilization is `0.8`; 2B vLLM serve logs confirm no `--max_num_batched_tokens` or `--max_num_seqs` args. Cancelled and cleaned the first incorrectly named 4B submissions `5471308`/`5471309`/`5471310` before resubmitting with explicit 4B experiment names.
- Raised Qwen3.5 branching-DAPO vLLM caps at `2026-06-03 23:24 EDT` after no-cap runs showed vLLM using the small default compile range. The Qwen launcher and smoke matrix now default to `max_num_batched_tokens=524288` and `max_num_seqs=4096`, with `max-logprobs=1` preserved and `decode_chunk_tokens=512` unchanged. Cancelled no-cap jobs `5471443`-`5471448`, cleaned their partial 2B scratch outputs, and submitted replacements `5471459`/`5471460`/`5471461` for 2B plus `5471462`/`5471463`/`5471464` for 4B on `quad`; 2B vLLM logs confirm the actual serve argv includes `--max_num_seqs 4096`, `--max_num_batched_tokens 524288`, and `compile_ranges_endpoints=[524288]`.
- Moved the Qwen3.5 2B/4B comparison matrix to Cardinal `gpu` at `2026-06-03 23:32 EDT`: branch-mode defaults are now `branch_prob=0.10` in the Qwen direct and matrix launchers, and the shared sbatch file no longer hardcodes Ascend-only excluded nodes. Cancelled Ascend jobs `5471459`-`5471464`, cleared old Qwen3.5 scratch artifact/checkpoint directories under `qwen35_branching_dapo`, and submitted Cardinal H100 jobs `11248276`/`11248278`/`11248279` for 2B branching/structured/epsilon plus `11248280`/`11248281`/`11248282` for 4B branching/structured/epsilon. Each requests `partition=gpu`, `gres=gpu:h100:4`, 48 CPUs, 256G, 72h, 400 steps, `max_num_batched_tokens=524288`, `max_num_seqs=4096`, `max-logprobs=1`, and 32K response length; Slurm scheduled all six for `2026-06-04T00:28:12` on `c0808/c0809/c0810/c0811/c0816/c0820`.
- The first Cardinal submission `11248276`/`11248278`/`11248279`/`11248280`/`11248281`/`11248282` failed immediately with exit `127` because Cardinal's non-login sbatch shell did not define the `module` command before `module load python/3.12`. Patched the Qwen3.5 sbatch wrapper to source Cardinal Lmod init files before loading modules, removed the failed zero-byte logs, and relaunched the six-run matrix as `11248381`/`11248382`/`11248383` for 2B branching/structured/epsilon plus `11248384`/`11248385`/`11248386` for 4B branching/structured/epsilon. Replacement job `11248381` started on `c0816`; logs show Ray initialized, dataset length `9149`, `branch_prob=0.1`, actor rollout TP=4, `max_num_batched_tokens=524288`, and `max_num_seqs=4096`.
- Started Qwen3-VL Thinking masked-SFT 7e-6 Zero2 8-epoch jobs at `2026-06-04 01:22 EDT`: array `5472223` runs `Qwen/Qwen3-VL-2B-Thinking` on `a0016` and `Qwen/Qwen3-VL-4B-Thinking` on `a0021`, with models cached in scratch under `models/qwen3_vl_{2b,4b}_thinking`. Added `qwen3_vl` loading via `AutoModelForImageTextToText`, validated configs/preflight, and both tasks reached training.
- Cleaned the abandoned Cardinal H100 Qwen3.5 RL attempts at `2026-06-04 01:30 EDT` after jobs `11248*`/`11249*` were cancelled or failed. Removed matching `qwen35_branching_dapo` artifacts/checkpoints/logs, isolated Cardinal compiler cache directories, local Slurm logs, and H100 W&B run folders. Verified no remaining H100/Cardinal/job-ID scratch paths, compiler cache paths, local log files, or queue entries remain; the qwen35 scratch root dropped to `18M` with artifacts/checkpoints effectively empty.
- Started the matching Qwen3-VL Thinking masked-SFT `1e-5` Zero2 8-epoch jobs at `2026-06-04 01:36 EDT`: array `5472248` runs 2B on `a0014` and 4B on `a0024` using the same scratch model dirs and dataset as the `7e-6` pair. Config parser and Slurm `--test-only` passed; both jobs passed Qwen3-VL/CUDA preflight, W&B runs are `4eab955b0a6e44198b555dca6dba3dd2` for 2B and `1129f4eaf0dd4dd2b898b69a79a29148` for 4B, and the 2B task reached optimizer steps.
- Investigated the Cardinal H100 Qwen3.5 RL slowdown/cache suspicion at `2026-06-04 01:42 EDT`. A no-cache relaunch ruled out stale shared vLLM cache as the direct decode-speed cause but paid a large cold-compile tax; then the sbatch wrapper was changed to use Cardinal CUDA `13.2.1` and scratch compiler caches scoped by cluster, CUDA module, GPU compute capability, and vLLM version. Relaunched the six-run 2B/4B matrix as `11249387`-`11249392` with `VLLM_DISABLE_COMPILE_CACHE=0`; vLLM logs now show cache directories under `Analysis/cache/compiler/cardinal/cuda_13.2.1/nvidia_h100_9.0/0.22.0`, and fresh Triton cubins report `.target sm_90a`. Early 2B H100 intervals recovered structured/epsilon generation throughput to about `2.3k`-`3.0k` tok/s while branching remained lower at about `0.8k`-`1.0k` tok/s, pointing the remaining gap back at branching request shape rather than wrong-hardware compiled artifacts.
- Exported Qwen3-VL Thinking SFT checkpoints to text-only Qwen3 causal-LM layouts for RL at `2026-06-04 03:05 EDT`: the 2B `1e-5` final model exports to `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/SFTTraining/exports/qwen3_vl_2b_thinking_lr1e5_text_only_qwen3_causal_lm`, and the 4B `7e-6` final model exports to `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/SFTTraining/exports/qwen3_vl_4b_thinking_lr7e6_text_only_qwen3_causal_lm`. Added `RLTraining/scripts/export_qwen3_vl_text_model.py` to map `model.language_model.*` weights to `Qwen3ForCausalLM`, strip VL-only mrope fields, support sharded safetensors, and validate local `AutoModelForCausalLM` reloads.
- Started Qwen3-VL text-only branching-DAPO RL on Cardinal H100s at `2026-06-04 03:05 EDT`: early 2B attempts on `c0811` failed with FSDP/NCCL CUDA misaligned-address errors even after disabling meta-init or actor fused kernels, while a single-GPU load probe succeeded on another node. Cleaned failed job artifacts for `11250701`, `11250717`, `11250823`, and `11250829`, then relaunched excluding `c0811`; 2B job `11250912` is running on `c0820`, and 4B `7e-6` job `11251375` is running on `c0821`. Both use 4 H100s, 32K responses, KV utilization `0.90`, branch probability `0.10`, epsilon-greedy probability `0.10`, 50 cluster-across candidates, `max_num_batched_tokens=524288`, `max_num_seqs=4096`, `max-logprobs=1`, W&B online, and scratch cache/artifact roots under `RLTraining/qwen3_vl_text_branching_dapo`.
- Cleaned git state before committing on `2026-06-04`: ignored the large generated `manual_runs/20260505`, `manual_runs/20260513`, and `manual_runs/20260526` artifact directories, removed Python/package cache byproducts from the local FlashAttention shim directories, and kept source/test changes visible for staging.
- Verified the Qwen3.5 H100 vLLM env at `2026-06-04 03:58 EDT`: `vllm==0.22.0` is already the latest installed version, but this was not a fresh full reinstall and the matching prebuilt `flashinfer-jit-cache==0.6.11.post2+cu129` was missing. Installed that cache package into the scratch env and ran standalone Qwen3.5 2B TP4 H100 decode benchmarks with dummy weights/text-only mode, matching the RL vLLM server shape. Jobs `11253228`/`11253229` completed; auto vs forced Triton GDN were effectively tied, with candidate-pool `n=50` at about `1.75k tok/s`, 4096-prompt decode at about `1.72k tok/s`, and 512-prompt decode at about `5.0k tok/s`. Startup still compiles/captures the large `524288` warmup shape and logs first-inference Triton JIT warnings for hybrid kernels, so the remaining RL slowdown is not explained by raw standalone vLLM decode throughput.
- Continued Qwen3.5 H100 vLLM system-side speed debugging at `2026-06-04 04:27 EDT`: added disabled-by-default benchmark/RL knobs for vLLM DCP, DBO, and `disable_hybrid_kv_cache_manager`. DCP alone is not usable for Qwen3.5 hybrid KV (`decode_context_parallel_size=4` violates the TP/KV-head limit; `decode_context_parallel_size=2` fails because hybrid KV groups with multiple block sizes do not support context parallelism). DBO is also not quick-usable: vLLM requires DeepEP all2all backends plus NVSHMEM, and Cardinal has no `nvshmem` module while PyPI `deep-ep` is source-only and fails metadata without NVSHMEM. Queued Cardinal H100 benchmarks `11254245`/`11254246` to test the remaining tradeoff: disabling hybrid KV alone, and disabling hybrid KV plus DCP 2.
- Completed the queued hybrid-KV tradeoff checks at `2026-06-04 04:41 EDT`: Cardinal benchmark jobs `11254245` and `11254246` both failed before generation because vLLM could not convert Qwen3.5's hybrid KV specs to one unified type, with or without DCP 2. No valid speed improvement came from DCP, DBO, or disabling the hybrid KV manager; these knobs remain opt-in/default-off for future tests but should not be used for the Qwen3.5 RL run.
- Built a clean scratch Qwen3.5 RL env at `2026-06-04 05:00 EDT` under `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/RLTraining/envs/qwen35-rl-cu129-fresh-20260604` with `vllm==0.22.0`, `torch==2.11.0+cu129`, `triton==3.6.0`, FlashInfer python/cubin/JIT cache `0.6.11.post2`, FLA `0.5.0`, and a source-built `causal-conv1d==1.6.2.post1` wheel containing both `sm_80` and `sm_90` kernels. The fresh-env Qwen3.5 TP4 H100 benchmark `11255235` used FlashAttention 3, FlashInfer GDN prefill, FlashInfer sampler, and FlashInfer allreduce norm fusion, but throughput stayed essentially unchanged from the prior env: `5898 tok/s` for 512 prompt/128 gen, `1729 tok/s` for 4096 prompt/128 gen, and `1757 tok/s` for the 4096 prompt `n=50` candidate-pool shape. This rules out stale vLLM install or missing FlashInfer/causal-conv packages as the current H100 long-context bottleneck.
- Ran the fuller Qwen3.5 TP4 H100 benchmark `11255414` against the fresh env with RL-like `gpu_memory_utilization=0.90` and `max_model_len=34816`. It increased KV cache capacity to `11,068,370` tokens / `317.91x` max concurrency, but long-context throughput remained below the target: 512-prompt/512-gen was `5923 tok/s`, while 4096-prompt/512-gen was `1747 tok/s` and the 4096-prompt `n=50` candidate-pool case was `1801 tok/s`. The bottleneck is therefore tied to long-context/hybrid Qwen3.5 request cost under the current TP4 H100 vLLM shape, not low KV capacity.
- Tested whether Qwen3.5 2B TP4 was simply too much tensor parallelism for H100 by submitting a TP1 fresh-env profile `11255547` with the same `gpu_memory_utilization=0.90`, `max_model_len=34816`, and `max_num_batched_tokens=524288`. TP1 failed before generation during vLLM kernel warmup/FlashInfer autotune with a CUDA OOM after allocating about `88G` on the single H100 and trying to allocate another `6G`; TP1 is not a drop-in replacement at the current high-cap RL shape.
- Also tested Qwen3.5 2B TP2 under the same fresh-env H100 profile as job `11255609`. It failed before producing benchmark rows during vLLM warmup with both H100s nearly full (`~92.7G` allocated each) and another `6G` allocation requested, so TP2 is also not a drop-in replacement at `gpu_memory_utilization=0.90`, `max_model_len=34816`, and `max_num_batched_tokens=524288`.
- Rechecked the clean scratch env at `2026-06-04 05:44 EDT`: PyPI reports `vllm==0.22.0` as both installed and latest, with `torch==2.11.0+cu129`, `triton==3.6.0`, FlashInfer python/cubin/JIT cache `0.6.11.post2`, FLA `0.5.0`, and `causal-conv1d==1.6.2.post1` installed. Added a VERL compatibility fix so `rollout.cudagraph_capture_sizes` is passed to vLLM 0.22 as `cudagraph_capture_sizes` instead of the stale `cuda_graph_sizes` key, and exposed rollout data parallel size plus CUDA graph capture sizes in the Qwen3.5 smoke launcher.
- Probed the same Qwen3.5 2B branching-DAPO shape with two TP2 vLLM data-parallel engines on 4 H100s. Job `11256414` used `data_parallel_size=2`, `tensor_parallel_size=2`, `gpu_memory_utilization=0.82`, and capped CUDA graph capture at 256; it got past the prior OOM but failed during vLLM startup with `custom_all_reduce.cuh:455 invalid argument`. Relaunched as `11256594` with only `disable-custom-all-reduce=True`; vLLM 0.22 started successfully using `['PYNCCL']` all-reduce, initialized about `8.4M`/`8.6M` KV tokens across the two engines, and began generating with early combined intervals up to about `1.55k tok/s`, below the `2k+` target but confirming the fresh install and custom-all-reduce root cause for the DP2/TP2 startup failure.
- Reverified the fresh scratch Qwen3.5 RL env at `2026-06-04 08:30 EDT`: PyPI still reports `vllm==0.22.0` as both installed and latest, with `torch==2.11.0+cu129`, `triton==3.6.0`, FlashInfer python/cubin/JIT cache `0.6.11.post2`, FLA `0.5.0`, `causal-conv1d==1.6.2.post1`, and editable VERL installed under `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/RLTraining/envs/qwen35-rl-cu129-fresh-20260604`. A TP4 FlashInfer-allreduce RL probe reached vLLM startup with `['FLASHINFER', 'PYNCCL']`, FlashAttention 3, FlashInfer GDN prefill, FlashInfer allreduce norm fusion, `gpu_memory_utilization=0.90`, `max_num_batched_tokens=524288`, and `max_num_seqs=4096`; the earlier same-shape probe still topped out around `1k` tok/s before shared-memory broadcast stalls, so the remaining issue is runtime/system behavior in the colocated RL path rather than a stale vLLM or missing-kernel install.
- Continued Cardinal H100 Qwen3.5 colocated-vLLM speed debugging at `2026-06-04 09:10 EDT`: patched the VERL vLLM async server to request `RequestOutputKind.FINAL_ONLY` because the code only consumes the final output, not intermediate streamed cumulative outputs. Probe `11264220` confirmed optimized kernels (`['FLASHINFER', 'PYNCCL']`, FlashAttention 3, FlashInfer GDN prefill, FlashInfer allreduce norm fusion) and KV capacity `10,474,939` tokens / `300.87x` max concurrency, but still plateaued near `1.0k` generation tok/s before dropping to `0.0` with `Running: 79` and `No available shared memory broadcast block found in 60 seconds`. Node-side checks showed H100s at 100% util but only about 120W, low KV usage, ample `/dev/shm`, and vLLM worker CPU in user-space; cancelled `11264220` and cleaned its scratch root while preserving logs.
- Fixed two launcher-side speed controls at `2026-06-04 09:11 EDT`: `ROLLOUT_ASYNC_SCHEDULING=false` now emits vLLM's actual `--no-async-scheduling` boolean flag instead of an ineffective value-form flag, and the shared branching-DAPO launcher now defaults `MAX_CONCURRENT_BRANCHES=512` rather than `64`. No-async probe `11265103` confirmed `--no-async-scheduling`, `Asynchronous scheduling is disabled`, and `max_concurrent_branches=512`, but it stalled earlier at `Running: 6` with the same shared-memory broadcast warning, so async scheduling is not the root cause. Cancelled `11265103`, cleaned its scratch root, and queued GDN-prefill Triton probe `11265637` to test whether FlashInfer GDN's worker-side JIT/autotune path is causing the H100 colocated stalls.
- Ran Cardinal H100 GDN-prefill Triton probe `11265637` at `2026-06-04 09:28 EDT` with the unchanged Qwen3.5 2B branching-DAPO shape, fresh vLLM `0.22.0` env, TP4, `gpu_memory_utilization=0.90`, `max_num_batched_tokens=524288`, `max_num_seqs=4096`, max logprobs `1`, FlashInfer all-reduce, and `gdn-prefill-backend=triton`. The worker confirmed `Using Triton/FLA GDN prefill kernel`, FlashAttention 3, KV capacity `10,474,939` tokens / `300.87x`, and no waiting queue, but generation stayed below target: steady intervals averaged about `1.03k` tok/s with max `1.20k`, high prefix hit rate (`~97%`), and low KV usage (`<=3%`). Cancelled the probe after `11:38` and cleaned its scratch root; GDN FlashInfer/JIT is not the main H100 slowdown.
- Added env-visible probe controls at `2026-06-04 09:30 EDT` without changing defaults: Qwen3.5 smoke launches can now set `ROLLOUT_ENABLE_PREFIX_CACHING` and `ROLLOUT_SCHEDULING_POLICY`, and the Slurm wrapper logs `VLLM_USE_SPINLOOP_EXT`, `VLLM_MQ_MAX_CHUNK_BYTES_MB`, and `VLLM_ALLOW_CHUNKED_LOCAL_ATTN_WITH_HYBRID_KV_CACHE` for auditability. Shell syntax validation passed. Submitted spinloop probe `11266508` with `VLLM_USE_SPINLOOP_EXT=1`, otherwise matching the Triton-GDN probe; Slurm scheduled it on Cardinal `c0810` at `2026-06-04T10:58:14`.
- Ran spinloop probe `11266508` at `2026-06-04 10:12 EDT` with the unchanged 4-H100 Qwen3.5 2B branching-DAPO shape plus `VLLM_USE_SPINLOOP_EXT=1`. vLLM confirmed Cardinal CUDA `13.2.1`, fresh `vllm==0.22.0`, Triton/FLA GDN prefill, FlashAttention 3, FlashInfer all-reduce norm fusion, KV capacity `10,472,860` tokens, and `300.81x` max concurrency, but generation stayed below target: steady avg/max about `1.23k`/`1.41k` tok/s with `Waiting: 0`, KV usage `<=2.3%`, and prefix hit around `97%`. Cancelled the probe and cleaned its scratch root; queued FLA packed recurrent-decode-off probe `11269734` with the same RL/branching shape for the next isolated system A/B.
- Reverified the fresh Qwen3.5 RL env at `2026-06-04 10:12 EDT`: PyPI reports `vllm==0.22.0` as latest and the scratch env has vLLM `0.22.0`, PyTorch `2.11.0+cu129`, Triton `3.6.0`, FlashInfer python/cubin/JIT cache `0.6.11.post2`, FLA `0.5.0`, `causal-conv1d==1.6.2.post1`, and editable VERL installed. Spinloop probe `11266508` confirmed optimized H100 kernels (FlashAttention 3, Triton/FLA GDN prefill, FlashInfer allreduce, KV capacity `10,472,860` tokens / `300.81x`) but stayed around `1.2k` generation tok/s with zero waiting and low KV usage, so it was cancelled and its scratch root was cleaned. Queued follow-up Cardinal probe `11269734` to disable FLA packed recurrent decode (`VLLM_ENABLE_FLA_PACKED_RECURRENT_DECODE=0`) while keeping the same branching/RL shape and spinloop setting.
- Requeued the FLA packed recurrent decode probe as cold-compile Cardinal job `11270116` at `2026-06-04 10:18 EDT` so the result cannot reuse a stale compiled graph: same Qwen3.5 2B branching-DAPO shape, fresh vLLM env, TP4/FSDP4, 4 H100s, `VLLM_USE_SPINLOOP_EXT=1`, `VLLM_ENABLE_FLA_PACKED_RECURRENT_DECODE=0`, and `VLLM_DISABLE_COMPILE_CACHE=1`. Also added a default-preserving `ROLLOUT_TOP_LOGPROBS` knob (default `1`) through the branching rollout settings and launchers for a possible no-logprob speed probe if the kernel-path test misses target. Validation passed with shell syntax checks, focused rollout-manager pytest (`2 passed, 10 deselected`), and targeted pyright (`0 errors`).
- Verified again at `2026-06-04 10:41 EDT` that the fresh scratch env has the current PyPI `vllm==0.22.0` plus `torch==2.11.0+cu129`, `triton==3.6.0`, FlashInfer python/cubin/JIT cache `0.6.11.post2`, FLA `0.5.0`, `causal-conv1d==1.6.2.post1`, and editable VERL. Cold FLA-packed-decode-off probe `11270116` still missed the target with 20 vLLM logger rows: overall gen min/avg/max `28.0/813.8/1126.7` tok/s, and after the first 5 rows `596.2/932.4/1126.7` tok/s, with `Waiting: 0`, high prefix hit rate, and low KV usage. The probe was cancelled before any trainer step landed, its scratch root was cleaned, and no-logprob Cardinal probe `11272115` was queued with the same RL/branching shape plus `ROLLOUT_TOP_LOGPROBS=0` to isolate vLLM response-logprob overhead.
- Immediately after queueing, both pending Cardinal jobs `11270118` (`q3txt-probe`) and `11272115` (`q35-nologp`) were cancelled from user id `47915` before allocation, with zero runtime and no stdout/stderr. There is no active Cardinal probe queued as of `2026-06-04 10:42 EDT`.
- Ran the Qwen3.5 2B standalone vLLM full decode benchmark on Ascend `quad` A100s as job `5475922` at `2026-06-04 11:00 EDT`. It used fresh `vllm==0.22.0`, TP4, `gpu_memory_utilization=0.90`, max model length `34816`, `max_num_batched_tokens=524288`, `max_num_seqs=4096`, dummy load, language-model-only mode, and Triton/FLA GDN prefill on node `a0014`. vLLM used FlashAttention 2, custom/PYNCCL all-reduce, KV cache `8,924,587` tokens / `256.34x` concurrency, and completed all full benchmark cases: `512p/512g` `11709.7` output tok/s, `4096p/512g` `13602.8` output tok/s, and `4096p n=50` candidate-pool `8747.0` output tok/s.
- Ran matching Qwen3 text-only standalone vLLM full decode benchmarks on Ascend `quad` A100s at `2026-06-04 11:08 EDT`. Qwen3 2B job `5475987` ran on `a0004` with FlashAttention 2, custom/PYNCCL all-reduce, KV cache `2,226,720` tokens / `63.96x` concurrency, and speeds `512p/512g` `4682.3` output tok/s, `4096p/512g` `7601.0` output tok/s, and `4096p n=50` candidate-pool `7108.2` output tok/s. Qwen3 4B job `5475988` ran on `a0014` with KV cache `1,612,896` tokens / `46.33x` concurrency and speeds `4134.6`, `6091.4`, and `5441.6` output tok/s for the same three cases.
- Corrected the standalone vLLM benchmark `full` case at `2026-06-04 12:18 EDT` so `candidate_pool_n50` now generates `512` tokens instead of the prior short `16` token probe. Added `--speculative-config-json` to the benchmark script so Qwen3.5 MTP can be tested with vLLM's documented `{"method":"mtp","num_speculative_tokens":1}` config. The Qwen3.5 export advertises `text_config.mtp_num_hidden_layers=1`, but direct `model.safetensors` key inspection found no `mtp`/`nextn`/`draft` tensors among 321 keys, so the MTP run may fail at load time or provide no useful accepted-token speedup. Slurm could not accept corrected submissions from this session because `squeue` and `sbatch --test-only` both failed with `Unable to contact slurm controller (connect failure)`, and DNS could not resolve `ascend-login01`/`ascend-login02` short or full hostnames.
- Ran the corrected exact `4096p`, `n=50`, `512g` standalone vLLM decode case on Ascend `quad` A100s at `2026-06-04 12:37 EDT`, using the fresh `vllm==0.22.0` scratch env, TP4, `gpu_memory_utilization=0.90`, `max_model_len=34816`, `max_num_batched_tokens=524288`, `max_num_seqs=4096`, dummy load, and language-model-only mode. Qwen3.5 2B rerun job `5477016` completed on `a0004` with FlashAttention 2, Triton/FLA GDN prefill, FlashInfer sampling, custom/PYNCCL all-reduce, KV cache `8,924,587` tokens / `256.34x`, and measured `23,582.3` output tok/s over `409,600` generated tokens in `17.37s`; the first attempt `5476883` failed only because the generated benchmark harness needed a `main()` guard for vLLM's `spawn` worker path, and its failed log pair was removed. Qwen3 text-only 2B job `5476884` completed on `a0014` at `11,096.1` output tok/s over `409,600` generated tokens in `36.91s`; Qwen3 text-only 4B job `5476885` completed on `a0024` at `8,843.8` output tok/s over `409,600` generated tokens in `46.31s`.
- Ran Qwen3.5 2B with vLLM MTP speculative decoding on Ascend `quad` A100s at `2026-06-04 12:56 EDT` after checking the current vLLM docs: the recommended config is `{"method":"mtp","num_speculative_tokens":1}` and vLLM 0.22 normalizes the deprecated `qwen3_5_mtp` alias to `mtp`. Job `5477070` completed on `a0004` using the fresh scratch env, TP4, dummy load, language-model-only mode, Triton/FLA GDN prefill, FlashAttention 2, FlashInfer sampling, custom/PYNCCL all-reduce, and MTP `num_spec_tokens=1`. It measured `2,421.3` tok/s for `512p/512g`, `4,844.2` tok/s for `4096p/512g`, and `6,864.8` tok/s for the exact corrected `4096p`, `n=50`, `512g` candidate-pool case, much slower than the no-MTP Qwen3.5 2B A100 result of `23,582.3` tok/s. The SFT export still has `mtp_num_hidden_layers=1` in config but no `mtp`/`nextn`/`draft` tensors in `model.safetensors`, so this dummy-weight benchmark only measures vLLM MTP overhead and does not prove a usable trained MTP head exists in the exported checkpoint.
- Relaunched the Qwen3.5 RL comparison matrix on Ascend `quad` A100s at `2026-06-04 13:06 EDT` after the A100 no-MTP speed check. Submitted 2B jobs `5477243`/`5477244`/`5477245` for branching, structured baseline, and epsilon-greedy; all three started immediately on `a0007`/`a0009`/`a0016`. Submitted matching 4B jobs `5477246`/`5477247`/`5477248`; they are pending with scheduler estimates around `14:02`/`15:05`/`15:59` EDT. The matrix uses the fresh scratch vLLM env, CUDA `12.9.1`, 4 A100s, 48 CPUs, 256G, 72h, 400 steps, 32K responses, Qwen3.5 SFT exports, W&B online, `ROLLOUT_GDN_PREFILL_BACKEND=triton`, `max_num_batched_tokens=524288`, `max_num_seqs=4096`, `max-logprobs=1`, branch probability `0.10`, epsilon-greedy probability `0.10`, 50 cluster-across candidates, and no MTP/speculative decoding. The 2B logs reached Ray/config startup and confirmed the intended model path and rollout settings.
- Restarted failed 4B Qwen3.5 branching job `5477246` as job `5478468` on Ascend `quad` at `2026-06-04 14:38 EDT` after the original run died at step 5 from `Gemini response missing JSON object` in the `cluster_across` selector. The replacement keeps the same 4B SFT export, 4 A100s, 400 steps, 32K responses, branch probability `0.10`, epsilon-greedy probability `0.10`, fanout `2`, max branch points `4`, 50 candidates, TP/FSDP 4, W&B online, and vLLM shape, but switches branching `selector_mode` to `embed_diverse_topk_random` so true-branch selection uses the epsilon-greedy diverse top-k selector instead of live Gemini clustering. Removed the failed `5477246` scratch artifact, checkpoint, and local W&B directories while preserving Slurm logs.
- Profiled branching-engine response logging under a synthetic high-concurrency candidate-pool shape at `2026-06-04`: 20 response events with 50 choices x 512 tokens spent `1.162s` before the change, dominated by per-token probability serialization and writer float quantization. Made vLLM response event payloads omit redundant `selected_probability` while preserving `selected_logprob`, token ids/text, request count, seeds, prompts, and sampling parameters; the SQLite writer and resume replay now derive probability from logprob when needed, and vLLM response payloads use a specialized writer quantizer. The same profile dropped to `0.595s`; scratch profile directories were removed. Validation passed: full branch-executor pytest (`73 passed`), event-source pytest (`9 passed`), and targeted pyright (`0 errors`).
- Re-profiled vLLM response event persistence against real Qwen3.5 branching-DAPO step traces at `2026-06-04 16:22 EDT` after the compact token-payload change. On 400 current-shape real `vllm_response` rows from the 2B step `batch_0013_step_000014` (36,747 token rows), full raw event-log storage averaged `0.471s` while compact raw storage averaged `0.246s`; on the 4B step `batch_0004_step_000005` (32,580 token rows), full averaged `0.408s` while compact averaged `0.244s`. Raw event JSON now omits duplicate `choices[*].tokens`/`token_ids` for successful vLLM responses after typed normalization, and `read_event_rows`/resume hydrate those fields back from `vllm_choice` and `vllm_choice_token`. Validation passed: event-source pytest (`10 passed`), branch-executor plus event-source pytest (`83 passed`), and targeted pyright (`0 errors`; pyright still reports an upstream version warning `1.1.408 -> 1.1.410`).
- Continued profiling branching-event persistence at `2026-06-04 16:38 EDT` against real 2B step response rows. cProfile on 1,200 current-shape successful `vllm_response` events (115,504 token rows) showed remaining time dominated by normalized token-table writes: before this pass, total write was `1.490s`, with `_token_values` at `0.432s` and SQLite `executemany` at `0.614s`; response serialization itself was only `0.099s` for 5,414 real choices, so it was not worth changing. Inlined the hot token-row optional conversion path and changed append-only `vllm_choice`/`vllm_choice_token` inserts from `INSERT OR REPLACE` to `INSERT OR IGNORE`; the same cProfile dropped to `1.070s`, with `_token_values` at `0.197s` and SQLite `executemany` at `0.583s`. The 400-row real samples now average `0.221s` on the 2B step and `0.237s` on the 4B step while writing the same token-row counts. Validation passed: branch-executor plus event-source pytest (`83 passed`) and targeted pyright (`0 errors`; same pyright version warning).
- Fixed the multi-run branching viewer index for live RL step databases at `2026-06-04 16:50 EDT`: `batch_*_step_*` SQLite run rows now display and sort by their parent experiment directory, so current 2B/4B branching, structured-baseline, and epsilon-greedy runs remain identifiable while duplicate step rows show only the latest step by default.
- Profiled epsilon-greedy speed on the live Qwen3.5 A100 matrix at `2026-06-04 17:23 EDT`. Recent 2B epsilon was effectively tied with structured baseline over the last five completed steps (`timing_s/gen` `497.57s` vs `496.91s`; `gen_tok_s_from_total` `2324.7` vs `2371.7`), while 4B epsilon lagged structured baseline (`553.89s` vs `497.47s`, `1666.4` vs `1856.9` tok/s) with larger long-decode variance. Latest epsilon step DBs showed candidate-pool embedding selector work was real but not dominant: 2B had `583` matched inline selector events with median `0.188s` selector latency and 4B had `1058` with median `0.174s`, but only small millisecond-scale coalescing windows were available in the current trace. Made epsilon scheduled roots explicitly disable true-branch scheduling while preserving inline epsilon selection, and added a shared-session OpenAI embedding coalescer that dedupes concurrent embedding texts without changing candidate vectors or selected-token semantics. Validation passed: selector/event-source/branch-executor pytest (`94 passed`), targeted pyright (`0 errors`; same pyright version warning), and Qwen3.5 launcher `bash -n`.
- Fixed steer repetition detection at `2026-06-04 17:42 EDT` after live 4B epsilon step `batch_0020_step_000021` showed `node_root_rollout_8` replay crossing the steer-repeat threshold at event `45519`/token `11215` but continuing to `max_gen_toks_reached`. The miss came from vLLM stop strings split across updates (`...<s` then `teer>` then steer text), while offline `generated_chunk` rows showed normalized `...<steer>` text. The runtime repetition parser now repairs split partial `<steer>` deltas for parser state only, without changing generated tokens or request shapes. Added a partial-stop regression that isolates steer repetition from exec repetition. Validation passed: focused regression (`1 passed`), repeat subset (`4 passed`), branch-executor/event-source/selector suite (`95 passed`), and targeted pyright (`0 errors`; same version warning).
- Generalized the repetition parser split-tag fix at `2026-06-04 17:53 EDT` so exec and steer repetition both carry partial open/close tag fragments across chunk boundaries. This covers split `<exec>` and `</exec>` as well as split `<steer>` without changing generated tokens or request shapes, and removes the earlier steer-only repair from the executor. Added direct parser regressions for split exec-open, split exec-close, and split steer-open streams. Validation passed: parser tests (`3 passed`), repeat subset (`7 passed`), branch-executor/event-source/selector/parser suite (`98 passed`), and targeted pyright (`0 errors`; same version warning).
- Fixed the RL failure from job `5477244` at `2026-06-04 18:00 EDT`: the structured-baseline run died after step 22 because forced repeat-close entered post-think continuation with prompt `29464` plus requested output `4717`, exceeding vLLM `max_model_len=33792` by `389` tokens. The Analysis decoding config now carries optional `max_model_len`, RL executor construction forwards `actor_rollout_ref.rollout.max_model_len`, and post-`</think>` continuation caps or skips requests against the remaining model context with `max_context_length_reached` when context is exhausted. Removed the failed run's scratch artifact and checkpoint roots while preserving Slurm logs. Validation passed: focused think-close regression (`4 passed`), full Analysis branch-executor/config suite (`89 passed`), focused RL executor construction test (`1 passed` in the scratch RL env), and targeted pyright in both Analysis and RL (`0 errors`; same version warning).
- Restarted the full Qwen3.5 RL comparison matrix at `2026-06-04 18:14 EDT` after the context-cap fix. Cancelled old active jobs `5477243`, `5477245`, `5477247`, `5477248`, and `5478468`; the earlier failed `5477244` baseline was already cleaned. Removed the old scoped scratch artifact/checkpoint roots and local W&B run dirs while preserving Slurm logs. Submitted replacements on Ascend `quad` with 4 A100s, 400 steps, 32K responses, Triton GDN prefill, max-logprobs `1`, W&B online, and the fresh scratch vLLM env: 2B branching `5480918`, 2B structured baseline `5480919`, 2B epsilon-greedy `5480920`, 4B branching diverse selector `5480921`, 4B structured baseline `5480922`, and 4B epsilon-greedy `5480923`. Follow-up queue check showed all six running.
- Fixed the Qwen3.5 RL restart failures at `2026-06-05 13:37 EDT`: RL prompt tokenization now left-truncates to the configured rollout prompt length before executor/DataProto use, and branching vLLM requests are clamped against the exact resolved request input ids before dispatch so generated prefixes cannot overrun `max_model_len`. Cleaned failed 2B run artifacts/checkpoints for jobs `5480918`/`5480919`/`5480920` and restarted the 2B branching, structured-baseline, and epsilon-greedy replacements as jobs `5488502`/`5488503`/`5488504`. Validation passed: Analysis branch-executor pytest (`78 passed`), RL rollout-manager pytest (`13 passed`), and targeted pyright in Analysis/RL (`0 errors`).
- Plotted exec-block lengths for Qwen3.5 RL runs with over 50 scored steps at `2026-06-05 14:21 EDT`. The included runs were 4B branching diverse `5480921` (93 scored steps), 4B epsilon-greedy `5480923` (116), and 4B structured baseline `5480922` (125); the restarted 2B runs were below the threshold. Used `leaf_score.length_tokens_exec` from completed step SQLite DBs, skipping in-progress DBs without scored leaves. Wrote full-scale and zoomed PNGs plus the per-step CSV under `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/RLTraining/qwen35_branching_dapo/plots/`.
- Corrected the Qwen3.5 exec-length plot at `2026-06-05`: the prior `leaf_score.length_tokens_exec` plot showed only the last completed exec-block heuristic, which understated total execution content. Regenerated plots by reconstructing scored leaf paths from context-keyed `generated_chunk` rows plus selected branching edge text, then summing whitespace-token estimates inside all `<exec>` regions. Corrected outputs are `qwen35_4b_total_exec_words_over_steps_corrected_20260605.png`, `qwen35_4b_exec_segment_words_over_steps_corrected_20260605.png`, and `qwen35_4b_exec_lengths_corrected_20260605.csv` in the scratch plots directory.
- Added a 10-step rolling average plot for corrected average exec-block length at `2026-06-05 14:52 EDT`. The per-step value is total reconstructed whitespace-token words inside `<exec>` regions divided by total reconstructed exec blocks; the rolling line is trailing 10 scored steps weighted by block count. Outputs are `qwen35_4b_exec_block_avg_rolling10_corrected_20260605.png` and `qwen35_4b_exec_block_avg_rolling10_corrected_20260605.csv` under the scratch plots directory.
- Added corrected 4B exec-block count and steer-block length plots at `2026-06-05 15:07 EDT`. The block-count plot uses reconstructed `<exec>` block counts per scored leaf with a trailing 10 scored-step rolling average; the steer plot uses reconstructed `<steer>` block words per block with the same rolling window. Outputs include `qwen35_4b_exec_block_count_rolling10_corrected_20260605.png`, `qwen35_4b_steer_block_avg_rolling10_corrected_20260605.png`, `qwen35_4b_exec_vs_steer_block_avg_rolling10_corrected_20260605.png`, and their CSVs in the scratch plots directory. Confirmed the failed high-step 2B DB roots had been cleared; only the low-step promptfix 2B roots remained at 9-12 step DBs.
- Regenerated the Qwen3.5 rolling exec/steer block plots at `2026-06-05 20:50 EDT` including the now-running promptfix 2B runs. The extraction reconstructs leaf text from `generated_chunk` plus selected `edge_selected.candidate_text` so the per-block metrics are based on full scored paths, not `leaf_score` previews. Included ranges were 2B branching `5488502` steps 1-45, 2B epsilon-greedy `5488504` steps 1-52, 2B structured baseline `5488503` steps 1-55, 4B branching `5480921` steps 1-122, 4B epsilon-greedy `5480923` steps 1-125, and 4B structured baseline `5480922` steps 1-125. Wrote combined `qwen35_all_with_running_2b_*_rolling10_20260605.{png,csv}` and 2B-only `qwen35_2b_running_*_rolling10_20260605.{png,csv}` outputs under the scratch plots directory.
- Regenerated the same Qwen3.5 exec/steer block plots at `2026-06-05 21:00 EDT` with centered smoothing instead of trailing smoothing. Used an 11-step centered window (5 scored steps before and after; partial windows at series edges) and refreshed current DB ranges: 2B branching `5488502` steps 1-46, 2B epsilon-greedy `5488504` steps 1-52, 2B structured baseline `5488503` steps 1-57, 4B branching `5480921` steps 1-123, 4B epsilon-greedy `5480923` steps 1-125, and 4B structured baseline `5480922` steps 1-125. Wrote combined `qwen35_all_with_running_2b_*_centered11_20260605.{png,csv}` and 2B-only `qwen35_2b_running_*_centered11_20260605.{png,csv}` outputs under the scratch plots directory.
- Plotted Qwen3.5 reward components at `2026-06-05 21:09 EDT` for all six current RL runs using `leaf_metric.structure_reward` and `leaf_metric.answer_reward` from each scored step DB. Used an 11-step centered leaf-weighted average. Included ranges were 2B branching `5488502` steps 1-47, 2B epsilon-greedy `5488504` steps 1-53, 2B structured baseline `5488503` steps 1-59, 4B branching `5480921` steps 1-123, 4B epsilon-greedy `5480923` steps 1-125, and 4B structured baseline `5480922` steps 1-125. Wrote `qwen35_all_structure_reward_centered11_20260605.png`, `qwen35_all_answer_reward_centered11_20260605.png`, and `qwen35_all_reward_functions_centered11_20260605.csv` under the scratch plots directory.
- Updated the Qwen3.5 reward plots at `2026-06-05 21:23 EDT` to make reward scales explicit without changing the underlying CSV: structure reward is plotted on `0.0-0.1` with a dashed max line at `0.1`, and answer reward is plotted on `0.0-1.0` with a dashed max line at `1.0`.
- Added separate W&B scalar logging for reward components at `2026-06-05 21:34 EDT`: the repo-local RL trainer now reduces `structure_reward` and `answer_reward` from reward-extra arrays into `reward/structure_reward/{mean,max,min}` and `reward/answer_reward/{mean,max,min}` before the per-step logger call. This affects newly started or restarted jobs; already-running Python processes will not pick it up without restart/backfill. Validation passed: focused RL pytest (`2 passed, 19 deselected`; existing Ray/NPU warnings) and targeted pyright (`0 errors`; version update warning only).
- Added W&B scalar logging for answer diversity and zero-answer-reward prompts at `2026-06-05 22:00 EDT`: the RL trainer now logs `answer/given_answer_count`, `answer/unique_given_answer_count`, `answer/unique_given_answer_ratio`, `answer/prompt_count`, `answer/prompts_zero_answer_reward_all_rollouts_count`, and `answer/prompts_zero_answer_reward_all_rollouts_ratio` from reward-extra arrays. Regenerated centered 11-step plots for all six Qwen3.5 runs from SQLite leaf metrics, including running 2B ranges through branching `1-51`, epsilon `1-59`, structured `1-64`, and all 4B ranges through `1-125`. Outputs are `qwen35_all_unique_given_answer_ratio_centered11_20260605.png`, `qwen35_all_prompts_zero_answer_reward_all_rollouts_count_centered11_20260605.png`, and `qwen35_all_answer_diversity_zero_reward_centered11_20260605.csv` under the scratch plots directory. Validation passed: focused RL pytest (`3 passed, 19 deselected`; existing Ray/NPU warnings) and targeted pyright (`0 errors`; version update warning only).
- Refreshed the previous centered Qwen3.5 plots at `2026-06-05 22:12 EDT`: reward plots now include 2B branching through step `52`, 2B epsilon-greedy through `60`, 2B structured baseline through `65`, and all 4B runs through `125`; exec/steer plots were regenerated from reconstructed scored leaf paths using `generated_chunk` plus selected edge text. Updated files include `qwen35_all_reward_functions_centered11_20260605.csv`, `qwen35_all_structure_reward_centered11_20260605.png`, `qwen35_all_answer_reward_centered11_20260605.png`, `qwen35_all_with_running_2b_*_centered11_20260605.{png,csv}`, and `qwen35_2b_running_*_centered11_20260605.{png,csv}` under the scratch plots directory.
- Added W&B scalar logging for steer/exec block lengths and block counts at `2026-06-05 22:25 EDT`: the reward payload now emits `exec_block_count`, `exec_block_word_count`, `steer_block_count`, and `steer_block_word_count`, and the trainer reduces them into `blocks/{exec,steer}/num_blocks_total`, `num_blocks_per_leaf`, `words_total`, `words_per_leaf`, and `avg_words_per_block`. This affects newly started or restarted jobs; already-running Python processes will not pick it up without restart/backfill. Validation passed: focused RL pytest (`4 passed, 40 deselected`; existing Ray/NPU warnings) and targeted pyright (`0 errors`; version update warning only).
- Restarted the failed Qwen3.5 4B RL set at `2026-06-05 22:30 EDT` after cleaning failed 4B artifact/checkpoint roots for jobs `5480921`/`5480922`/`5480923`. New 4B jobs are `5495686` branching (`2**5` leaves, `branch_prob=0.07`, diverse selector), `5495687` structured baseline (`n=32`), and `5495688` epsilon-greedy (`n=32`), all on Ascend `quad` with 4 A100s, 72h, 400 steps, 32K responses, W&B online, and vLLM caps `524288`/`4096`. Submitted additional 2B branching ablations `5495689`/`5495690`/`5495691` using the same `2**5`/`0.07` branching setup, no high-clip widening (`clip_ratio_low=clip_ratio_high=0.2`), with variants `exec_temp=0.85`, `exec_temp=0.7` plus steer/exec `top_p=1.0`, and default temp/top-p without high-clip widening. Restarted shared artifact-root viewers on `http://127.0.0.1:51130/` and `http://127.0.0.1:51131/`.
- Fixed the shared branching viewer run-root index at `2026-06-05 23:03 EDT`: discovery now includes every `batch_*_step_*` SQLite child under an experiment root instead of collapsing each experiment to only its newest step before duplicate filtering. The "show older duplicate runs" checkbox can now reveal prior step DBs. Restarted the single login-1 viewer on `http://127.0.0.1:51130/`; the index now reports `sqlite_runs=208` and `hidden_duplicates=202`. Validation passed: focused visualizer pytest (`24 passed`) and targeted pyright (`0 errors`).
- Added repetition tracking and non-empty repeat forced-close text at `2026-06-06`: repeat-loop metadata now carries from Analysis `DecodeOutcome`/`LeafRollout` into RL reward extras, and the trainer logs W&B `repetition/*` forced-close counts, ratios, repeated-block counts, and similarity summaries. Repeat forced-close now writes `I am repeating now. Time to stop.` inside an open `<exec>` before appending `</exec>`, preventing empty exec blocks from this path. Validation passed: focused Analysis repeat-loop pytest (`4 passed`), focused RL repetition/reward/block pytest (`5 passed`; existing Ray/NPU warnings), targeted Analysis/RL pyright (`0 errors`), and a direct `DecodeOutcome -> LeafRollout -> leaf_event_payload` bridge check.
- Changed boxed-answer reward validation at `2026-06-06`: repeated boxed answers after the think block no longer create a structure issue, and prediction parsing now explicitly scores the final complete `\boxed{...}` expression. Validation passed: focused reward pytest (`5 passed, 17 deselected`) and targeted RL pyright (`0 errors`).
- Clarified the default RL dataset system prompt at `2026-06-06`: replaced the terse restriction-heavy prompt with guidance that `<steer>` blocks choose and check the next step while `<exec>` blocks carry out calculations/deductions, preserving the one-think-block alternating steer/exec format and final boxed answer. Validation passed: `python -m pytest BuildRLDataset/tests/test_build_rl_dataset.py -q` (`6 passed`).
- Regenerated `BuildRLDataset/output/train.parquet` at `2026-06-06` with the clarified system prompt by rerunning the export stage only. The existing answer-format export filter stayed active: `9149` train rows kept, with `8451` scalar/simple-fraction answers, `698` exact symbolic numeric answers, and `851` `other` rows skipped.
- Restarted the Qwen3.5 RL runs at `2026-06-06 00:55 EDT` with 8 prompts per step and 32 rollouts per prompt across all modes, and epsilon-greedy/branch candidate pools set to 50. Cancelled old jobs `5488502`/`5488503`/`5488504`, `5495686`/`5495687`/`5495688`, and pending ablations `5495689`/`5495690`/`5495691`; cleaned their scoped artifact/checkpoint roots while preserving Slurm logs. New running jobs are 2B branching `5496037`, 4B branching `5496040`, 4B structured baseline `5496041`, 4B epsilon-greedy `5496042`, and 2B branching ablations `5496043`/`5496044`; ablation `5496045` is pending. Initial 2B structured/epsilon jobs `5496038`/`5496039` hit a shared HuggingFace datasets `.incomplete` cache race, so the submit launcher now passes a per-submission/per-mode processed dataset cache and those two modes were retried as pending jobs `5496161`/`5496162`.
- Fixed ragged branching actor-update padding at `2026-06-06 01:39 EDT`: the existing auto-padding path only padded to actor DP width, but verl's actor update requires each DP shard to be divisible by `actor.ppo_mini_batch_size * rollout.n / dp_size`, so 8x32 branching batches with 181-228 realized leaves failed with assertions like `56 % 64 != 0`. The trainer now pads `update_actor`/`update_critic` batches to the global update mini-batch divisor while keeping DP-width padding for log-prob/value stages. Validation passed: focused RL padding pytest (`2 passed`) and targeted RL pyright (`0 errors`). Cleaned failed/cancelled branching artifact/checkpoint roots for jobs `5496037`, `5496040`, `5496043`, `5496044`, and `5496045`, then submitted patched replacements `5496400` (2B branching), `5496401` (4B branching), `5496402` (2B clip02 exec_temp=.85), `5496403` (2B clip02 top_p=1), and `5496404` (2B clip02 default).
- Extended OpenAI embedding retry backoff at `2026-06-06 13:02 EDT` after 2B branching/ablation and 4B epsilon runs failed on transient `OpenAI embedding error 500 after 5 attempts`. Embedding retries still use 5 attempts, but the waits are now `5s`, `10s`, `20s`, and `40s` before the final attempt. Validation passed: focused selector-runtime pytest (`4 passed`) and targeted Analysis pyright (`0 errors`). Cleaned failed embedding-error artifact/checkpoint roots for jobs `5496400`, `5496402`, `5496404`, and `5496042`, then submitted replacements `5498610` (2B branching), `5498611` (2B clip02 exec_temp=.85), `5498612` (2B clip02 default), and `5498613` (4B epsilon-greedy).
- Prepared Qwen3.5 Base (not post-trained) masked-SFT runs at `2026-06-08 13:02 EDT`: the base repos are `Qwen/Qwen3.5-2B-Base` and `Qwen/Qwen3.5-4B-Base`, both `model_type=qwen3_5` / `Qwen3_5ForConditionalGeneration`. Updated SFT to resolve Transformers from the upstream GitHub source commit `e569f888604774e4dd78ba9063ee70999109ef7e` because released `4.57.6` does not recognize `qwen3_5`, and added `qwen3_5` to the image-text loader path. Created `1e-5`, Zero2, 8-epoch configs for 2B/4B base with scratch model dirs `models/qwen35_{2b,4b}_base`, validated config parsing and `qwen3_5` auto-model mapping in a scratch UV env, then submitted preemptible-quad array `5512589`; Slurm estimates start at `2026-06-09T00:49:23` on `a0012`.
- Requeued the Qwen3.5 Base masked-SFT array at `2026-06-08 13:42 EDT` after `5512589` failed during setup: task 0 hit a shared scratch UV env removal race, and task 1 inherited that env while the shared train sbatch was syncing Eval dependencies despite `SKIP_EVAL=1`. The base wrapper now uses task-specific scratch envs, and `SFTTraining/slurm/train.sbatch` now uses the active `UV_PROJECT_ENVIRONMENT` for training while skipping Eval setup when disabled. Removed the corrupted shared env and submitted replacement preemptible-quad array `5512740`, currently pending with Slurm start estimate `2026-06-09T04:37:59`.
- Moved the Qwen3.5 Base masked-SFT runs to Cardinal `gpu` at `2026-06-08 13:43 EDT`: added a Cardinal-specific H100 array wrapper using `--partition=gpu` and `--gres=gpu:h100:4`, plus explicit Lmod bootstrap in both the Cardinal wrapper and shared SFT train sbatch because Cardinal batch shells did not define `module` by default. First Cardinal attempt `11377370` failed immediately with `module: command not found`; patched and resubmitted array `11377392`, then cancelled superseded Ascend arrays `5512740` and `5512823`. Verified `11377392_0` is running on `c0817` and `11377392_1` on `c0818`; both passed CUDA preflight, created task-specific scratch envs, and entered masked-SFT startup.
- Fixed Cardinal H100 Qwen3.5 Base masked-SFT startup at `2026-06-08 14:25 EDT`: added `tilelang>=0.1.11` to the SFT UV environment after `flash-linear-attention` rejected Hopper/Triton without TileLang, then restarted the requested Qwen3.5-2B Base LR sweep as array `11379844` on Cardinal `gpu`. Task mapping is `11379844_0` = `1e-5` for 12 epochs, `11379844_1` = `3e-6` for 8 epochs, and `11379844_2` = `1e-6` for 8 epochs; task 0 is running on `c0818`, has W&B run `2ea6368528ca4421acf92a61549354c6`, and reached optimizer steps. Per request, cancelled the in-progress Qwen3.5-4B Base run `11378713_1` after the 2B sweep was started and pruned its output to keep only `checkpoint-120` under the 4B output directory; prior 2B 8-epoch run `11378713_0` completed successfully.
- Pruned and restarted Qwen3.5 Base masked-SFT runs at `2026-06-08 15:45 EDT`: the latest 2B Base `1e-5` 12-epoch run had already been cancelled and retained checkpoints `240,300,360,420,480,540,600,660` because `save_total_limit=8` had pruned earlier saves, so the requested "ckpt 3" was interpreted as the third retained checkpoint and the output now contains only `checkpoint-360`. Added and submitted a fresh Qwen3.5-4B Base Cardinal `gpu` run at `7e-6` for 4 epochs as job `11382812` using Zero2 and 4 H100s; it is running on `c0818`, W&B run `f9324f408c1e46bd86247544490c3089`, and reached optimizer progress.
- Corrected the Qwen3.5-2B Base checkpoint retention at `2026-06-08 15:55 EDT`: the intended `1e-5` third save was `checkpoint-180`, but that checkpoint is no longer present in the latest 12-epoch output. Preserved the user-approved fallback by pruning the `3e-6` 8-epoch output to only `checkpoint-240`; the older 2B `1e-5` 8-epoch `checkpoint-180` still exists and was left intact.
- Finalized Qwen3.5 Base checkpoint retention at `2026-06-08 16:05 EDT`: the 4B `7e-6` 4-epoch Cardinal job `11382812` had already completed cleanly before cancellation was needed. Removed all other Qwen3.5 Base SFT output roots and non-checkpoint artifacts, including `final_model` and `benchmark_evals`; retained only the 2B `3e-6` `checkpoint-240` and 4B `7e-6` `checkpoint-180` scratch output directories.
- Exported the retained Qwen3.5 Base SFT checkpoints for RL and submitted the full Ascend `quad` comparison matrix at `2026-06-08 16:18 EDT`. New vLLM/RL export dirs are `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/SFTTraining/vllm_exports/qwen35_2b_base_structured_lr3e6_checkpoint-240_official_layout` and `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/SFTTraining/vllm_exports/qwen35_4b_base_structured_lr7e6_checkpoint-180_official_layout`; both validated in the RL env via config/tokenizer resolution, meta-model construction, and safetensors inspection. Capped post-`</think>` answer continuation requests at `2048` tokens with a distinct `post_think_toks_reached` stop reason. Submitted 2B jobs `5514223` structured baseline, `5514224` epsilon-greedy, and `5514225` branching; submitted 4B jobs `5514226` structured baseline, `5514227` epsilon-greedy, and `5514228` branching. All six request 4 A100s, 48 CPUs, 256G, 72h, 400 steps, 8 prompts per step, 32 rollouts per prompt, 32K responses, clip ratios `0.2/0.28`, exec temperature `0.7`, steer temperature `1.0`, 50 candidates for eps/branching pools, branching `2**5` shape with `branch_prob=0.07`, W&B online, and Triton GDN prefill. Validation passed: Analysis branch-executor pytest (`79 passed`) and targeted pyright (`0 errors`).
- Plotted `"Wait"` and `"Alternatively"` use in tagged generated blocks for `qwen35_2b_structured_baseline_a100quad400_p8n32_c50_bp5p007_retry_20260606_5496161` at `2026-06-08 16:43 EDT`: reconstructed full scored leaf text from typed `generated_chunk` rows keyed by `leaf_score` doc/node streams, counted case-insensitive word-boundary matches inside `<steer>`/`<exec>` blocks across 188 scored steps, skipped unscored step 189, and wrote `qwen35_2b_structured_baseline_a100quad400_p8n32_c50_bp5p007_retry_20260606_5496161_wait_alternatively_blocks_20260608_163308.{csv,png}` under the scratch RL plots directory.
- Restarted the Qwen3.5 Base SFT RL matrix at `2026-06-08 16:54 EDT` after jobs on Ascend nodes `a0010` and `a0011` repeatedly failed during CUDA device setup with `CUDA-capable device(s) is/are busy or unavailable`, and the only surviving run had initialized W&B in offline mode because the Slurm wrapper defaulted `WANDB_MODE` to `offline`. The Qwen3.5 smoke sbatch now defaults W&B to online. Cancelled the offline active jobs, removed failed/offline scratch checkpoint roots while preserving Slurm logs, and submitted online replacements excluding `a0010,a0011`: 2B jobs `5514385` structured baseline, `5514386` epsilon-greedy, `5514387` branching; 4B jobs `5514388` structured baseline, `5514389` epsilon-greedy, `5514390` branching.
- Fixed and restarted the 2B Qwen3.5 Base SFT epsilon-greedy run at `2026-06-08 17:27 EDT`: job `5514386` had reached online W&B and active vLLM generation, then failed in step 1 when OpenAI embeddings returned a transient Cloudflare `520` and the embedding retry predicate did not classify it as retryable. Embedding HTTP retries now cover all `5xx` statuses while keeping auth/client errors fail-fast. Validation passed: focused selector-runtime pytest (`12 passed`) and targeted pyright (`0 errors`). Cleaned failed `5514386` checkpoint/artifact roots and submitted replacement `5514588` with W&B online and `a0010,a0011` excluded.
- Cleared inactive Qwen3.5 branching-DAPO SQLite event DBs at `2026-06-08 17:18 EDT`: preserved active Slurm roots for jobs `5514385`/`5514386`/`5514387` plus pending IDs `5514388`/`5514389`/`5514390`, removed `3,844` old `tree_events.sqlite`/WAL/SHM/lock files from non-active artifact roots, and freed about `2967.7 GiB`. Verification left exactly three active `tree_events.sqlite` DBs under the running 2B replacement runs.
- Fixed the branching-DAPO advantage estimator at `2026-06-08 21:35 EDT` so branch reward credit is token-local instead of broadcast across every generated token. Branch metadata now carries generated-token offsets for each path branch; inter-prompt credit starts only after the first branch offset, and each recursive intra-branch delta activates only after its own branch point. Validation passed: reward pytest (`22 passed`), direct branch-aware advantage regression with integer response masks, direct offset resolver regression, and targeted pyright (`0 errors`). The broader manager pytest is currently blocked by the local UV environment's PyTorch/torchvision CUDA major-version mismatch, not by this change.
- Restarted branching-only Qwen3.5 Base SFT RL jobs at `2026-06-08 21:38 EDT` to pick up the token-local advantage estimator. Cancelled old 2B branching `5514387` and pending 4B branching `5514390`, removed the old 2B scoped checkpoint/artifact roots, and submitted replacements `5516073` (2B) and `5516074` (4B) with the same online 32-rollout, 8-prompt, 5-branchpoint, 50-candidate, `branch_prob=0.07`, exec-temp `0.7`, steer-temp `1.0`, and clip-high `0.28` setup.
- Corrected the branching-DAPO token-local estimator at `2026-06-08 21:53 EDT` so recursive intra-branch deltas do not propagate downstream into descendant branch segments. Each branch delta now applies only to its unbranched token span `[branch_offset, next_branch_offset)`, while the final inter-prompt reward component applies only after the deepest branch. Validation passed: direct segment-local intra-advantage regression, reward pytest (`22 passed`), and targeted pyright (`0 errors`). Superseded queued jobs `5516073`/`5516074` before they started and submitted corrected replacements `5516099` (2B) and `5516100` (4B).
- Corrected the branching-DAPO inter/intra split at `2026-06-08 22:10 EDT`: inter-prompt GRPO advantage is again applied across the full valid response, while intra-branch deltas remain segment-local and never propagate past the next branch point. Simulations showed sibling-std normalization collapses binary branch deltas to nearly pure `±2`, so the recommended default for binary branching is raw local deltas (or prompt/global scaling later), not local sibling-std normalization. Validation passed: reward pytest (`22 passed`), direct inter-everywhere plus segment-local intra regression, and targeted pyright (`0 errors`). Superseded queued jobs `5516099`/`5516100` before they started and submitted replacements `5516311` (2B) and `5516312` (4B).
- Restarted the Qwen3.5 Base SFT RL comparison subset at `2026-06-08 22:25 EDT` with 16K response length and 16 rollouts per prompt. Submitted 2B branching `5516408`, 2B structured baseline `5516409`, 4B branching `5516410`, and 4B structured baseline `5516411` on Ascend `quad`; all four started immediately. Branching uses fanout `2`, max branch points `4` for a `2**4=16` leaf target, 50 candidates, `branch_prob=0.07`, exec temp `0.7`, steer temp `1.0`, and online W&B. After verifying all four new jobs were queued/running, cancelled older jobs `5516311`, `5516312`, `5514588`, `5514389`, `5514388`, and `5514385`, and removed their scoped scratch checkpoint/artifact roots while preserving logs.
- Added prompt-weighted answer pass-rate logging at `2026-06-09 12:04 EDT`: the RL trainer now computes per-prompt answer pass rates from realized leaves and logs their average as `answer/problem_pass_rate_mean`, with min/max companion metrics. This is the average of problem pass rates, distinct from the global leaf-weighted `reward/answer_reward/mean`. Targeted pyright passed (`0 errors`); local pytest/direct imports are currently blocked before execution by the existing PyTorch/torchvision CUDA major-version mismatch in the RL UV environment.
- Added retroactive SQLite plotting for prompt-weighted RL problem pass rates at `2026-06-09 12:28 EDT`: `Analysis/scripts/plot_rl_problem_passrates.py` scans completed step DBs, averages `answer_reward` within each problem, then averages across problems per step. Generated `qwen35_base_sft_16k_n16_problem_passrates_20260609.{csv,png}` under the scratch RL plots directory for active jobs `5516408`/`5516409`/`5516410`/`5516411`. Validation passed: targeted Analysis pyright (`0 errors`).
- Added centered rolling smoothing to the retroactive RL problem-passrate plotter at `2026-06-09 12:47 EDT`: `--center-window 11` writes centered problem/leaf mean columns and plots the centered 11-step average with partial edge windows. Generated `qwen35_base_sft_16k_n16_problem_passrates_centered11_20260609.{csv,png}` from the active jobs' latest completed SQLite step DBs. Validation passed: targeted Analysis pyright (`0 errors`).
- Fixed dynamic viewer leaf reconstruction at `2026-06-10`: structured-baseline leaves like `leaf_node_root_rollout_14_6297` now hydrate clicked leaf details from the parent `decode:<node_id>` typed vLLM trajectory when no old `baseline:*` stream exists, so final unpaired `think_close_continuation` text/tokens are included instead of falling back to truncated `leaf_score.text_preview`. Verified the reported step-200 doc0 leaf now returns `22,662` chars / `6,217` tokens ending in `\boxed{2}` over HTTP on `127.0.0.1:51130`; restarted the login01 viewer. Validation passed: visualizer pytest (`25 passed`) and targeted pyright (`0 errors`).
- Added a steer-only RL actor update mode at `2026-06-10`: `RL_UPDATE_MODE=steer_only` keeps normal rollout/reward/advantage computation but temporarily masks the actor update to tokens inside complete `<steer>...</steer>` blocks, with selected-token metrics and fail-fast handling when no steer tokens are present. Validation passed: new update-mask pytest (`5 passed`), launcher `bash -n`, and targeted RLTraining pyright (`0 errors`); the heavier rollout-manager pytest remains blocked by the existing PyTorch/torchvision CUDA-major mismatch.
- Moved the branching exec/steer handoff boundary at `2026-06-10`: exec decode now stops on excluded `</exec` and normalizes to the post-exec decision point, so the steer decode policy chooses either a fresh `<steer>` block or `</think>`. Post-exec steer candidates now must start, after whitespace, with `<steer>` or `</think>`; candidates with no tag or prose before a later `<steer>` terminate with `missing_steer_or_think_close` instead of being normalized into an empty/malformed steer block. The malformed termination now emits graph-visible `malformed_steer_decision` events and renders distinctly in verification-vs-stop heatmaps. Validation passed: Analysis branch-executor pytest (`83 passed`), visualizer pytest (`28 passed`), steer-normalization pytest (`16 passed`), targeted post-exec/malformed regression pytest (`4 passed`), targeted visualizer heatmap pytest (`1 passed`), and targeted Analysis pyright (`0 errors`).
- Fixed steer-only span accumulation at `2026-06-10 17:02 EDT` after the live Qwen3.5 gs50 steer-only jobs showed huge pre-clip grad norms from masks selecting only about `0.05-0.10%` of response tokens. SQLite event data for structured steer-only step 3 had `299,375` `steer_block_generated` tokens, but the actor update mask selected only `1,054`; the branch executor was updating `PathState.steer_phase_token_spans` without updating the local span accumulator used by final leaf outcomes. Non-terminal steer continuations now use the same `adopt_decode_state` helper, preserving all generated steer-phase spans through later termination. Validation passed: focused branch-executor/steer-span pytest (`5 passed`) and targeted Analysis pyright (`0 errors`).
- Cancelled invalid steer-only jobs `5529150`/`5529151` after the fix, removed their scoped artifact/checkpoint roots while keeping Slurm logs, and submitted fixed replacements `5529982` (structured steer-only) and `5529981` (branching steer-only). The all-token controls `5529148`/`5529149` were left running.
- Corrected steer-only RL masking at `2026-06-10` to use tracked steer-phase token spans instead of regex-matching only text inside `<steer>...</steer>`. Branch-selected candidates and non-branch/epsilon steer continuations now carry response-token spans for all tokens generated by steer-phase requests, including generated decision tags, normalization suffixes, and terminal `</think>` candidates; RL forwards those spans through `reward_scores` and masks actor updates from them. Validation passed: update-mask pytest (`7 passed`), Analysis steer-span pytest (`2 passed`), selected branch-executor steer/think-close pytest (`41 passed`), targeted RLTraining pyright (`0 errors`), and targeted Analysis pyright (`0 errors`).
- Diagnosed large steer-only RL gradient norms at `2026-06-10 16:59 EDT`: active steer-only jobs were selecting only about `0.05-0.11%` of response tokens for actor updates, while SQLite step data showed `13,391` steer block events but only `36/128` structured-baseline leaves with any serialized steer span. The bug was span accumulation: fresh steer trigger outcomes did not carry prior `steer_phase_token_spans`, so later tracking replaced most path spans with only the latest span or no span on malformed termination. Fixed the branch executor span append helper to preserve accumulated path spans before adding the next steer-request delta. Validation passed: Analysis steer-span pytest (`4 passed`), branch-executor pytest (`83 passed`), and targeted Analysis pyright (`0 errors`).
- Normalized steer-only actor-update gradient scale at `2026-06-10 17:45 EDT`: after span tracking was fixed, steer-only masks correctly selected about `9-16%` of response tokens, but verl's token-mean actor loss divided by the smaller steer-only mask denominator and amplified gradients relative to all-token runs. The trainer still masks actor updates to steer-phase tokens only, but now scales actor-update advantages by `selected_tokens / full_response_tokens` during the temporary masked update and restores the full tensors afterward; it also logs `actor/update_mask/loss_scale`. Cancelled amplified steer-only jobs `5529981`/`5530019`, cleaned their scoped scratch roots, and submitted scaled replacements `5530281` (branching steer-only) and `5530282` (structured steer-only) while leaving all-token controls `5529148`/`5529149` running. Validation passed: update-mask pytest (`7 passed`) and targeted RLTraining pyright (`0 errors`); broader trainer pytest remains blocked by the local PyTorch/torchvision CUDA-major mismatch.
- Corrected the Qwen3.5 gs50 branch steer-only comparison job at `2026-06-10 21:30 EDT`: historical n16 comparison logs and the active branch all-token control used `rollout_n=16`, while the scaled branch steer-only replacement `5530281` had inherited the older branching launcher default `N_RESP_PER_PROMPT=1`. Cancelled `5530281`, removed its scoped artifact/checkpoint roots while preserving Slurm logs, and submitted corrected replacement `5533662` with `N_RESP_PER_PROMPT=16`, fanout `2`, max branch points `4`, 50 candidates, `branch_prob=0.07`, epsilon `0.1`, and `RL_UPDATE_MODE=steer_only`. The runtime config printed `rollout_n=16` and `update_mode='steer_only'` before vLLM startup.
- Corrected steer-only actor-update scaling at `2026-06-10 22:11 EDT`: the linear `selected_tokens / full_response_tokens` advantage scale made both steer-only jobs' gradient norms about half of their all-token controls. The actor update still masks gradients to steer-phase tokens only, but the applied scale is now `sqrt(selected_tokens / full_response_tokens)`, which matches the expected variance change from taking a token-mean over a selected token subset. Validation passed for `tests/test_update_masking.py` (`7 passed`) and targeted RLTraining pyright (`0 errors`); the full trainer test remains blocked before collection by the local PyTorch/torchvision CUDA-major mismatch. Cancelled under-scaled steer-only jobs `5533662`/`5530282`, cleaned their scoped roots, and submitted replacements `5534124` (branching steer-only n16) and `5534125` (structured steer-only).
- Fixed exec-close duplication in branching prefixes at `2026-06-10 23:20 EDT`: live SQLite rows showed vLLM sometimes returned text ending in partial `</exec` while also reporting `stop_reason="</exec"`, and the branch executor appended a full `</exec>` marker, producing malformed `</exec</exec>` prefix tails in the visualization and follow-up prompts. Explicit exec-stop completion now appends only the missing suffix (`>` for partial `</exec`) and keeps completed close tags unchanged. Validation passed: steer-normalization plus branch-executor pytest (`100 passed`) and targeted Analysis pyright (`0 errors`).
- Tightened exec-close stop completion at `2026-06-10 23:25 EDT`: the explicit exec-stop path now completes any non-empty suffix prefix of `</exec>` rather than only the common `</exec` partial. Added a regression that checks every prefix length from `<` through `</exec`. Validation passed: steer-normalization plus branch-executor pytest (`100 passed`) and targeted Analysis pyright (`0 errors`).
- Restricted exec-close completion to explicit string stop reasons at `2026-06-10 23:29 EDT`: max-length finishes already route through the length-boundary handler, and integer/non-string stop reasons no longer trigger the steer-boundary prefix completion path. Validation passed: steer-boundary, steer-normalization, and branch-executor pytest (`106 passed`) plus targeted Analysis pyright (`0 errors`).
- Corrected length-boundary normalization at `2026-06-10 23:36 EDT`: `finish_reason="length"` now normalizes/repairs partial boundary-tag prefixes only when the decode request was capped by the per-chunk decode limit, while exhausting the full response/context budget remains a hard leaf stop. Added regressions for chunk-limited partial `</exec>` repair and response-budget hard stop. Validation passed: branch-executor, steer-normalization, and steer-boundary pytest (`108 passed`) plus targeted Analysis pyright (`0 errors`).
- Stored final leaf completions directly at `2026-06-11 00:00 EDT`: `leaf_completed`/`leaf_scored` now persist full completion text, RL leaf scoring forwards the full text instead of only previews, and typed `node_event` rows carry final event text for the dynamic viewer. Removed final-output reconstruction from the viewer payload/UI path; leaf inspectors now render saved completion text only, while decode/candidate events still show token probability strips. Added right-pane previous/next event navigation and moved low-priority event metadata into collapsible details. Validation passed: visualizer/event DB pytest (`38 passed`), branch-executor pytest (`91 passed`), targeted Analysis pyright (`0 errors`), and targeted RLTraining pyright (`0 errors`).
- Restarted the active Qwen3.5 gs50 steer-only pair at `2026-06-11 00:06 EDT` with `max_steer_tokens=30`. The shared RL launch path now exposes `MAX_STEER_TOKENS` through `run_branching_dapo_olmo3.sh`, `run_branching_dapo_qwen35_smoke.sh`, and `submit_qwen35_smoke_matrix.sh`; shell syntax validation passed and both replacement submissions passed `sbatch --test-only`. Submitted replacement jobs `5535477` (branching steer-only n16, run label `branch-steer-sqrt-n16-steer30`) and `5535476` (structured steer-only, run label `struct-steer-sqrt-steer30`), then cancelled superseded jobs `5534124`/`5534125` and removed only their scoped checkpoint/artifact roots while preserving Slurm logs.
- Cleaned superseded gs50 steer-only run clutter at `2026-06-11 00:15 EDT`: removed the old steer-only Slurm logs for jobs `5529150`, `5529151`, `5529981`, `5529982`, `5530019`, `5530281`, `5530282`, `5533662`, `5534124`, and `5534125`, and deleted their per-job dataset cache directories under `Analysis/cache/datasets/jobs/`. Verified the remaining gs50 checkpoint/artifact roots are only the active steer30 jobs `5535476`/`5535477` plus the all-token controls `5529148`/`5529149`.
- Restarted the full Qwen3.5 gs50 four-job comparison at `2026-06-11 00:24 EDT` after the earlier partial restart only replaced the steer-only pair. Submitted replacement jobs `5535625` (branch all, run label `gs50_branch_all_steer30`), `5535626` (struct all, run label `gs50_struct_all_steer30`), `5535627` (branch steer-only n16), and `5535628` (struct steer-only), all with `max_steer_tokens=30`, `400` training steps, `save_freq=20`, and the same rollout/update settings as the superseded runs. Cancelled the previous active four jobs `5529148`, `5529149`, `5535476`, and `5535477`, then removed only their scoped checkpoint/artifact roots and per-job dataset caches while preserving Slurm logs. Verified from the new startup config dumps that all four replacements report `max_steer_tokens: 30`.
- Re-restarted the Qwen3.5 gs50 four-job comparison at `2026-06-11 00:55 EDT` to restore W&B logging after the previous restart came up with `trainer.logger=['console']` only. Changed the Qwen smoke launch defaults so both `run_branching_dapo_qwen35_smoke.sh` and `submit_qwen35_smoke_matrix.sh` default to `["console","wandb"]`, validated both scripts with `bash -n`, cancelled console-only jobs `5535625`, `5535626`, `5535627`, and `5535628`, and removed only their scoped checkpoint/artifact roots plus per-job dataset caches. Submitted replacement jobs `5536007` (branch all), `5536008` (struct all), `5536009` (branch steer-only n16), and `5536010` (struct steer-only) with the same gs50 4B model, `max_steer_tokens=30`, `400` steps, `save_freq=20`, and the same rollout/update settings as the canceled set. Verified from all four new startup config dumps that `trainer.logger` is now `['console', 'wandb']`.
- Tightened the dynamic viewer inspector at `2026-06-11 01:16 EDT`: vLLM event details now keep the right pane focused on summary, prefix tail, and generated tokens, with event/request/response/choice metadata collected into bottom dropdowns instead of appearing above the content. Prompt and generated-chunk bookkeeping also moved to the bottom metadata area. Validation passed: visualizer helper pyright (`0 errors`) and Python syntax compilation.
- Made selected tree events visually explicit at `2026-06-11 01:21 EDT`: clicking an event chip or stepping through events now redraws the SVG immediately and adds an orange outline halo around the selected event in the tree. Validation passed: visualizer helper pyright (`0 errors`) and Python syntax compilation.
- Tightened RL answer scoring at `2026-06-11 01:31 EDT`: `branching_dapo.reward_fn.parse_prediction_answer` no longer falls back to parsing the raw tail text when no complete `\\boxed{...}` answer is present. Reward accuracy now considers only complete boxed answers in the post-think answer text; missing-boxed responses return no extracted prediction and `raw_answer_acc=False`. Validation passed: focused reward pytest (`22 passed` with repo `PYTHONPATH`) and targeted RLTraining pyright (`0 errors`).
- Fixed malformed repeat forced-close handling at `2026-06-11 01:31 EDT`: repetition termination now closes the active steer/exec tag stack to a think-close-ready boundary instead of normalizing to a new `<steer>` prompt and appending `</think>` inside it. The post-exec steer-decision gate now rejects malformed candidates such as `<steer></think>` or nested control tags while still allowing stop-excluded valid steer candidates. Validation passed: focused steer-boundary/branch-executor pytest (`95 passed`) and targeted Analysis pyright (`0 errors`).
- Tightened steer-repeat termination at `2026-06-11 02:07 EDT`: when a steer continuation triggers repetition termination, the triggering repeated steer candidate is discarded instead of being added and then force-closed. The forced close now appends only `</think>` at the clean pre-candidate decision boundary, and the synthetic close token is recorded in `steer_phase_token_spans` for steer-only RL masking. Steer repetition similarity now uses cleaned steer text with edge control tags/partial tags removed and a stricter `0.92` threshold. Validation passed: focused repetition/branch-executor/viewer pytest (`91 passed`) and targeted Analysis pyright (`0 errors`).
- Added explicit actor-LR overrides to the shared RL launch path at `2026-06-11 13:54 EDT`: `run_branching_dapo_olmo3.sh`, `run_branching_dapo_qwen35_smoke.sh`, and `submit_qwen35_smoke_matrix.sh` now respect `ACTOR_LR` instead of baking in `1e-6`, while preserving the current default. Bash syntax validation passed. Verified the live gs50 structured-all job `5536458` is still on base LR `1e-6` with `lr_warmup_steps=10` (step-1 `actor/lr=1e-7`). Submitted two additional 4B gs50 structured-baseline jobs against the same 16K/n16/all-token/steer30 shape and merged-HF model without touching the active four-job comparison: `5541944` (`q35-gs50-struct-all-lr1e7-steer30`, run label `gs50_struct_all_lr1e7_steer30`, `ACTOR_LR=1e-7`) and `5541945` (`q35-gs50-struct-all-lr1e5-steer30`, run label `gs50_struct_all_lr1e5_steer30`, `ACTOR_LR=1e-5`). Both `sbatch --test-only` checks succeeded before submission; both jobs are currently pending on `quad` with reason `Priority`.
- Replaced the extra gs50 LR pair at `2026-06-11 14:01 EDT` after the original `1e-7` and `1e-5` jobs had already started. Cancelled running jobs `5541944` and `5541945`, waited for them to leave `squeue`, and removed only their scoped checkpoint roots plus per-job dataset caches while preserving Slurm logs. Reused the same 4B merged-HF gs50 structured-baseline shape (quad A100 x4, 400 steps, 16K response, `rollout_n=16`, `max_steer_tokens=30`, W&B online) but resubmitted as `5547242` (`q35-gs50-struct-all-lr5e6-steer30`, run label `gs50_struct_all_lr5e6_steer30`, `ACTOR_LR=5e-6`, `RL_UPDATE_MODE=all`) and `5547243` (`q35-gs50-struct-steer-sqrt-lr3e6-steer30`, run label `gs50_struct_steer_sqrt_lr3e6_steer30`, `ACTOR_LR=3e-6`, `RL_UPDATE_MODE=steer_only`). Both replacement `sbatch --test-only` checks succeeded before submission.
- Replaced the extra gs50 structured LR pair with branching LR runs at `2026-06-12 09:31 EDT`: sacct showed `5547242` failed after step 60 on `assert_no_text_after_first_steer_close` because a steer candidate ended as `</steer></think>`, and `5547243` was still running slowly at step 51 after 11h18m. Cancelled stalled `5547243`, waited for it to leave Slurm, and removed only the scoped checkpoint roots plus per-job dataset caches for `5547242`/`5547243`. Tightened `branch_decode_utils` so a bare terminal `</think>` immediately after `</steer>` is accepted/preserved, while other non-whitespace suffixes still fail. Validation passed: focused steer-boundary pytest (`10 passed`) and targeted Analysis pyright (`0 errors`). Submitted the requested 4B gs50 branching pair with `ACTOR_LR=3e-6`, `rollout_n=16`, selector `embed_diverse_topk_random`, branch prob `0.07`, fanout `2`, max branch points `4`, candidates `50`, epsilon `0.1`, `max_steer_tokens=30`, W&B online: `5553508` (`q35-gs50-branch-all-lr3e6-steer30`, `RL_UPDATE_MODE=all`) and `5553509` (`q35-gs50-branch-steer-sqrt-lr3e6-steer30`, `RL_UPDATE_MODE=steer_only`). Both `sbatch --test-only` checks succeeded and both jobs started on `quad`.
- Cancelled the original hung gs50 structured steer-only run at `2026-06-12 12:53 EDT`: job `5536460` (`q35-gs50-struct-steer-sqrt-steer30`, experiment `qwen35_4b_branch_gs50_structured_baseline_gs50_struct_steer_sqrt_steer30_5536460`) was still running after 1d10h and had only reached about step 114 in the log. Issued `scancel`, waited for it to leave Slurm, confirmed sacct state `CANCELLED`, and removed only its scoped checkpoint root plus dataset cache while preserving Slurm logs. Remaining live jobs are `5536458` structured-all, `5553508` branching all-token 3e-6, and `5553509` branching steer-only 3e-6.
- Submitted an HF-base Qwen3.5 4B structured baseline at `2026-06-12 13:04 EDT`: validated `Qwen/Qwen3.5-4B-Base` in the RL env as `model_type=qwen3_5` / `Qwen3_5ForConditionalGeneration`, then launched job `5563246` (`q35-hf4b-struct-all-lr3e6-steer30`) on `quad` with `MODEL_PATH=Qwen/Qwen3.5-4B-Base`, `ACTOR_LR=3e-6`, `RL_UPDATE_MODE=all`, `rollout_n=16`, `max_steer_tokens=30`, 400 steps, save every 20, W&B online, and project `branching_dapo_qwen35_base_hf`. The `sbatch --test-only` forecast passed before submission, the job started on `a0007`, and the startup config printed the intended model path, LR, structured-baseline mode, and all-token update mode.
- Fixed the dynamic run picker at `2026-06-12 14:01 EDT` so HF-base Qwen3.5 runs are visible and identifiable: `hf4b_base`/`base_hf` run names now render the variant as `base_hf`, and the login02 `51130` viewer was restarted with the active HF-base 4B structured run root included. Verified the served index shows `5563246` as a non-duplicate row with latest batch `batch_0008_step_000009` and passrate `0.0469 (n=8)`.
- Fixed steer decision-boundary routing at `2026-06-13 EDT`: the first Qwen `<think>` boundary and branch-disabled post-`</exec>` resume boundaries now sample `steer_single_candidate` before any generic `decode_chunk`, so generated `<steer>...</steer>`/`</think>` decision text is tracked as steer-phase output instead of exec decode text. The initial `<think>` boundary no longer receives a synthetic `<steer>` insertion before sampling, preserving model-generated boundary tags for steer-only masking. Validation passed: branch-executor plus steer-normalization pytest (`106 passed`) and targeted Analysis pyright (`0 errors`).
- Submitted an additional Qwen3.5 4B gs50 structured-baseline all-token run at `2026-06-13 EDT` without cancelling any existing jobs. `sbatch --test-only` predicted immediate start on `a0003`; submitted job `5610079` (`q35-gs50-struct-all-lr3e6-steer30`) on `quad` with the merged gs50 HF model, `ACTOR_LR=3e-6`, `RL_UPDATE_MODE=all`, `rollout_n=16`, `max_steer_tokens=30`, 16K responses, 400 steps, save every 20, and W&B online. Startup config verified the intended model path, LR, experiment name, project, all-token update mode, and training shape; the job is running on `a0003`.
- Submitted an additional Qwen3.5 4B gs50 structured-baseline all-token exec-temperature-1 run at `2026-06-13 EDT` without cancelling any existing jobs. `sbatch --test-only` predicted immediate start on `a0012`; submitted job `5610395` (`q35-gs50-struct-all-lr3e6-execT1`) on `quad` with the merged gs50 HF model, `ACTOR_LR=3e-6`, `EXEC_TEMPERATURE=1.0`, `RL_UPDATE_MODE=all`, `rollout_n=16`, `max_steer_tokens=30`, 16K responses, 400 steps, save every 20, and W&B online. Startup config verified runtime rollout `temperature: 1.0`, model path, LR, experiment name, project, all-token update mode, and training shape; the job is running on `a0012`.
- Added `Analysis/scripts/plot_steer_exec_faithfulness.py` at `2026-06-14 EDT` for steer/exec embedding faithfulness plots. It samples 25 evenly spaced scored step DBs per run and up to 200 valid adjacent steer/exec pairs per selected step from `leaf_score.text`, strips `<think>`/`<steer>`/`<exec>` tags before embedding, uses `text-embedding-3-small` with batch size `1024`, and caches only SHA256 keys plus float32 vectors in `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/RLTraining/qwen35_branching_dapo/faithfulness_cache/20260614/embeddings.sqlite`. The real run covered roots `qwen35_4b_branch_gs50_branching_gs50_branch_all_lr3e6_steer30_5553508_20260612T133359Z` and `qwen35_4b_branch_gs50_structured_baseline_gs50_struct_all_steer30_5536458_20260611T062724Z`, sampled `10,000` pairs, cached `18,937` vectors, and wrote only plots under `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/RLTraining/qwen35_branching_dapo/faithfulness/20260614/`: `steer_exec_similarity_over_steps.png`, `steer_exec_similarity_vs_correctness.png`, and `steer_exec_similarity_by_correctness_over_steps.png`. Validation passed: focused pytest (`6 passed`), targeted pyright (`0 errors`), and a fake-embedding dry run over 2 steps x 3 pairs.
- Cancelled the active Qwen3.5 4B gs50 branching steer-only run at `2026-06-14 EDT`: issued `scancel` for job `5553509` (`q35-gs50-branch-steer-sqrt-lr3e6-steer30`), confirmed `sacct` state `CANCELLED by 47915`, waited for it to clear from `squeue`, and left all other jobs untouched. No artifact cleanup was performed.
- Added OpenRouter embedding fallback for `embed_diverse_topk_random` at `2026-06-15 EDT`: the selector now accepts `OPEN_ROUTER_KEY` when `OPENAI_API_KEY` is absent and uses `openai/text-embedding-3-small` through OpenRouter after OpenAI embedding retries are exhausted. Coalesced embedding batches are keyed by the concrete primary/fallback provider shape, and selector runtime now forwards both keys for sync and async paths. Validation passed: focused selector-runtime pytest (`15 passed`) and targeted Analysis pyright (`0 errors`; version update warning only).
- Added a reward-scoring timeout guard for branching DAPO at `2026-06-15 EDT`: `_attach_reward_outputs` now wraps each async reward-loop score request in a 60s timeout and assigns score `0.0` plus explicit `reward_timeout` metadata when a scorer hangs, preventing one stuck Math-Verify parse/verify call from blocking the whole training step. Reward extra keys are now unioned across rows so a timeout in row zero does not suppress normal reward metrics. Added a focused timeout regression. Validation passed: targeted RLTraining pyright (`0 errors`) and Python compile; focused pytest remains blocked by the local PyTorch/torchvision CUDA-major mismatch during test collection.
- Added RL segment advantage labels to the branching tree viewer at `2026-06-15 EDT`: the trainer now persists per-segment mean combined token advantages into typed SQLite `node_advantage` rows after branch advantage computation, and RL tree labels prefer those signed advantage values over hierarchy prefixes while falling back to hierarchy labels when rows are missing.
- Submitted a new Qwen3.5 4B gs50 branchp10 all-token run at `2026-06-15 13:51 EDT` after validating `quad` availability with `sbatch --test-only`. Job `5654647` (`q35-gs50-branch-all-lr2e6-bp10`) is running on `a0022` with the merged gs50 HF model, `ACTOR_LR=2e-6`, selector `embed_diverse_topk_random`, branch probability `0.10`, fanout `2`, max branch points `4`, candidates `50`, epsilon `0.1`, `rollout_n=16`, `max_steer_tokens=30`, 16K responses, 400 steps, save every 20, W&B online, and all-token updates.
- Decoupled branching DAPO answer accuracy from structure validity at `2026-06-15 14:22 EDT`: `answer_acc`/`answer_reward` now follow raw boxed-answer correctness, while `format_valid`/`structure_reward` remain independent. Validation passed: focused reward pytest (`22 passed`) and targeted pyright (`0 errors`). Cancelled the misconfigured 4B gs50 branchp10 LR-2e-6 run `5654647` after it used `train_batch_size=4`/`max_prompt_length=2048`, removed only its scoped artifact/checkpoint roots plus dataset cache, and submitted replacement job `5654897` (`q35-gs50-branch-all-lr2e6-bp10`) on `quad`. Startup config verified the intended old branchp10 shape with only LR changed: merged gs50 HF model, `ACTOR_LR=2e-6`, `train_batch_size=8`, `max_prompt_length=1024`, 16K responses, `rollout_n=16`, selector `embed_diverse_topk_random`, branch probability `0.10`, fanout `2`, max branch points `4`, candidates `50`, epsilon `0.1`, `max_steer_tokens=30`, 400 steps, save every 20, W&B online, and all-token updates.
- Tightened the branching viewer registry startup at `2026-06-15 15:33 EDT`: run-root discovery now supports child-prefix filtering and a latest-batch-only mode, and stale registry refreshes run in the background instead of blocking the index request. Restarted the login02 `51130` viewer with the qwen35 artifact parent, `--run-root-prefix qwen35`, and `--latest-batch-only`; the index now serves the latest SQLite batch per qwen35 experiment quickly while still reading run pages directly from SQLite. Validation passed: focused visualizer pytest (`30 passed`) and targeted pyright (`0 errors`).
- Fixed large live-run landing pages in the branching viewer at `2026-06-15 15:43 EDT`: the per-run index now avoids full `event_log` aggregates for large SQLite DBs, uses typed progress rows or distinct typed node attempts for doc links, and returns a bounded loading page while the first cold SQLite read warms in the background. Stale `/runs/<id>/` URLs now return a fast `404` instead of forcing synchronous scratch discovery. Verified the live LR-2e-6 branchp10 run page first responds in `<1s`, then serves the warmed doc-link page in `0.034s`. Validation passed: focused visualizer pytest (`31 passed`) and targeted pyright (`0 errors`).
- Restored the previous visual event model in the dynamic branching tree viewer at `2026-06-15 17:36 EDT`: the graph payload again renders lightweight merged `vllm_step` events while heavy generated text/probability details stay lazy-loaded through the existing event-detail endpoint. The live login02 `51130` viewer is now focused on the current LR-2e-6 branchp10 RL experiment root without `--latest-batch-only`, so the index shows all batch steps from `batch_0000_step_000001` through `batch_0008_step_000009`. Verified `batch_0008` doc 0 graph payload returned vLLM-step events and event `38` hydrated `11` token rows. Also fixed the tree outcome precedence so raw-correct but format-invalid leaves display the yellow-circle emoji instead of being hidden by verification truthiness. Validation passed: Python syntax compilation for touched viewer modules and live HTTP checks on `51130`.
- Cleaned dead branching-viewer code at `2026-06-15 17:55 EDT`: removed the superseded generated-chunk graph SQL/readers, unused full-attempt vLLM reader, unused node-attempt identity reader, obsolete generated-chunk graph index, and stale payload sampling/token helpers left over from the reverted graph model. The viewer still uses lightweight `vllm_step` graph rows and lazy event-detail hydration. Restarted `51130` on the focused LR-2e-6 branchp10 root; index shows `10` batch-step links, `batch_0008` doc 0 graph returns `77` vLLM-step-style events, and event `38` hydrates `11` token rows. Validation passed: Python syntax compilation and live HTTP checks on `51130`.
- Cleaned git state at `2026-06-16 EDT`: classified the dirty tree before staging, found no untracked generated artifacts requiring new ignore rules, and kept the new untracked files as source/test/script additions. The only ignored clutter observed was existing local cache/venv output that is already excluded. Kept the local `RLTraining/verl` vLLM compatibility patch out of the pushed parent history so the parent repo still points at the upstream submodule SHA, then committed the accumulated branching/RL/viewer/SFT source changes.
- Removed the old JSONL/static branching stack at `2026-06-16 EDT`: deleted the unused single-prompt branching engine, static report/bundle builders, JSONL replay/payload viewer modules, compare/migration scripts, and their tests. Kept the active SQLite dynamic viewer by extracting shared `AttemptKey` and selected-progress helpers into small modules, then updated docs to point at `tree_events.sqlite` and `scripts/serve_branching_viz.py`. Validation passed: focused viewer pytest (`24 passed`), adjacent branching event-source/viewer pytest (`34 passed`), targeted pyright (`0 errors`; version update warning only), and a stale-reference scan that only found the active run-registry module.
- Removed the old Unsloth SFT path at `2026-06-16 EDT`: deleted `SFTTraining/sft_training/train_unsloth.py`, the ignored Unsloth run config, stale Unsloth pycache, and `SFTTraining/unsloth_compiled_cache`, then removed the README warning for that alternate environment. Source/docs filename and text sweeps no longer find Unsloth references outside this history note. Validation passed: `compileall` for `sft_training`, config tests (`8 passed`), and targeted pyright for the active SFT trainer/config modules (`0 errors`). Full SFT pyright/pytest remain blocked by pre-existing `non_sequitur_masking.py` typing plus local Python 3.9/TRL/NumPy collection issues.
- Audited the Qwen3.5 branching-DAPO RL launch/config flow at `2026-06-16 EDT`: traced the submitter, Slurm wrapper, Qwen35 wrapper, generic Hydra override wrapper, Python task runner config dump, and a recent `5654897` startup log. No code changes were made beyond this note; the main finding is that run shape is repeated and mutated across shell environment layers, making typed run-spec generation the highest-leverage cleanup.
- Added typed Qwen3.5 RL run-spec generation at `2026-06-16 EDT`: `branching_dapo/run_specs.py` now owns Slurm resources, shared training/decode shape, actor runtime switches, per-mode rollout defaults, manifest writing, dry-run, and `sbatch --test-only`/submit behavior. `submit_qwen35_smoke_matrix.sh` now delegates to the typed launcher, and real submissions mirror `run_spec.json` into the predicted checkpoint directory after parsing the Slurm job id. The local ignored `branching_dapo_qwen35_smoke.sbatch` wrapper also copies `RUN_SPEC_JSON` at job startup when present. Documented the new path and examples in `RLTraining/README.md`. Validation passed: focused run-spec pytest (`3 passed`), targeted run-spec pyright (`0 errors`), Python compile, `bash -n`, default four-mode dry-run, and a branchp10 `sbatch --test-only` forecast with no queued job left behind.
- Cleaned branching engine dead contracts at `2026-06-16 EDT`: removed the JSONL-era `TreeEvent`, deleted entropy-trigger config/plumbing from Analysis and branching-DAPO, removed sync executor compatibility wrappers, moved frontier scheduling, request-runtime bookkeeping, and non-branch steer continuation into focused helper modules, and changed per-doc diagnostics from `doc_diagnostics.jsonl` to canonical `doc_diagnostics_recorded` SQLite event rows while documenting `clustering_debug.jsonl` as an intentional raw selector trace. Validation passed: branch-executor pytest (`90 passed`), run-matrix/event-source pytest (`22 passed`), selector-runtime pytest (`15 passed`), touched-file Python compile, and stale-reference scans. Analysis pyright is still blocked only by missing optional `lm_eval`; RL pytest/pyright are blocked in this shell by missing project deps/settings (`tensordict`/`pytest`, `math_verify`, `verl`, and Python-version config).
- Aligned the dynamic branching viewer with canonical SQLite doc diagnostics at `2026-06-16 EDT`: the tree payload now exposes the latest `doc_diagnostics_recorded` payload under `meta.diagnostics` while keeping that document-level event out of node replay chips. Added a regression covering diagnostics metadata plus graph exclusion. Validation passed: focused visualizer pytest (`25 passed`), adjacent run-matrix/event-source pytest (`22 passed`), Python compile for touched files, targeted visualizer pyright (`0 errors`), and stale visualization scans for old JSONL artifacts.
- Added configurable RL branching tree-log retention at `2026-06-16 EDT`: completed `batch_####_step_######/tree_events.sqlite` logs now persist every `PERSISTENT_LOG_INTERVAL_STEPS` trainer steps while current and failed steps remain available for live viewing/debugging. The setting flows through `BranchingRolloutSettings`, the Qwen3.5 typed run specs, the Hydra launcher, and `run_spec.json` manifests. Validation passed: run-spec pytest (`3 passed`), touched-file Python compile, shell syntax check, targeted pyright (`0 errors`), and dry-run manifest verification; broader `test_branching_dapo.py` remains blocked at collection by the local PyTorch/torchvision CUDA-major mismatch.
- Diagnosed failed Qwen3.5 4B gs50 branchp10 all-token run `5654897` at `2026-06-16 EDT`: Slurm state was `FAILED`/`1:0` after `06:10:26` on `a0022`, not timeout. The job completed trainer metrics through step 17 and failed while generating `batch_0017_step_000018`. The root cause was vLLM CUDA OOM, not the later HTTP 500 wrapper: vLLM scheduled `523841` tokens, `200` running requests, `max_num_batched_tokens=524288`, `max_concurrent_branches=512`, and multiple TP workers had only about `3.9-4.1 GiB` free before a `4.50 GiB` allocation. The artifact DB `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/RLTraining/qwen35_branching_dapo/artifacts/qwen35_4b_branch_gs50_branching_gs50_branch_all_lr2e6_branchp10_steer30_20260615T182257Z/batch_0017_step_000018/tree_events.sqlite` was preserved for inspection; no cleanup or restart was performed.
- Cleaned branching-eval launch configs at `2026-06-16 EDT`: moved 21 checkpoint-specific OLMo/Qwen YAML launch snapshots from `Analysis/branching_eval/*.yaml` into `Analysis/branching_eval/archive/checkpoint_run_configs/`, leaving only maintained `example_*.yaml` files at the top level. Added typed eval run-spec generation in `branching_eval/run_specs.py`, a generic non-mutating Slurm wrapper `Analysis/slurm/branching_eval.sbatch`, focused tests, and docs showing the generated `config.yaml`/`run_spec.json` path for real checkpoint evals. Also created the Codex `$eval-job` skill under `/users/PAA0201/ollieproudman/.codex/skills/eval-job` for starting, monitoring, restarting, and documenting eval runs. Validation passed: eval run-spec/config pytest (`15 passed`), targeted pyright (`0 errors`; version warning only), shell syntax check, dry-run generation, and skill validation.
- Reduced Qwen3.5 RL vLLM batch-token cap at `2026-06-16 EDT`: changed the typed matrix default and direct Qwen35 wrapper fallback from `524288` to `420000` for `ROLLOUT_MAX_NUM_BATCHED_TOKENS`, leaving GPU memory utilization, max seqs, branchp10 rollout shape, and other launch settings unchanged. Added a focused run-spec assertion for the new resolved value. Validation passed: run-spec pytest (`3 passed`), targeted run-spec pyright (`0 errors`), shell syntax checks, and a branchp10 dry-run manifest check showing `ROLLOUT_MAX_NUM_BATCHED_TOKENS=420000` with the previous surrounding shape intact. Removed the dry-run scratch submission directory afterward.
- Restarted failed Qwen3.5 4B gs50 branchp10 all-token run `5654897` at `2026-06-16 13:05 EDT`: `sbatch --test-only` accepted the same production `quad` 4xA100, 48 CPU, 256G, 72h request and forecast queued start on `a0001` at `2026-06-18T05:19:11`. Removed only the failed run's scoped checkpoint root, timestamped artifact root, and dataset cache while preserving Slurm logs. Submitted replacement job `5667691`, currently `PENDING` on `quad` with reason `Priority`. The copied `run_spec.json` confirms the intended branchp10 shape with `ROLLOUT_MAX_NUM_BATCHED_TOKENS=420000`, `gpu_memory_utilization=0.80`, `max_num_seqs=4096`, `train_batch_size=8`, `max_prompt_length=1024`, `max_response_length=16384`, `rollout_n=16`, selector `embed_diverse_topk_random`, branch probability `0.10`, fanout `2`, max branch points `4`, candidates `50`, epsilon `0.1`, `ACTOR_LR=2e-6`, `400` steps, save every `20`, W&B online, and all-token updates.
- Moved the `qwen35_4b_branch_gs50_branching_gs50_branch_all_lr3e6_branchp10_steer30_5611877` branching viewer to login02 at `2026-06-16 13:20 EDT`: stopped the stale login02 `51130` LR-2e-6 viewer and the temporary login01 copy, then restarted `Analysis/scripts/serve_branching_viz.py` on `ascend-login02.hpc.osc.edu:51130` against `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/RLTraining/qwen35_branching_dapo/artifacts/qwen35_4b_branch_gs50_branching_gs50_branch_all_lr3e6_branchp10_steer30_5611877_20260614T031807Z`. Verified `GET /`, the latest run page `batch_0216_step_000217-0ac8443f`, doc 0 HTML, and the doc graph JSON endpoint from login02.
- Added a training-step column to the dynamic branching viewer run picker at `2026-06-16 13:26 EDT`: `Analysis/scripts/visualize_branching_run_registry.py` now parses `batch_####_step_######` names into a human step value and shows it on the root DB selector page without extra SQLite reads. Restarted the login02 `51130` viewer for `5611877`; verified the root table header includes `step` and the latest `batch_0216_step_000217` row shows `217`.
- Restarted cancelled Qwen3.5 4B gs50 structured-baseline all-token run `5611096` at `2026-06-16 14:22 EDT`: the old job `q35-gs50-struct-all-lr3e6-rerun` had been cancelled after reaching step `114` on `a0012`. `sbatch --test-only` accepted the same production `quad` 4xA100, 48 CPU, 256G, 72h request and forecast queued start on `a0001` at `2026-06-18T08:19:11`. Removed only the old run's scoped checkpoint root, timestamped artifact root, and dataset cache while preserving Slurm logs. Submitted replacement job `5668624`, currently `PENDING` on `quad`. The copied `run_spec.json` confirms the intended structured-baseline shape with `ROLLOUT_MAX_NUM_BATCHED_TOKENS=420000`, `gpu_memory_utilization=0.80`, `max_num_seqs=4096`, `train_batch_size=8`, `max_prompt_length=1024`, `max_response_length=16384`, `rollout_n=16`, selector `random`, branch probability `0.0`, epsilon `0.0`, `ACTOR_LR=3e-6`, `400` steps, save every `20`, W&B online, and all-token updates.
- Restarted the login02 branching viewer at `2026-06-16 14:42 EDT` after the interrupted multi-run attempt left `51130` stopped. The viewer is again focused on `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/RLTraining/qwen35_branching_dapo/artifacts/qwen35_4b_branch_gs50_branching_gs50_branch_all_lr3e6_branchp10_steer30_5611877_20260614T031807Z`; verified the server process, `127.0.0.1:51130` bind, root page response, step-column header, `5611877`, and latest `batch_0216_step_000217` row.
- Visualized `qwen35_4b_branch_gs50_branching_gs50_branch_all_lr3e6_steer30_5553508` on login02 at `2026-06-16 14:53 EDT`: found non-empty SQLite DBs under `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/RLTraining/qwen35_branching_dapo/artifacts/qwen35_4b_branch_gs50_branching_gs50_branch_all_lr3e6_steer30_5553508_20260612T133359Z` through latest `batch_0333_step_000334`, replaced the `51130` viewer with that run root, and verified the root page, step-column header, latest row, run page, doc 0 page, and graph JSON. These DBs are older-schema artifacts: the latest DB has `event_log` rows such as `vllm_request`/`vllm_response` and `node_event` data but no current typed `vllm_step` table, so the current viewer shows nodes while event-chip streams may be sparse.
- Checked the requested single-GPU serve target `qwen35_4b_branch_gs50_structured_baseline_gs50_struct_all_lr3e6_steer30_retry_5611096` at `2026-06-16 EDT`: the checkpoint root now contains only `run_spec.json` after the earlier scoped cleanup, with no `global_step_*`, `actor/huggingface`, `config.json`, or weight files. No Slurm serving job was submitted because there is no valid model checkpoint path. Nearby `5611097` execT1 checkpoints still have HF actor exports through `global_step_160`, and replacement structured job `5668624` is still pending without new checkpoints.
- Exported and queued serving for `qwen35_4b_branch_gs50_branching_gs50_branch_all_lr3e6_steer30_5553508` checkpoint `global_step_200` at `2026-06-17 EDT`: merged the FSDP actor checkpoint into HF/vLLM format at `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/RLTraining/qwen35_branching_dapo/served_models/qwen35_4b_branch_gs50_branching_gs50_branch_all_lr3e6_steer30_5553508_global_step_200_hf` using `verl/scripts/legacy_model_merger.py`, producing a 9.7G `model.safetensors`. A first 1-GPU preemptible-quad vLLM job exposed a stale `--disable-log-requests` flag and was replaced. Submitted corrected 2-GPU `nextgen` job `5683897` with tensor parallel size 2, 3h walltime, port `18000`, served model name `qwen35-4b-5553508-step200`; Slurm currently forecasts `2026-06-19 04:30` on `a0120`. Added executable stdlib streaming helper `RLTraining/scripts/stream_q35_4b_5553508_step200.py`; validation passed via Python compile.
- Switched the `5553508` checkpoint-200 vLLM serve job to one preemptible-quad A100 at `2026-06-17 EDT`: cancelled pending 2-GPU nextgen job `5683897`, submitted job `5689165` on `preemptible-quad` with 1 A100, 8 CPUs, 96G, 3h, port `18000`, and the corrected `--no-enable-log-requests` flag. The server is running on `a0012`, `/v1/models` responds for served model `qwen35-4b-5553508-step200`, and `RLTraining/scripts/stream_q35_4b_5553508_step200.py --max-tokens 48` streamed a smoke response successfully.
- Restarted the `5553508` checkpoint-200 vLLM serve job at `2026-06-17 14:42 EDT` after 16K completion requests hit HTTP 400 against the original 16K context cap. Replaced job `5689165` with job `5689251` on `preemptible-quad`/`a0012`, raised `--max-model-len` to `17408`, updated the streaming helper default job id and HTTP-error reporting, verified `/v1/models`, verified a streaming request with `max_tokens=16384` returns HTTP 200, and smoke-tested `RLTraining/scripts/stream_q35_4b_5553508_step200.py --max-tokens 16`.
- Moved the active branching viewer from login02 to login01 at `2026-06-17 14:48 EDT`: stopped the old login02 `51130` server and restarted `Analysis/scripts/serve_branching_viz.py` on login01 `127.0.0.1:51130` with the same two run roots, `qwen35_4b_branch_gs50_structured_baseline_gs50_struct_all_lr3e6_steer30_retry_5611096_20260616T222538Z` and `qwen35_4b_branch_gs50_branching_gs50_branch_all_lr2e6_branchp10_steer30_20260616T214458Z`. Verified the login01 root page and confirmed no login02 viewer process remains.
- Restarted the login01 `51130` branching viewer at `2026-06-18 13:12 EDT` for all currently RUNNING Qwen3.5 RL jobs: `5667691` branchp10 LR-2e-6, `5668624` structured LR-3e-6, `5683760` structured LR-2e-6 execT1, and `5689172` structured LR-2e-6. The server now runs with all four matching artifact roots and serves `sqlite_runs=74`; login02 has no remaining viewer process. The existing duplicate-hiding UI still hides older same-key structured rows until "show older duplicate runs" is enabled.
- Fixed the live branching viewer root crash at `2026-06-18 14:06 EDT`: retention deleted a cached live batch directory such as `batch_0247_step_000248` between registry refreshes, and `RunRegistry.entries()` crashed while sorting by `run_dir.stat()`, causing an empty HTTP response. The registry now drops vanished entries during sorting instead of failing the request. Restarted login01 `51130` with the same four running Qwen3.5 RL artifact roots; `GET /` returns `200`, reports `sqlite_runs=75`, and includes all four roots.
- Refreshed the login01 `51130` branching viewer at `2026-06-18 17:12 EDT` to match the currently RUNNING jobs only. Slurm had two running visualization-backed RL jobs: `5683760` (`qwen35_4b_branch_gs50_structured_baseline_gs50_struct_all_lr2e6_execT1_steer30_retry_5611097_20260617T154138Z`) and `5705876` (`qwen35_4b_branch_gs50_structured_baseline_gs50_struct_all_lr3e6_steer30_repcheck_off_retry_5611096_20260618T205045Z`). Restarted `Analysis/scripts/serve_branching_viz.py` on login01 with those two roots; `GET /` returns `200` with `sqlite_runs=16`, includes both running roots, and excludes the now-cancelled branchp10 and LR-2e-6 non-execT1 roots.
- Reconfigured the login01 `51130` viewer at `2026-06-18 17:22 EDT` to show all retained steps for the two currently RUNNING jobs instead of latest-batch-only. The root page returns `200`, reports `sqlite_runs=16`, includes only the `5683760` LR-2e-6 execT1 root and `5705876` LR-3e-6 repcheck-off root, and excludes the cancelled branchp10 and LR-2e-6 non-execT1 roots.
- Submitted Qwen3.5 4B gs50 structured-baseline exec-temperature-1 LR-2e-6 rerun from failed job `5611097` at `2026-06-17 11:08 EDT`: used the old `q35-gs50-struct-all-lr3e6-execT1-rerun` config as the source but changed `ACTOR_LR` from `3e-6` to `2e-6` and named the new experiment `qwen35_4b_branch_gs50_structured_baseline_gs50_struct_all_lr2e6_execT1_steer30_retry_5611097`. `sbatch --test-only` accepted the production `quad` 4xA100, 48 CPU, 256G, 72h request and forecast start on `a0001` at `2026-06-17T18:12:58`. Removed only the failed LR-3e-6 run's scoped checkpoint root, timestamped artifact root, and dataset cache while preserving Slurm logs. Submitted job `5683760`, currently `PENDING` on `quad` with reason `Priority`. The copied `run_spec.json` confirms `EXEC_TEMPERATURE=1.0`, `ROLLOUT_MAX_NUM_BATCHED_TOKENS=420000`, `gpu_memory_utilization=0.80`, `max_num_seqs=4096`, `train_batch_size=8`, `max_prompt_length=1024`, `max_response_length=16384`, `rollout_n=16`, selector `random`, branch probability `0.0`, epsilon `0.0`, `ACTOR_LR=2e-6`, `400` steps, save every `20`, W&B online, and all-token updates.
- Filled steer-trigger branch fanout after candidate deduplication at `2026-06-17 EDT`: `_selected_ids_for_branch` now keeps selector order, prefers unique steer text, backfills from remaining pool candidates after text deduplication, and repeats candidate IDs only if the pool itself is smaller than `branch_fanout`, so triggered branch points reach the configured fanout whenever the pool is non-empty. Validation passed: focused steer-selection pytest (`3 passed`), adjacent branch-executor selector pytest (`3 passed, 87 deselected`), Python compile, and targeted pyright (`0 errors`).
- Submitted a separate Qwen3.5 4B gs50 structured-baseline non-execT1 LR-2e-6 run from the cancelled `5611096` shape at `2026-06-17 14:28 EDT`: left the active LR-3e-6 replacement job `5668624` running and used experiment `qwen35_4b_branch_gs50_structured_baseline_gs50_struct_all_lr2e6_steer30_retry_5611096`. `sbatch --test-only` accepted the production `quad` 4xA100, 48 CPU, 256G, 72h request and forecast start on `a0022` at `2026-06-17T14:27:33`; real job `5689172` is running on `a0022`. No cleanup was performed because the old `5611096` scoped roots had already been removed and this is a new LR-2e-6 variant. The copied `run_spec.json` confirms `EXEC_TEMPERATURE=0.7`, `ROLLOUT_MAX_NUM_BATCHED_TOKENS=420000`, `gpu_memory_utilization=0.80`, `max_num_seqs=4096`, `train_batch_size=8`, `max_prompt_length=1024`, `max_response_length=16384`, `rollout_n=16`, selector `random`, branch probability `0.0`, epsilon `0.0`, `ACTOR_LR=2e-6`, `400` steps, save every `20`, W&B online, and all-token updates.
- Tightened repeat-loop forced-close thresholds at `2026-06-18 15:39 EDT`: exec repetition now requires `0.90` normalized similarity and `4` repeated exec blocks; steer repetition now requires exact `1.0` normalized similarity and `5` repeated steer blocks. The similarity lookback window remains `3` same-family blocks and the production token floor remains `5000` generated tokens. Validation passed: branch-executor repeat-loop pytest (`4 passed, 86 deselected`), repetition-parser pytest (`4 passed`), Python compile for the repetition runtime files, and targeted Analysis pyright (`0 errors`; version update warning only).
- Added `Analysis/scripts/plot_wait_counts_over_steps.py` at `2026-06-18 EDT` for raw whole-word `wait` tracking over retained RL step DBs. The run covered `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/RLTraining/qwen35_branching_dapo/artifacts/qwen35_4b_branch_gs50_structured_baseline_gs50_struct_all_lr3e6_steer30_retry_5611096_20260616T222538Z`, skipped empty/in-progress DBs, and used all `25` saved scored steps from trainer step `10` through `250`. It counted `92,288` literal punctuation-aware `wait` instances across `90,117,994` rollout characters, with `1,024.0796` waits per million characters, `330,380` exec blocks, and mean exec-block length `205.27` characters. Correlations were `r=0.563` for raw wait count vs total exec chars and `r=-0.249` for wait rate vs mean exec-block chars. Wrote only plots under `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/RLTraining/qwen35_branching_dapo/wait_counts/20260618/qwen35_4b_branch_gs50_structured_baseline_gs50_struct_all_lr3e6_steer30_retry_5611096_20260616T222538Z/`: `wait_raw_count_over_steps.png`, `wait_per_million_chars_over_steps.png`, and `wait_rate_vs_exec_block_length.png`. Validation passed: focused pytest (`4 passed`) and targeted pyright (`0 errors`).
- Updated the normalized `wait` plot at `2026-06-18 EDT` to overlay mean exec-block characters over time on a right-hand y-axis in `wait_per_million_chars_over_steps.png`. Regenerating the run now included newly scored trainer step `251`, so the snapshot uses `26` saved steps from `10` through `251`: `95,823` raw `wait` instances, `94,230,522` rollout chars, `1,016.8998` waits per million chars, `358,900` exec blocks, mean exec-block length `195.84` chars, raw wait vs total exec chars `r=0.562`, and wait-rate vs mean exec-block chars `r=-0.209`. Validation passed: focused pytest (`4 passed`) and targeted pyright (`0 errors`).
- Added an RL repetition-truncation knob and queued the paired Qwen3.5 structured-baseline comparison at `2026-06-18 16:34 EDT`: `REPETITION_CHECKING_ENABLED` now flows through typed Qwen35 run specs, Slurm/Hydra launch env, `BranchingRolloutSettings`, `BranchingConfig`, and the branch executor; when false, repeated exec/steer block state updates and forced-close checks are skipped. Submitted `5705877` (`qwen35_4b_branch_gs50_structured_baseline_gs50_struct_all_lr3e6_steer30_repcheck_on_retry_5611096`, repetition checking on) and `5705876` (`qwen35_4b_branch_gs50_structured_baseline_gs50_struct_all_lr3e6_steer30_repcheck_off_retry_5611096`, repetition checking off), both pending on `quad` with reason `Priority`. Both match the `5611096` LR-3e-6 structured-baseline shape: 4xA100, 48 CPU, 256G, 72h, Qwen3.5 4B merged HF model, prompt batch `8`, `rollout_n=16`, max prompt `1024`, max response `16384`, max model len `17408`, max steer tokens `30`, `ROLLOUT_MAX_NUM_BATCHED_TOKENS=420000`, W&B online, `400` steps, save every `20`. Validation passed: run-spec pytest (`3 passed`), Analysis config pytest (`13 passed`), branch-executor repeat-loop pytest (`4 passed, 86 deselected`), direct RL settings parser check, shell syntax checks, Python compile, targeted RL/Analysis pyright (`0 errors`; version warning only), and both `sbatch --test-only` forecasts. The broader `test_branching_dapo.py` slice remains blocked at collection by the local PyTorch/torchvision CUDA major-version mismatch.
- Stopped two active Qwen3.5 RL jobs at `2026-06-18 16:46 EDT` at user request: cancelled job `5668624` for `qwen35_4b_branch_gs50_structured_baseline_gs50_struct_all_lr3e6_steer30_retry_5611096` and job `5667691` for `qwen35_4b_branch_gs50_branching_gs50_branch_all_lr2e6_branchp10_steer30`. `sacct` reports both as `CANCELLED` after leaving `squeue`; Slurm logs/checkpoint/artifact roots were preserved and no cleanup was performed. The newly queued repetition-check comparison jobs `5705876`/`5705877` remain pending, and the LR-2e-6 structured jobs `5683760`/`5689172` remain running.
- Replaced the active non-execT1 LR-2e-6 structured-baseline retry run at `2026-06-18 16:50 EDT`: cancelled job `5689172` for `qwen35_4b_branch_gs50_structured_baseline_gs50_struct_all_lr2e6_steer30_retry_5611096` on `a0022` after `1-02:19:21`, preserving its logs/checkpoints/artifacts because this was a requested stop rather than failed-run cleanup. Submitted replacement job `5706085` as `qwen35_4b_branch_gs50_epsilon_greedy_gs50_struct_all_lr3e6_eps30_steer30_retry_5611096`, pending on `quad`. The copied checkpoint-side `run_spec.json` confirms `mode=epsilon_greedy`, `EPSILON_GREEDY_PROB=0.30`, `ACTOR_LR=3e-6`, `ROLLOUT_MAX_NUM_BATCHED_TOKENS=420000`, `gpu_memory_utilization=0.80`, `max_num_seqs=4096`, prompt batch `8`, `rollout_n=16`, max prompt `1024`, max response `16384`, max model len `17408`, max steer tokens `30`, repetition checking enabled, W&B online, `400` steps, and save every `20`.
- Submitted a second eps-greedy Qwen3.5 4B gs50 LR-3e-6 run without repetition checking at `2026-06-20 00:23 EDT`: job `5732897`, experiment `qwen35_4b_branch_gs50_epsilon_greedy_gs50_struct_all_lr3e6_eps30_steer30_repcheck_off_retry_5611096`, currently pending on `quad`. This mirrors running eps-greedy job `5706085` except `REPETITION_CHECKING_ENABLED=False`. `sbatch --test-only` accepted the 4xA100, 48 CPU, 256G, 72h request and forecast a `quad` start; the copied checkpoint-side `run_spec.json` confirms `mode=epsilon_greedy`, `EPSILON_GREEDY_PROB=0.30`, `ACTOR_LR=3e-6`, `ROLLOUT_MAX_NUM_BATCHED_TOKENS=420000`, `gpu_memory_utilization=0.80`, `max_num_seqs=4096`, prompt batch `8`, `rollout_n=16`, max prompt `1024`, max response `16384`, max model len `17408`, max steer tokens `30`, W&B online, `400` steps, and save every `20`.
- Updated the login01 `51130` branching viewer at `2026-06-20 14:46 EDT` to show all retained steps for the four currently RUNNING Qwen3.5 RL jobs (`5732897`, `5706085`, `5705877`, `5705876`) plus the explicit older execT1 root `qwen35_4b_branch_gs50_structured_baseline_gs50_struct_all_lr2e6_execT1_steer30_retry_5611097_20260617T154138Z`. Verified `GET /` returns `200`, reports `sqlite_runs=122`, includes all five roots, and login02 has no `51130` viewer listener.
- Moved the existing five-root branching viewer from login01 to login02 at `2026-06-21 20:06 EDT`: stopped the login01 `51130` process and restarted `Analysis/scripts/serve_branching_viz.py` on `ascend-login02.hpc.osc.edu:51130` with the same retained-step roots (`5732897`, `5706085`, `5705877`, `5705876` roots plus explicit older execT1 `5611097`). Verified login02 `GET /` returns `200`, reports `sqlite_runs=174`, includes all five roots, and login01 has no `51130` viewer listener. Slurm now reports only `5732897` still RUNNING, so the viewer intentionally preserves the prior visible root set rather than narrowing to the single currently running job.
- Changed the Qwen eps-greedy default candidate pool to match the branching default at `2026-06-21 20:19 EDT`: `RLTraining/branching_dapo/run_specs.py` and `RLTraining/scripts/run_branching_dapo_qwen35_smoke.sh` now default eps-greedy `NUM_CANDIDATES` to `50` instead of `4`, while preserving `branch_fanout=1`, `max_branch_points=1`, and `EPSILON_GREEDY_PROB` defaults/overrides. Restarted the latest eps-greedy repcheck-off run by cancelling job `5732897` (`num_candidates=4`), cleaning only its scoped checkpoint root, artifact root, and dataset cache while preserving Slurm logs, then submitting replacement job `5776849` as `qwen35_4b_branch_gs50_epsilon_greedy_gs50_struct_all_lr3e6_eps30_cand50_steer30_repcheck_off_retry_5732897`. The copied checkpoint-side `run_spec.json` confirms `mode=epsilon_greedy`, `num_candidates=50`, `branch_fanout=1`, `EPSILON_GREEDY_PROB=0.30`, `ACTOR_LR=3e-6`, `REPETITION_CHECKING_ENABLED=False`, `ROLLOUT_MAX_NUM_BATCHED_TOKENS=420000`, prompt batch `8`, `rollout_n=16`, max response `16384`, W&B online, `400` steps, and save every `20`. Validation passed: `RLTraining/tests/test_run_specs.py` (`3 passed`), targeted pyright (`0 errors`; version warning only), shell syntax checks, dry-run manifest inspection, and `sbatch --test-only`.
- Added response-length and curated failure-type analysis for the completed repcheck-on structured-baseline run at `2026-06-21 20:43 EDT`: `Analysis/scripts/plot_response_lengths_and_failures.py` reads retained `tree_events.sqlite` step DBs via `leaf_score`/`leaf_metric`, plots average response tokens for correct vs incorrect leaves, and plots both primary mutually exclusive failure categories and overlapping curated failure signals. Ran it on `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/RLTraining/qwen35_branching_dapo/artifacts/qwen35_4b_branch_gs50_structured_baseline_gs50_struct_all_lr3e6_steer30_repcheck_on_retry_5611096_20260618T215954Z/batch_0399_step_000400`; outputs are under `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/RLTraining/qwen35_branching_dapo/response_failure_analysis/20260621/qwen35_4b_branch_gs50_structured_baseline_gs50_struct_all_lr3e6_steer30_repcheck_on_retry_5611096_20260618T215954Z/`. The run has `39` saved scored steps, `4992` leaves, overall passrate `0.3794`, and a late collapse after about step `300`: post-collapse steps `310-400` average `1927` tokens for correct leaves vs `1024` for incorrect leaves, with primary failures dominated by malformed steer/think boundaries (`579`, `54.8%` of incorrect) plus missing boxed answers (`209`, `19.8%`) and wrong boxed answers (`191`, `18.1%`). Validation passed: `uv run black`, Python compile, targeted pyright (`0 errors`; version warning only), regenerated plots, and visual inspection of all three PNGs.
- Tightened the branching-DAPO reward structure validator at `2026-06-21 23:11 EDT` after checking malformed steer-decision edge cases. All `1046` retained `missing_steer_or_think_close` leaves from the repcheck-on structured run remain invalid, but synthetic full responses with nested or mismatched control tags inside `<steer>...</steer>` had been accepted; `validate_steer_exec_sequence` now emits `control_tag_inside_steer_exec_block` for those cases. Validation passed: focused reward pytest (`23 passed`), pyright with the RL venv (`0 errors`), Python compile, and a direct retained-step validator sweep.
- Closed the follow-up `<</steer>` reward-validator hole at `2026-06-21 23:22 EDT`: doubled close tags inside matched steer/exec blocks could be swallowed as a normal close with a dangling `<` in the segment content, so the validator now treats dangling `<` before a close as `control_tag_inside_steer_exec_block`. Focused probes for stray and in-block `<</steer>`/`<</exec>` are invalid; validation passed reward pytest (`23 passed`), pyright (`0 errors`), and Python compile.
- Expanded branching-DAPO reward-validator edge-case coverage at `2026-06-21 23:39 EDT` and penalized incomplete control-tag fragments such as `<stee</exec>`. The validator now treats partial `think`/`steer`/`exec` tag prefixes inside steer/exec content as `control_tag_inside_steer_exec_block` while preserving ordinary math comparisons like `x < y`. Added parametrized tests for valid comparison/prose cases plus duplicated opens/closes, malformed spellings, attributes, ordering, think-boundary, boxed-answer, nested-control, and incomplete-tag failures. Validation passed: reward pytest (`62 passed`), pyright (`0 errors`), Python compile, and retained malformed-stop sweep (`0/1046` accepted).
- Removed the malformed steer-boundary truncation flow at `2026-06-21 23:58 EDT`: post-`</exec>` single-steer continuations and selected steer candidates that do not begin with a valid `<steer>`/`</think>` decision are now retained as ordinary continued path text instead of producing terminal `missing_steer_or_think_close` leaves or new `malformed_steer_decision` events. Reward validation still penalizes malformed structure at scoring time. Validation passed: branch-executor pytest (`90 passed`), steer-selection pytest (`3 passed`), targeted pyright (`0 errors`), Python compile, and active-runtime grep confirmed no remaining `missing_steer_or_think_close`/`malformed_steer_decision` references in `branch_executor.py` or `steer_decode_flow.py`.
- Restarted the latest Qwen3.5 eps-greedy repcheck-off run at `2026-06-22 01:05 EDT` so fresh workers load the malformed-boundary truncation removal. Failed job `5776849` had already exited `FAILED`; removed only its scoped checkpoint root, timestamped artifact root, submission manifest root, and dataset-cache root while preserving Slurm logs. Submitted replacement job `5794408` on `quad` as `q35rl-epsilon-greedy`, with checkpoint manifest `qwen35_4b_branch_gs50_epsilon_greedy_gs50_struct_all_lr3e6_eps30_cand50_steer30_repcheck_off_retry5732897_5794408/run_spec.json`. Verified the copied manifest keeps the intended shape: `mode=epsilon_greedy`, epsilon `0.30`, `num_candidates=50`, branch fanout `1`, max branch points `1`, LR `3e-6`, prompt batch `8`, rollout `n=16`, max response `16384`, max steer tokens `30`, repetition checking disabled, `ROLLOUT_MAX_NUM_BATCHED_TOKENS=420000`, `ROLLOUT_MAX_NUM_SEQS=4096`, W&B logging, `400` steps, save every `20`. `sbatch --test-only` passed before submission; job `5794408` is pending with Slurm start time `2026-06-22T09:00:00`. A duplicate pending submit `5794410` was cancelled immediately and its duplicate checkpoint-manifest root was removed.
- Diagnosed failed restart job `5794408` at `2026-06-22 10:47 EDT`: it started on `a0004`, completed training steps `1` and `2`, then vLLM EngineCore died during the next generation wave from CUDA OOM. The decisive log line is `CUDA out of memory. Tried to allocate 3.60 GiB. GPU 0 ... 2.87 GiB is free`, with scheduler stats showing `num_running_reqs=400`, `total_num_scheduled_tokens=419585`, and `kv_cache_usage=0.4987` under `ROLLOUT_MAX_NUM_BATCHED_TOKENS=420000` / `ROLLOUT_MAX_NUM_SEQS=4096`. The later `'State' object has no attribute 'server'` / `VllmRequestError` is a secondary HTTP error after the engine died. The partial DB had no `malformed_steer_decision` events, confirming the fresh worker path included the malformed-boundary truncation removal. Cleaned only the scoped failed checkpoint root, timestamped artifact root, submission manifest root, and dataset-cache root while preserving Slurm logs.
- Restarted failed eps-greedy job `5794408` at `2026-06-22 11:42 EDT` as Slurm job `5854067`, keeping old DB/artifacts untouched after the restart request. The new run `qwen35_4b_branch_gs50_epsilon_greedy_gs50_struct_all_lr3e6_eps30_cand50_btok320k_steer30_repcheck_off_retry5794408_5854067` preserves the previous shape except for lowering `ROLLOUT_MAX_NUM_BATCHED_TOKENS` from `420000` to `320000` while keeping `ROLLOUT_GPU_MEMORY_UTILIZATION=0.80`, `ROLLOUT_MAX_NUM_SEQS=4096`, candidate pool `50`, eps `0.30`, LR `3e-6`, `rollout_n=16`, max response `16384`, repetition checking disabled, W&B online, `400` steps, and save every `20`. Verified the dry-run manifest, `sbatch --test-only`, and the copied checkpoint-side `run_spec.json`; job `5854067` is pending on `quad`.
- Submitted a sibling eps-greedy LR `1e-6` run at `2026-06-22 11:58 EDT` as Slurm job `5854210`, leaving job `5854067` queued. The new run `qwen35_4b_branch_gs50_epsilon_greedy_gs50_struct_all_lr1e6_eps30_cand50_btok320k_steer30_repcheck_off_retry5794408_5854210` matches the `5854067` shape except `ACTOR_LR=1e-6`: candidate pool `50`, eps `0.30`, `ROLLOUT_MAX_NUM_BATCHED_TOKENS=320000`, `ROLLOUT_GPU_MEMORY_UTILIZATION=0.80`, `ROLLOUT_MAX_NUM_SEQS=4096`, `rollout_n=16`, max response `16384`, repetition checking disabled, W&B online, `400` steps, and save every `20`. Verified the dry-run manifest, `sbatch --test-only`, copied checkpoint-side `run_spec.json`, and `squeue`; no cleanup was performed.
- Focused the login02 `51130` branching viewer on the newest retained short/failure-style run at `2026-06-22 11:43 EDT`: the latest FAILED jobs `5776849`/`5794408` no longer have retained artifact DB roots, so the viewer now targets the retained TIMEOUT artifact `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/RLTraining/qwen35_branching_dapo/artifacts/qwen35_4b_branch_gs50_epsilon_greedy_gs50_struct_all_lr3e6_eps30_steer30_retry_5611096_20260618T222413Z` from job `5706085`. Verified login02 `GET /` returns `200`, reports `sqlite_runs=35`, includes the target root, latest retained step `/runs/batch_0349_step_000350-46e1a980/` loads, a doc page contains the tree SVG, and login01 has no `51130` viewer listener.
- Moved the focused newest-retained-failure viewer from login02 to login01 at `2026-06-22 11:46 EDT`: stopped the login02 `51130` process and restarted `Analysis/scripts/serve_branching_viz.py` on `ascend-login01.hpc.osc.edu:51130` against the same retained TIMEOUT artifact from job `5706085`. Verified login01 `GET /` returns `200`, reports `sqlite_runs=35`, includes the target root, and login02 has no `51130` viewer listener.
- Exported and queued vLLM serving for `qwen35_4b_branch_gs50_structured_baseline_gs50_struct_all_lr2e6_execT1_steer30_retry_5611097` checkpoints `global_step_240` and `global_step_300` at `2026-06-22 EDT`: merged both FSDP actor checkpoints into HF-format served model dirs under `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/RLTraining/qwen35_branching_dapo/served_models/`, added text-only torchvision import stubs because the local torchvision wheel has a CUDA-version mismatch, and submitted 5-hour `preemptible-nextgen` single-A100 vLLM jobs. Initial jobs exposed two runtime issues, so the wrappers now pass `--language-model-only`, `--skip-mm-profiling`, and `VLLM_USE_FLASHINFER_SAMPLER=0`. Added executable side-by-side streaming client `RLTraining/scripts/stream_q35_4b_5611097_steps_side_by_side.py`, defaulting to temperature `0.7`, top-p `0.95`, and max tokens `16384`.
- Added raw completion-endpoint helper `RLTraining/scripts/stream_q35_4b_5611097_completion.py` at `2026-06-22 EDT`: it streams `/v1/completions` rather than chat completions, defaults to the step-240 vLLM job/model, prompt `Hello`, temperature `1.0`, top-p `0.95`, and max tokens `256`. Validation passed via Python compile and a short live `Hello` completion smoke call.
- Updated `RLTraining/scripts/stream_q35_4b_5611097_completion.py` at `2026-06-22 EDT` to apply the Qwen chat template to prompt text before calling `/v1/completions`, while keeping `--raw-prompt` for exact manual prefixes and `--print-rendered-prompt` for inspection. Validation passed via Python compile, rendered-prompt check, and a short live templated `Hello` completion.
- Set the default max generation length for `RLTraining/scripts/stream_q35_4b_5611097_completion.py` to `16384` tokens at `2026-06-22 EDT`, matching the side-by-side streaming helper while still allowing `--max-tokens` overrides.
- Added `--partial-completion` to `RLTraining/scripts/stream_q35_4b_5611097_completion.py` at `2026-06-22 EDT`, as an explicit alias for appending partial assistant/model text after the rendered user prompt and before streaming from `/v1/completions`. Validation passed via Python compile, help output, and rendered-prompt inspection.
- Added the RL training system prompt to `RLTraining/scripts/stream_q35_4b_5611097_completion.py` at `2026-06-22 EDT`, rendering it before the user prompt for completion-endpoint experiments. Verified the constant matches `BuildRLDataset/output/train.parquet`, and that partial completions still append after the assistant `<think>` prefix.
- Made the default system prompt in `RLTraining/scripts/stream_q35_4b_5611097_completion.py` less math-specific at `2026-06-22 EDT`, changing it from "Solve the math problem" / "calculations" to generic task/concrete-work wording while preserving the structured `<think>/<steer>/<exec>` format and boxed final-answer convention. Validation passed via Python compile and rendered-prompt inspection.
- Removed boxed-answer guidance from `RLTraining/scripts/stream_q35_4b_5611097_completion.py` at `2026-06-22 EDT` and changed the completion helper default temperature to `0.7`. The rendered prompt now asks for a clear concise final answer after `</think>` without mentioning boxed formatting. Validation passed via Python compile, help output, and rendered-prompt inspection.
- Updated the default steer guidance examples in `RLTraining/scripts/stream_q35_4b_5611097_completion.py` at `2026-06-22 EDT`: `<steer>` is now described as guiding thinking, making executive decisions, choosing subproblems, slowing down, enumerating, verifying, and backtracking, with compact inline examples for each. Validation passed via Python compile and rendered-prompt inspection.
- Updated the RL training system prompt in `BuildRLDataset/build_rl_dataset.py` at `2026-06-22 EDT`: kept the math-solving focus and boxed final-answer instruction, but replaced the old `<steer>` purpose text with the example-based list for guiding thinking, executive decisions, subproblems, slowing down, enumeration, verification, and backtracking. Regenerated `BuildRLDataset/output/train.parquet` from the existing stratified rows; row counts remained `9149` train rows and `851` skipped answer-format rows. Verified source, manifest, and parquet system prompts match; `BuildRLDataset` py_compile and focused pytest passed (`6 passed`).
- Added the same RL system prompt to `RLTraining/scripts/stream_q35_4b_5611097_steps_side_by_side.py` at `2026-06-22 EDT`, so side-by-side chat-completion probes send a `system` message before the user prompt for both checkpoints. Verified the request JSON system prompt exactly matches the regenerated `BuildRLDataset/output/train.parquet` prompt and Python compile passed.
- Added `epsilon_greedy_off_policy` rollout mode at `2026-06-22 EDT`: the mode verbalizes one enumerated candidate list at an epsilon-selected steer boundary, then branches by synthetic `Continue with #k` steer insertions at the second boundary. Defaults are `OFF_POLICY_NUM_CANDIDATES=5` and `BRANCH_FANOUT<=OFF_POLICY_NUM_CANDIDATES`; synthetic steer spans are logged in visible text but excluded from actor loss, while generated enumeration/continuation tokens remain sampled. Added typed SQLite `verbalized_sampling_*` rows, dynamic viewer hydration for selected/other options plus raw enumeration text, branch-token offset alignment for off-policy child edges, and `RLTraining/scripts/debug_verbalized_off_policy_sampling.py` for `/v1/completions` debugging against a running vLLM endpoint. Validation passed: focused Analysis viewer/branch-executor pytest, RL run-spec/update-mask pytest, targeted pyright, launch dry-run/default inspection, invalid fanout rejection, SQLite round-trip smoke, shell syntax checks, compile checks, and diff whitespace checks; broad `test_branching_dapo.py` remains blocked locally by the PyTorch/torchvision CUDA-major mismatch.
- Disabled verbalized off-policy branching at the empty initial `<think>` boundary at `2026-06-22 EDT`, so `epsilon_greedy_off_policy` only applies after the model has produced at least one normal steer/exec segment. Validation passed: branch-executor compile, targeted pyright, and diff whitespace check. Ran a manual ckpt-300 BranchExecutor batch against the live vLLM endpoint `a0111:18001` on a hard ordered-triples reciprocal-sum problem with `branch_prob=0.05`, `rollout_count=16`, `OFF_POLICY_NUM_CANDIDATES=5`, and `BRANCH_FANOUT=2`; run directory `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/Analysis/branching_eval/manual_off_policy_probe/ckpt300_hard_math_batch16_20260622T160203` contains `tree_events.sqlite` with `1976` events, `24` leaves, `8` verbalized branch points, and no first-boundary off-policy branch.
- Made `epsilon_greedy_off_policy` fanout-one behavior inline at `2026-06-22 16:18 EDT`: when `BRANCH_FANOUT=1`, the selected synthetic `<steer>Continue with #k</steer><exec>` is appended to the same node/path and the decode stream is reset like epsilon-greedy inline, instead of creating a one-child branch edge. Validation passed: Black, branch-executor compile, targeted pyright, and a live ckpt-300 BranchExecutor probe at `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/Analysis/branching_eval/manual_off_policy_probe/ckpt300_hard_math_inline16_nobranch_20260622T201712`, which produced `16` leaves, `10` verbalized decisions, all with `branch_fanout=1`, and `0` `edge_selected` rows.
- Added a verbalized off-policy cooldown at `2026-06-22 16:24 EDT`: `epsilon_greedy_off_policy` now requires at least `5` counted non-initial steer boundaries since the previous off-policy trigger before another trigger is eligible, preventing rapid repeated enumerate/continue insertions. The counter is carried in `PathState`, reset only after an actual verbalized off-policy trigger, and ignored for non-off-policy modes. Validation passed: Black, branch-executor/runtime-types compile, targeted pyright, and a direct predicate check showing counts `0..4` blocked, count `5` allowed, and the initial `<think>` boundary still blocked.
- Relaxed the first `epsilon_greedy_off_policy` trigger warm-up at `2026-06-22 16:36 EDT`: before the first actual off-policy trigger, only the first counted real steer decision boundary is blocked, so the second counted steer boundary can trigger subject to epsilon probability. After any actual off-policy trigger, the existing `5`-boundary cooldown still applies. Validation passed: Black, branch-executor/runtime-types compile, targeted pyright, and a direct predicate check showing initial blocked, pre-first-trigger counts `[1=False, 2=True, 3=True]`, and post-trigger counts `[1..4=False, 5=True]`.
- Added `Analysis/scripts/generate_sft_warmup_off_policy.py` and `Analysis/scripts/submit_sft_warmup_off_policy_ckpt300.sbatch` at `2026-06-22 EDT` to regenerate the SFT warm-up prompts with the branching engine, the general completion-script system prompt, ckpt 300, `epsilon_greedy_off_policy` at probability `0.02`, `branch_fanout=1`, 5 verbalized candidates, one rollout per source prompt, and final structure-cleaned JSONL/parquet outputs. Validation passed: Black, targeted pyright, Python compile, and a live one-prompt ckpt-300 smoke producing SQLite events plus raw/cleaning artifacts. Submitted full 9,149-prompt generation job `5856627` on `preemptible-nextgen` with run dir `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/Analysis/branching_eval/sft_warmup_off_policy/sft_warmup_ckpt300_offpolicy_p02_nobranch_5856627_20260622T205253Z`; startup verified vLLM requests and SQLite event logging.
- Replaced oversized SFT warm-up generation job `5856627` at `2026-06-22 EDT` after narrowing the source to the final filtered SFT artifact. Cancelled `5856627` while preserving its partial artifacts/logs, updated `generate_sft_warmup_off_policy.py` to read final SFT JSONL multi-turn `messages` prompts by dropping the original target assistant completion and prepending the general system prompt, and updated the Slurm wrapper to use `--limit 2000` from `BuildSFTDataset/output_transform_async_16384/transformed_subset_analysis/merged_with_output_transformed_output_aug390_t1p0_pruned_top10_max_exec_truncated_seed42_plus_non_sequitur500_1to2_v2.jsonl`. Validation passed: Black, targeted pyright, shell syntax, and a live one-row final-SFT smoke. Submitted corrected job `5856737` on `preemptible-nextgen`; run dir `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/Analysis/branching_eval/sft_warmup_off_policy/sft_warmup_final_sft2000_ckpt300_offpolicy_p02_nobranch_5856737_20260622T211241Z` is running on `a0218` with vLLM active and SQLite events/logged completions starting.
- Completed the ckpt-300 SFT warm-up off-policy dataset fill at `2026-06-22 EDT`: the first 2K raw run `5856737` yielded `1721` valid cleaned rows, so added source-index filtering plus forced initial `<think>\n<steer>` top-up support and a merge helper. Submitted short top-up jobs `5858183` over rejected source rows and `5858201` over unused final-SFT rows, cancelled both after enough valid replacements were written, and merged to `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/Analysis/branching_eval/sft_warmup_off_policy/sft_warmup_final_sft2000_ckpt300_offpolicy_p02_nobranch_complete_2000_20260623T0300Z` with exactly `2000` cleaned JSONL/parquet rows and no duplicate `source_index`. Validation passed: Black, targeted pyright, shell syntax, and final row/schema integrity checks.
- Fixed `epsilon_greedy_off_policy` activation at `2026-06-23 EDT`: the branching executor now keys verbalized off-policy behavior from explicit `BranchingConfig.verbalized_off_policy_enabled` instead of the selector label, RL rollout construction passes that flag, and Analysis run-matrix helpers treat off-policy as epsilon-style scheduling while keeping true branching enabled for fanout-one inline mode. Re-launched the full filtered 2K SFT warm-up regeneration as Slurm job `5864833` on `preemptible-nextgen` with ckpt 300, `epsilon_greedy_prob=0.02`, `branch_fanout=1`, `OFF_POLICY_NUM_CANDIDATES=5`, forced initial steer-open, and run dir `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/Analysis/branching_eval/sft_warmup_off_policy/sft_warmup_final_sft2000_ckpt300_verbalized_offpolicy_p02_nobranch_fixed_5864833_20260623T035744Z`. Startup validation passed: targeted pyright, Python compile, shell syntax, `sbatch --test-only`, live SQLite writes, `verbalized_sampling_decision` rows, and an early raw validity spot-check of `77/77`.
- Staged completion for the fixed SFT warm-up regeneration at `2026-06-23 EDT`: after the fixed base run showed one early format reject, submitted top-up job `5866030` over unused source indices `2000..2499` with the same ckpt-300 off-policy settings and forced initial steer-open. Also submitted dependent CPU merge job `5866139` (`afterok:5864833:5866030`) to write the final 2K cleaned JSONL/parquet under `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/Analysis/branching_eval/sft_warmup_off_policy/sft_warmup_final_sft2000_ckpt300_verbalized_offpolicy_p02_nobranch_fixed_complete_2000_<timestamp>`.
- Completed the fixed ckpt-300 verbalized off-policy SFT warm-up dataset at `2026-06-23 EDT`: base job `5864833` finished with `2000` raw rows, `1935` cleaned rows, and `631` verbalized off-policy decisions; the first top-up `5866030` failed because the requested unused source-index range exceeded the final filtered JSONL length, so its scoped failed artifact dir was removed. Corrected top-up job `5927541` over source indices `2000..2192` completed with `193` raw rows and `181` cleaned rows, then merge job `5927543` wrote the final dataset to `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/Analysis/branching_eval/sft_warmup_off_policy/sft_warmup_final_sft2000_ckpt300_verbalized_offpolicy_p02_nobranch_fixed_complete_2000_20260623T131907Z` with exactly `2000` cleaned JSONL/parquet rows, `2000` unique source indices, and no duplicates.
- Added `Analysis/sft_warmup_verbalized_offpolicy_examples.md` at `2026-06-23 EDT` with five chat-viewable examples from the corrected fixed 2K dataset, including base and top-up rows plus their `verbalized_sampling_decision` excerpts.
- Stripped generation-only system prompts from the fixed ckpt-300 verbalized off-policy SFT warm-up final dataset at `2026-06-23 EDT`: rewrote `cleaned_generations.jsonl` and `cleaned_generations.parquet` in `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/Analysis/branching_eval/sft_warmup_off_policy/sft_warmup_final_sft2000_ckpt300_verbalized_offpolicy_p02_nobranch_fixed_complete_2000_20260623T131907Z` so `prompt` and `messages` contain `0` system rows. Updated the generator and merge helper to strip system rows from cleaned outputs going forward. Added `Analysis/sft_warmup_verbalized_offpolicy_stats.md` and `.json` with matched source/domain, length, and `wait`/`alternatively` comparisons against the original filtered SFT source.
- Added `Analysis/sft_warmup_verbalized_offpolicy_block_stats.md` and `.json` at `2026-06-23 EDT` with block-length stats for the fixed 2K ckpt-300 verbalized off-policy SFT warm-up dataset. Excluding control tags, average steer blocks are `5.25` tokens and average exec blocks are `104.18` tokens; normal steers average `5.11`, synthetic enumerate steers `14.00`, and synthetic continue steers `4.00` tokens.
- Replaced problematic fixed SFT warm-up rows at `2026-06-23 EDT`: the generator cleaner and merge helper now reject completions whose post-`</think>` final answer contains `<think>`, `<steer>`, or `<exec>` control tags. Re-merged the existing base/top-up artifacts into the same final 2K JSONL/parquet path, replacing the `89` previously counted mismatch rows without a new GPU job. Validation now shows `2000` unique rows, `0` system prompt rows, `0` final-answer control-tag rows, and `0` block-count mismatches; refreshed the summary and block-stat reports.
- Replaced the pending uniform verbalized off-policy SFT warm-up regeneration job at `2026-06-23 EDT` to use a 2-GPU quad vLLM shape: added `VLLM_TENSOR_PARALLEL_SIZE` to `Analysis/scripts/submit_sft_warmup_off_policy_ckpt300.sbatch`, cancelled pending preemptible-nextgen job `5968306` before start, and submitted job `5968626` on `quad` with `--gres=gpu:a100:2`, `--cpus-per-task=16`, `--mem=192G`, `VLLM_TENSOR_PARALLEL_SIZE=2`, `GEN_LIMIT=2193`, forced initial steer-open, and the same uniform `3..10` off-policy regeneration/finalization path. `sbatch --test-only` accepted the shape and forecast `a0001` at `2026-06-23T15:07:02`; the submitted job started on `a0001` at `2026-06-23T15:07:42`, and vLLM startup confirms `tensor_parallel_size=2`, `world_size=2`, and TP ranks `0/1`. Patched the wrapper again to prefer `TENSOR_PARALLEL_SIZE` and unset the submitted `VLLM_TENSOR_PARALLEL_SIZE` env alias for future runs, avoiding vLLM's unknown-environment-variable warning.
- Restarted the uniform verbalized off-policy SFT warm-up regeneration at `2026-06-23 16:30 EDT` with the selected-option exec prefill changed to the colon form `To carry out option k:`. Cancelled no-space job `5976375` after `00:11:25`, validated the exact option-2 suffix, and submitted replacement job `5977600` on `quad` with `2` A100s, `TENSOR_PARALLEL_SIZE=2`, `GEN_LIMIT=2193`, forced initial steer-open, run label `sft_warmup_final_sft2000_ckpt300_verbalized_offpolicy_p02_nobranch_uniform3to10_proceed_colon_fullsource`, ckpt 300, probability `0.02`, one rollout per prompt, no branching, and uniform candidate counts `3..10`. `sbatch --test-only` forecast `a0001`; the job is running on `a0001`.
- Restarted the uniform verbalized off-policy SFT warm-up regeneration at `2026-06-23 17:51 EDT` with corrected synthetic spacing and wording. The first synthetic steer now uses `Enumerate n distinct options for the immediate next decision/step` and avoids adding a second newline when the prefix already ends with one; selected continuations now use `Proceed with option k` plus `<exec>Let's do option k:`. Added a finalizer check for `</exec>\n\n<steer>` spacing regressions. Cancelled stale job `5977600`, removed its scoped partial artifact directory while preserving Slurm logs, validated formatting/compile/pyright/direct suffix assertions, and submitted replacement job `5986319` on `quad` with `2` A100s, `TENSOR_PARALLEL_SIZE=2`, `GEN_LIMIT=2193`, forced initial steer-open, run label `sft_warmup_final_sft2000_ckpt300_verbalized_offpolicy_p02_nobranch_uniform3to10_proceed_letsdo_fullsource`, ckpt 300, probability `0.02`, one rollout per prompt, no branching, and uniform candidate counts `3..10`.
- Replaced pending uniform verbalized off-policy SFT warm-up job `5986319` at `2026-06-23 18:23 EDT` with a single-GPU quad shape per request. `sbatch --test-only` accepted `quad`, `1` A100, `8` CPUs, `96G`, `TENSOR_PARALLEL_SIZE=1`, `GEN_LIMIT=2193`, forced initial steer-open, and run label `sft_warmup_final_sft2000_ckpt300_verbalized_offpolicy_p02_nobranch_uniform3to10_proceed_letsdo_1gpu_fullsource`, forecasting `a0024` at `2026-06-24T05:42:52`. Cancelled the not-started two-GPU job `5986319` and submitted replacement job `5989788`; Slurm recorded the intended `ReqTRES=cpu=8,mem=96G,gres/gpu:a100=1`.
- Replaced pending uniform verbalized off-policy SFT warm-up job `5989788` at `2026-06-23 18:37 EDT` with a 2-GPU nextgen TP2 shape plus the non-NVLink vLLM env path. Added `NON_NVLINK_VLLM=1` support to the Slurm wrapper, which exports `NCCL_P2P_DISABLE=1`, `VLLM_DISABLE_PYNCCL=1`, and `VLLM_SKIP_P2P_CHECK=0` and prints them into the Slurm log. `sbatch --test-only` accepted `nextgen`, `2` A100s, `16` CPUs, `192G`, `TENSOR_PARALLEL_SIZE=2`, `GEN_LIMIT=2193`, forced initial steer-open, and run label `sft_warmup_final_sft2000_ckpt300_verbalized_offpolicy_p02_nobranch_uniform3to10_proceed_letsdo_nextgen2_fullsource`, forecasting `a0290` at `2026-06-24T11:01:01`. Cancelled the not-started 1-GPU quad job `5989788` and submitted replacement job `5991449`; Slurm recorded `ReqTRES=cpu=16,mem=192G,gres/gpu:a100=2` and later forecast `a0131` at `2026-06-23T22:15:55`.
- Restarted the branching viewer on login01 at `2026-06-24 09:33 EDT` for all currently running RL jobs with non-empty SQLite artifacts. `Analysis/scripts/serve_branching_viz.py` now serves port `51130` on `ascend-login01.hpc.osc.edu` for job `5854067` (4B epsilon greedy LR-3e-6 repcheck-off, 11 DBs through `batch_0106_step_000107`), job `5854210` (4B epsilon greedy LR-1e-6 repcheck-off, 5 DBs through `batch_0043_step_000044`), and job `6016366` (4B verbalized off-policy epsilon branching bp4 repcheck-off, 4 DBs through `batch_0035_step_000036`). Verified `GET /` on login01 returned `200`, includes all three job IDs, and the listener is bound at `127.0.0.1:51130`; log path `/tmp/branching_viz_51130_login01_20260624T093334.log`.
- Replaced Qwen3.5 4B verbalized off-policy RL job `6015759` at `2026-06-23 21:57 EDT` after confirming its copied manifest had `max_branch_points=1`. Cancelled the pending wrong-shape job and submitted corrected job `6016366` with the same `epsilon_greedy_off_policy` shape except `OFF_POLICY_MAX_BRANCH_POINTS_PER_ROLLOUT=4`: run label `gs50_offpolicy_branch_all_lr3e6_eps10_fanout2_bp4_steer30_repcheck_off`, fanout `2`, epsilon trigger probability `0.10`, `BRANCH_PROB=0.0` because this mode is epsilon-triggered, candidates uniformly `3..10`, `REPETITION_CHECKING_ENABLED=False`, LR `3e-6`, prompt batch `8`, rollout count `1`, max response `16384`, `ROLLOUT_MAX_NUM_BATCHED_TOKENS=320000`, W&B online, and stable `quad` 4xA100/48 CPU/256G/72h resources. Verified the checkpoint-side `run_spec.json` for `6016366`; Slurm shows it pending with start time `2026-06-24T09:30:00`.
- Submitted Qwen3.5 4B verbalized off-policy epsilon-greedy branching RL job `6015759` at `2026-06-23 21:52 EDT`: mode `epsilon_greedy_off_policy`, run label `gs50_offpolicy_branch_all_lr3e6_eps10_fanout2_steer30_repcheck_off`, model `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/RLTraining/qwen35_branching_dapo/merged_hf/qwen35_4b_branching_5496401_global_step_50_hf`, project `branching_dapo_qwen35_gs50`, prompt batch `8`, rollout count `1`, max prompt `1024`, max response `16384`, max steer `30`, LR `3e-6`, `400` steps, save every `20`, selector `embed_diverse_topk_random`, epsilon probability `0.10`, fanout `2`, max branch points `1`, verbalized candidates uniformly `3..10`, `ROLLOUT_MAX_NUM_BATCHED_TOKENS=320000`, W&B online, and `REPETITION_CHECKING_ENABLED=False`. `sbatch --test-only` accepted the stable `quad` 4xA100, 48 CPU, 256G, 72h request; submitted job is pending with scheduler start time `2026-06-24T09:30:00`. Verified both submission and checkpoint-side `run_spec.json` manifests match the intended off-policy branching shape.
- Restarted the uniform verbalized off-policy SFT warm-up regeneration at `2026-06-23 16:17 EDT` with no trailing space in the selected-option exec prefill. Cancelled job `5974560` after `00:13:44`, changed the prefill from `To carry out option k I need to ` to `To carry out option k I need to`, validated the exact option-2 suffix, and submitted replacement job `5976375` on `quad` with `2` A100s, `TENSOR_PARALLEL_SIZE=2`, `GEN_LIMIT=2193`, forced initial steer-open, run label `sft_warmup_final_sft2000_ckpt300_verbalized_offpolicy_p02_nobranch_uniform3to10_proceed_nospace_fullsource`, ckpt 300, probability `0.02`, one rollout per prompt, no branching, and uniform candidate counts `3..10`. `sbatch --test-only` forecast `a0001`; the job is running on `a0001`.
- Restarted the uniform verbalized off-policy SFT warm-up regeneration at `2026-06-23 16:01 EDT` with the new selected-option wording. Submitted Slurm job `5974560` on `quad` with `2` A100s, `TENSOR_PARALLEL_SIZE=2`, `GEN_LIMIT=2193`, forced initial steer-open, run label `sft_warmup_final_sft2000_ckpt300_verbalized_offpolicy_p02_nobranch_uniform3to10_proceed_fullsource`, ckpt 300, probability `0.02`, one rollout per prompt, no branching, and uniform candidate counts `3..10`. `sbatch --test-only` accepted the shape and forecast `a0010` at `2026-06-23T16:16:30`; the submitted job is pending on `quad` with reason `Priority`.
- Stopped the active uniform verbalized off-policy SFT warm-up regeneration at `2026-06-23 15:57 EDT`: cancelled Slurm job `5968626` before it could continue producing rows with the old selected-option wording. Updated the synthetic selected-option insertion from `Continue with #k` plus `Option k was ` to `Proceed with option k` plus `To carry out option k I need to ` in both the branching executor and the standalone vLLM debug probe. Validation passed: Black, targeted Analysis/RL pyright, Python compile, source-string scan, and a direct suffix assertion for option `2`.
- Updated verbalized off-policy epsilon sampling at `2026-06-23 EDT`: the branching executor now samples the verbalized candidate count uniformly per trigger from `3..10`, inserts `\n<steer>Enumerate {n} distinct immediate next decisions or moves.</steer>\n<exec>`, and inserts selected continuations as `\n<steer>Continue with #k</steer>\n<exec>Option k was ` so the synthetic prefill is visible but masked as off-policy. The Analysis and RL typed configs now use `off_policy_min_candidates`/`off_policy_max_candidates`, and Qwen launch wrappers pass `OFF_POLICY_MIN_CANDIDATES=3` / `OFF_POLICY_MAX_CANDIDATES=10`. Added a finalizer that merges exactly `2000` rows, validates spacing/system/final-answer invariants, checks the typed SQLite candidate-count histogram, and writes summary plus 50-row spot-check artifacts. Validation passed: Analysis pyright, RL pyright, py_compile, shell syntax, targeted Analysis pytest (`103 passed`), RL run-spec pytest (`3 passed`), merge/finalizer pyright, and a direct suffix/sampling probe. Pending submits `5967880` and `5967987` were cancelled before start so the active replacement can cover all `2193` filtered source rows and finalize itself; submitted active job `5968306` on `preemptible-nextgen` with run label `sft_warmup_final_sft2000_ckpt300_verbalized_offpolicy_p02_nobranch_uniform3to10_fullsource`, forced initial steer-open, ckpt 300, probability `0.02`, one rollout per prompt, and no branching. Existing final datasets were preserved pending validation.
- Added `Analysis/sft_warmup_verbalized_offpolicy_50_spotcheck.md` at `2026-06-23 EDT`: a 50-row evenly spaced spot-check document for the cleaned verbalized off-policy SFT warm-up dataset, with source/domain metadata plus collapsible prompt, full completion, and final-answer sections for each sample.
- Salvaged the ckpt-300 uniform verbalized off-policy SFT warm-up run at `2026-06-23 EDT`: failed job `5995797` had produced `1991` raw rows and `1845` valid rows before a vLLM over-context 400 aborted the generator. Added `max_model_len=17408` to the warm-up generator/wrapper and made context-length request rejections row-level, rebuilt the base cleaned sidecars, then resumed the same run directory with job `6016365` on one preemptible-quad A100. The resume completed all `2193` source rows and wrote `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/Analysis/branching_eval/sft_warmup_off_policy/sft_warmup_final_sft2000_ckpt300_verbalized_offpolicy_p02_nobranch_uniform3to10_proceed_letsdo_asap_pquad1_fullsource_complete_2000_5995797_20260623T231545Z` with exactly `2000` cleaned rows, `2000` unique source indices, `0` system prompt rows, `0` steer/exec spacing violations, and `0` final-answer control-tag rows. Validation passed: Black, py_compile, targeted pyright, shell syntax, Slurm `--test-only`, and final JSON validation summary.
- Added Ascend SFT launch support and started the Qwen3.5 4B warm-up SFT pair at `2026-06-24 EDT`: new run configs `qwen35_4b_instruct_warmup_offpolicy_lr7e6_zero3_3ep_20260624.yaml` and `qwen35_4b_base_warmup_offpolicy_lr7e6_zero3_3ep_20260624.yaml` train on the final cleaned 2K verbalized off-policy warm-up JSONL, with LR `7e-6`, `3` epochs, max sequence `16384`, assistant-only supervision, non-sequitur masking path, packing/padding-free enabled, and W&B project `decomposed_reasoning_sft`. Added `SFTTraining/slurm/train_ascend.sbatch` for PAA0201 `quad` 4xA100 launches with scratch caches, online W&B, `SKIP_EVAL=1` by default, Slurm-spool-safe project resolution, and automatic ZeRO-2/ZeRO-3 Accelerate config selection from the run config. Initial ZeRO-2 submits `6119901`/`6119902` reached W&B and then OOMed at the first packed 16K training step on A100s; their empty output dirs were removed. ZeRO-3 replacements `6119907` (instruct, W&B `df4c582c799842b48e63fb69d3a41e55`, node `a0003`) and `6119908` (base, W&B `615c6241ae444560b381cec7a5340ad4`, node `a0016`) passed config/dataset checks, Slurm `--test-only`, W&B online startup, and continued past the previous OOM point to at least `7/135` optimizer steps.
- Changed RL structured decode post-think handling at `2026-06-24 EDT`: exec-chunk generation now treats excluded `</think>` as a rollout stop marker, appends the synthetic close tag when vLLM omits stop text from the returned choice, and resumes through the post-think continuation path instead of letting exec emit `</think>` plus answer text in one decode chunk. Reduced the shared post-think continuation default cap from `2048` to `512` tokens. Validation passed: Python compile for the touched executor/decode files, focused post-think pytest (`5 passed, 86 deselected`), full branch-executor pytest (`91 passed`), and targeted pyright (`0 errors`).
- Added ordinary tokenizer-added control-token SFT support at `2026-06-24 EDT`: `SFTTraining` configs can now declare non-special no-split added tokens, and the Qwen3.5 4B warm-up SFT base/instruct configs add `<steer>`, `</steer>`, `<exec>`, and `</exec>`. Switched SFT DeepSpeed scheduler configs from `WarmupDecayLR` to `WarmupLR` so LR stays flat after warmup; cancelled the wrong-schedule jobs `6120151`/`6120152`, removed only their partial outputs, and completed corrected jobs `6120185`/`6120186`. Final artifacts are under `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/SFTTraining/outputs/qwen35_4b_{instruct,base}_warmup_offpolicy_added_tokens_lr7e6_zero3_3ep_20260624/final_model`; saved tokenizer checks show all four tags map to one non-special token and trainer LR logs are flat at `7e-6` from step `30` through `120`. Validation passed: focused SFT pytest, pyright for touched SFT files, DeepSpeed JSON validation, and final tokenizer checks.
- Started single-GPU nextgen vLLM servers for the added-token Qwen3.5 4B SFT pair at `2026-06-24 EDT`: submitted preemptible-nextgen jobs `6120310` (instruct, `a0109:18020`, served as `qwen35-4b-instruct-addedtok-sft`) and `6120311` (base, `a0111:18021`, served as `qwen35-4b-base-addedtok-sft`) using new `SFTTraining/slurm/serve_vllm_ascend.sbatch`. Updated `RLTraining/scripts/stream_q35_4b_5611097_steps_side_by_side.py` defaults to these two servers and switched it to `/v1/completions` with a rendered prompt plus assistant `<think>` prefix. Verified both `/v1/models` endpoints and a short side-by-side streaming smoke call.
- Restarted Qwen3.5 4B off-policy epsilon branching RL at `2026-06-24 12:16 EDT` so it picks up the 512-token post-think continuation change. Cancelled the three running RL jobs `5854067` and `5854210` (normal inline `epsilon_greedy`) plus `6016366` (`epsilon_greedy_off_policy` LR `3e-6`), preserving logs/checkpoints/artifacts. Submitted replacements `6120708` (`ACTOR_LR=3e-6`) and `6120710` (`ACTOR_LR=2e-6`) with run labels `gs50_offpolicy_branch_all_lr{3,2}e6_eps10_fanout2_bp4_steer30_repcheck_off_pt512`. Both are running on `quad` (`a0002` and `a0006`) with `4` A100s, `48` CPUs, `256G`, `72h`, Qwen3.5 4B gs50 checkpoint, `rollout_mode=epsilon_greedy_off_policy`, `BRANCH_PROB=0.0`, epsilon trigger probability `0.10`, fanout `2`, max branch points `4`, off-policy candidates `3..10`, `N_RESP_PER_PROMPT=1`, `branch_interpolated_grpo`, max response `16384`, `ROLLOUT_MAX_NUM_BATCHED_TOKENS=320000`, W&B online, and repetition checking off. Validation: dry-run manifests inspected and removed, both `sbatch --test-only` calls accepted, checkpoint-side `run_spec.json` copies verified, runtime config dumps confirmed off-policy mode and LR split, source still has `POST_THINK_CONTINUATION_MAX_TOKENS = 512` and `ROLLOUT_STEER_STOP = ("</exec", THINK_CLOSE_TAG)`, and early logs had no errors.
- Smoke-tested the added-token Qwen3.5 4B SFT vLLM servers through the branching engine at `2026-06-24 15:25 EDT`. The live instruct server `a0223:18020` and base server `a0246:18021` both completed a one-doc structured rollout and a true `mode=branching`/`selector=random` fanout-2 Dolci smoke. The branching smokes wrote normal SQLite artifacts under `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/Analysis/branching_eval/manual_added_token_smoke/*_8ep_branching_20260624T192332Z`, with `trigger_fired`, `selector_applied`, two `edge_selected` rows, three nodes, and two scored leaves per model. Candidate edges normalized to `<steer>...</steer>\n<exec>`, so the no-split added `<steer>/<exec>` tokens did not break trigger detection, branch creation, SQLite logging, or leaf scoring. The existing Dolci runner still emits unclosed aiohttp client-session warnings on exit.
- Submitted two Qwen3.5 4B added-token SFT-seeded RL clones at `2026-06-24 15:37 EDT`, duplicating the running LR `3e-6` off-policy branching job `6120708` exactly except for `MODEL_PATH`, `MODEL_NAME_SLUG`, and run-label suffix. New jobs are `6121669` (`qwen35_4b_instruct_addedtok8ep`, model `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/SFTTraining/outputs/qwen35_4b_instruct_warmup_offpolicy_added_tokens_lr7e6_zero3_8ep_decay_20260624/final_model`) and `6121670` (`qwen35_4b_base_addedtok8ep`, model `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/SFTTraining/outputs/qwen35_4b_base_warmup_offpolicy_added_tokens_lr7e6_zero3_8ep_decay_20260624/final_model`). Both use `epsilon_greedy_off_policy`, LR `3e-6`, prompt batch `8`, `N_RESP_PER_PROMPT=1`, max response `16384`, max steer `30`, epsilon `0.10`, fanout `2`, max branch points `4`, off-policy candidates `3..10`, `branch_interpolated_grpo`, `ROLLOUT_MAX_NUM_BATCHED_TOKENS=320000`, W&B online, and stable `quad` 4xA100/48 CPU/256G/72h resources. Dry-run manifests matched `6120708` on mode/shape/decode/resources/actor-runtime/retention, dry-run submission dirs were removed, `sbatch --test-only` accepted both, and checkpoint-side `run_spec.json` copies were verified; both jobs are pending with forecast starts around `2026-06-24T18:54`.
- Restarted the branching RL visualization server on login02 at `2026-06-24 17:08 EDT` for the current running RL jobs. `Analysis/scripts/serve_branching_viz.py` is listening on `127.0.0.1:51130` on `ascend-login02.hpc.osc.edu`, log `/tmp/branching_viz_51130_login02_20260624T170801.log`, and `GET /` returned `200` (`23359` bytes). Included runs are jobs `6120708` (LR `3e-6` gs50 off-policy), `6120710` (LR `2e-6` gs50 off-policy), `6121669` (instruct added-token 8ep seed), and `6121670` (base added-token 8ep seed), filtered to roots with non-empty batch-level `tree_events.sqlite` files.
- Corrected the login02 branching viewer at `2026-06-24 17:21 EDT` after finding stale `51130` listeners on both login01 and login02 still serving old `5854xxx/6016366` roots. Stopped both stale listeners, then manually started a fresh login02-only `serve_branching_viz.py` on `127.0.0.1:51130` with explicit current roots for jobs `6120708`, `6120710`, `6121669`, and `6121670`. Verified `GET /` on login02 includes all four current job IDs and excludes `5854067`, `5854210`, and `6016366`; login01 has no remaining `51130` listener. Log path: `/tmp/branching_viz_51130_login02_current_20260624T212114Z.log`.
- Fixed branching runtime stop-string request shape at `2026-06-24 17:35 EDT`: steer candidate requests now stop on full `</steer>` instead of partial `</steer`, and rollout exec-boundary requests now stop on full `</exec>` instead of partial `</exec`. Because vLLM requests already set `include_stop_str_in_output=True`, new runs should receive complete close tags directly rather than partial strings that need prefix repair. Verified current DBs showed partial `</steer` leaking into assistant-prefix tails for the added-token RL jobs while partial `</exec` appeared only in raw `vllm_choice` rows and was repaired before prefixes; validation passed with `py_compile` and direct stop-marker checks.
- Replaced the global full-stop-string branching change with an explicit config knob at `2026-06-24 18:31 EDT`: default runtime behavior is back to legacy partial stop prefixes `</steer`/`</exec`, while `use_full_stop_strings` / `USE_FULL_STOP_STRINGS=True` opts added-token runs into full `</steer>`/`</exec>` stops. The knob now flows through `BranchingConfig`, RL rollout settings, Hydra overrides, and typed Qwen35 run specs. Validation passed: Python compile, shell syntax checks, direct stop-marker/default checks, RL settings parser check, `RLTraining/tests/test_run_specs.py` (`3 passed`), targeted RL pyright (`0 errors`), and targeted Analysis pyright from the `Analysis` project (`0 errors`). Cancelled added-token jobs `6121669` and `6121670`, removed only their scoped checkpoint/artifact/submission/dataset-cache roots plus local dry-run/test-only manifests, and submitted replacements `6123047` (instruct added-token 8ep seed) and `6123048` (base added-token 8ep seed). Both copied checkpoint-side `run_spec.json` manifests confirm `USE_FULL_STOP_STRINGS=True`, LR `3e-6`, prompt batch `8`, `N_RESP_PER_PROMPT=1`, max response `16384`, epsilon `0.10`, fanout `2`, max branch points `4`, off-policy candidates `3..10`, and `ROLLOUT_MAX_NUM_BATCHED_TOKENS=320000`; the original gs50 jobs `6120708` and `6120710` were left running.
- Restarted the branching RL visualization server on login02 at `2026-06-24 20:31 EDT` for the current four RL jobs after the generic launcher failed to replace a stale listener. Killed the old `51130` process manually and started `Analysis/scripts/serve_branching_viz.py` on `ascend-login02.hpc.osc.edu` with explicit roots for jobs `6123047`, `6123048`, `6120708`, and `6120710`. Verified `GET /` includes the four current job ids, excludes cancelled `6121669`/`6121670`, and serves from pid `3645315`; log path `/tmp/branching_viz_51130_login02_current_20260625T0031.log`.
- Added the structured steer/exec system prompt to the final cleaned 2K verbalized off-policy SFT warm-up dataset at `2026-06-24 21:45 EDT`, writing JSONL/parquet plus summary under `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/Analysis/branching_eval/sft_warmup_off_policy/sft_warmup_final_sft2000_ckpt300_verbalized_offpolicy_p02_nobranch_uniform3to10_proceed_letsdo_asap_pquad1_fullsource_complete_2000_5995797_20260623T231545Z_system_prompt_20260625`. Added 12-epoch added-token Qwen3.5 4B SFT configs for instruct/base using the new dataset, ZeRO-3 warmup-decay, LR `7e-6`, assistant-only supervision, non-sequitur masking, packing, and ordinary added `<steer>/<exec>` tokens; submitted jobs `6124079` (instruct) and `6124080` (base), both running on `quad` with W&B online and early training steps logged. Added `initial_assistant_prefix` to the RL rollout settings/launcher path, appending only the extra configured prefix tokens after the chat-template `<think>\n` tail so token ids and executor text stay aligned. Cancelled obsolete added-token off-policy jobs `6123047`/`6123048`, removed only their scoped artifact/checkpoint/submission/cache roots, fixed typed launcher label parsing to honor `SMOKE_RUN_LABEL` when `RUN_LABEL` is absent, cancelled short-lived misnamed replacements `6124087`/`6124088`, then fixed the prefix path to read `INITIAL_ASSISTANT_PREFIX` from the environment instead of passing raw angle-bracket text through Hydra after `6124093`/`6124094` failed with a `LexerNoViableAltException`. Cleaned those failed scoped roots and relaunched addedtok8ep structured-baseline replacements `6124098` (instruct) and `6124099` (base) with `N_RESP_PER_PROMPT=16`, `EPSILON_GREEDY_PROB=0.0`, `BRANCH_PROB=0.0`, GRPO, max response `16384`, max steer `30`, `USE_FULL_STOP_STRINGS=True`, repetition checking off, and `INITIAL_ASSISTANT_PREFIX=<think>\\n<steer>`. Validation passed: dataset row/source/system-prompt assertions, SFT config parser, py_compile, shell syntax, RL settings parser, Slurm `--test-only`, copied checkpoint-side run specs, focused pyright, queue checks, and early SFT log inspection.
- Restarted the branching RL visualization server on login02 at `2026-06-24 23:21 EDT` for the active 12-epoch added-token structured-baseline replacements and the existing off-policy runs. Stopped stale pid `1514592`, which was still serving the cancelled 8-epoch roots `6124098`/`6124099`, then started `Analysis/scripts/serve_branching_viz.py` on `127.0.0.1:51130` with explicit roots for jobs `6125528`, `6125529`, `6120708`, and `6120710`. Verified `GET /` includes all four current job ids and `addedtok12ep`, excludes `6124098`/`6124099`, and login01 has no `51130` listener. Log path: `/tmp/branching_viz_51130_login02_current_20260625T032114Z.log`.
- Prepared the current tree for commit at `2026-06-25 EDT`: split SFT warm-up cleaning/output helpers into `Analysis/scripts/sft_warmup_cleaning.py` so the generator stays under the file-size limit, restored `</think>` as a rollout stop marker, fixed successful-steer extraction for compacted SQLite choice rows, and moved the SFT vLLM Slurm log target to scratch. Validation passed: Analysis pyright plus script pyright and full pytest (`306 passed`), SFT pyright and pytest (`30 passed`, NVML warnings only), BuildRLDataset pyright and pytest (`6 passed`), RL pyright plus focused run-spec/reward pytest (`65 passed`), shell syntax checks, and `git diff --check`. The broader `RLTraining/tests/test_branching_dapo.py` collection remains blocked by the local PyTorch/torchvision CUDA major-version mismatch.
- Removed the largest generated SFT warm-up markdown artifacts from the staged tree to keep the commit focused: `Analysis/sft_warmup_verbalized_offpolicy_50_spotcheck.md`, `Analysis/sft_warmup_verbalized_offpolicy_colon_50_review.md`, and `Analysis/sft_warmup_verbalized_offpolicy_current_letsdo_examples.md`.
- Removed the generated YAML run/config files from tracking as requested, including the archived checkpoint run configs under `Analysis/branching_eval/archive/checkpoint_run_configs/` and the SFT warm-up run configs under `SFTTraining/configs/runs/`.
- Removed the largest remaining generated SFT warm-up example file: `Analysis/sft_warmup_verbalized_offpolicy_current_letsdo_5_full.md`.
- Added the initial `vLLM_Experimental` native benchmark scaffold at `2026-06-25 19:45 EDT`: repo-local UV project with typed config/grammar/scoring/KV-accounting helpers, vLLM runtime materializer under scratch, one-GPU debug Slurm wrapper, manifest/metrics writer, and unit tests. Verified the active 12ep RL runs already use the SFT `final_model` paths directly with vLLM language-only loading: job `6125528` uses the instruct 12ep final model and job `6125529` uses `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/SFTTraining/outputs/qwen35_4b_base_warmup_offpolicy_added_tokens_system_prompt_lr7e6_zero3_12ep_decay_20260625/final_model`, with `+actor_rollout_ref.rollout.engine_kwargs.vllm.language-model-only=True`; no separate text export is needed. Materialized patched vLLM `0.22.0` runtime at `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/runtimes/vllm_0.22.0_19ef0b6fd88c823d_e6c18e82c62c8e7c`. Failed smoke submits `6134181`, `6134183`, `6134195`, and `6134207` exposed wrapper path, missing `lm_eval`, and Analysis-vs-RL Torch ABI issues; scoped failed benchmark dirs were removed and Slurm logs preserved. Current fixed grammar-temp smoke job `6134218` is running on `a0021` with one `NVIDIA A100-SXM4-80GB`, model path set to the base 12ep final model, `MAX_MODEL_LEN=17408`, `MAX_NUM_BATCHED_TOKENS=65536`, 15 AIME25 load rows, `MAX_TOKENS=512`, isolated Analysis subprocess prompt loading, and vLLM `language_model_only=True` / `skip_mm_profiling=True`. Validation passed: `uv run pytest -q` (`14 passed`), `uv run pyright src tests` (`0 errors`), shell syntax checks, Slurm `--test-only`, prompt-loading smoke, and runtime manifest inspection. Native branch modes remain fail-closed until the scheduler KV fork/promote patch is implemented.
- Advanced `vLLM_Experimental` branch-mode execution at `2026-06-25 20:29 EDT`: `grammar_temp` full debug sweep job `6134236` completed all `15` AIME25 load rows on `a0021` (`NVIDIA A100-SXM4-80GB`, `81920` MiB) using the base 12ep `final_model`, `MAX_MODEL_LEN=17408`, and `MAX_NUM_BATCHED_TOKENS=65536`. Added iterative vLLM branch execution for `eps_on_policy_diverse` and `eps_off_policy_verbalized` using prefix-aware grammar state, seeded boundary firing, length-normalized logprob plus lexical diversity selection, verbalized option enumeration/selection wording, and benchmark env overrides for smoke sweeps. Fixed two off-policy grammar issues found by debug jobs: invalid control tags are now masked inside steer/exec blocks, and forced hidden off-policy prefixes use the no-whitespace tag adjacency required by the current token grammar. Successful one-GPU smoke evidence: job `6134247` ran on-policy with `candidate_count=2`, `FIRE_RATE=1.0`, fired `2` boundaries and created `4` branches; job `6134291` ran verbalized off-policy with fixed `3` options, fired `1` boundary and created `2` selected branches; job `6134297` ran the planned on-policy `candidate_count=50` shape, fired `1` boundary and created `50` branches. All successful smoke manifests report `implementation_surface=iterative_vllm_prefix_cache`, `native_scheduler_kv_fork=false`, and A100-80GB hardware, so these rows are valid functionality checks but not the final KV-sharing native scheduler implementation. Failed off-policy smoke artifact dirs `vllm-exp-eps-offpolicy-smoke-20260626T001219Z` and `vllm-exp-eps-offpolicy-smoke-20260626T001728Z` were removed; Slurm logs were preserved. Current `squeue` shows RL jobs `6125528` and `6125529` still running structured-baseline 12ep runs, while old off-policy RL jobs `6120708` and `6120710` are cancelled. Validation passed: `uv run pytest -q` (`24 passed`), `uv run pyright src tests` (`0 errors`), `bash -n` for Slurm/scripts, Slurm `--test-only` for the debug smoke shapes, and successful debug jobs `6134247`, `6134291`, and `6134297`.
- Brought up the matched `Analysis/branching_eval` comparator path for `vLLM_Experimental` at `2026-06-25 21:14 EDT`: added scratch-generated comparator specs and a Slurm-spool-safe `Analysis/slurm/branching_eval.sbatch`, changed Analysis run specs to resolve paths from the active worktree, and extended the branch-engine vLLM launcher with `VLLM_BIN`, `VLLM_EXTRA_ARGS`, and served `max_model_len`. The comparator wrapper now runs Analysis in its UV env while spawning the working RL vLLM env at `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/RLTraining/envs/qwen35-rl-cu129/bin/vllm`, with `--language-model-only --skip-mm-profiling`, `VLLM_USE_FLASHINFER_SAMPLER=0`, `MAX_MODEL_LEN=17408`, node-local Torch/Inductor/Triton/CUDA caches, and scratch-only outputs. Failed startup attempts exposed and fixed, in order, the stale canonical Analysis path, old Analysis vLLM `0.15.1` rejecting `qwen3_5`, missing serve-side `max_model_len` causing a `262144` context, and GPFS Torch-Inductor `Stale file handle`; failed output/spec dirs for those attempts were removed while Slurm logs were preserved. Successful one-GPU A100-80GB comparator smokes: `6134356` structured baseline completed on `a0021` in `00:06:23`, `6134367` epsilon-greedy completed on `a0021` in `00:06:19`, and `6134369` verbalized off-policy epsilon-greedy completed on `a0010` in `00:06:16`. Each wrote a normal `tree_events.sqlite`, `run_manifest.json`, and diagnostics under `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/comparator_jobs/outputs/*smoke4*`; serve logs confirm vLLM `0.22.0` and `Using max model len 17408`. These are smoke rows with `doc_ids=0`, `max_gen_toks=128`, `num_candidates=4`, and `branch_fanout=2`, so they validate launch/runtime compatibility rather than the final load sweep. Validation passed: Analysis focused pytest (`4 passed`), Analysis targeted pyright (`0 errors`), `vLLM_Experimental` pytest (`27 passed`), `vLLM_Experimental` pyright (`0 errors`), shell syntax checks, Slurm `--test-only`, and successful debug jobs above. Remaining gap: native vLLM V1 scheduler KV fork/promote/free is still not implemented; current branch-mode experimental rows use iterative vLLM prefix-cache branching and explicitly record `native_scheduler_kv_fork=false`.
- Completed the first matched `vLLM_Experimental` nextgen load benchmark pass at `2026-06-26 08:36 EDT`: fixed branch-mode grammar replay so retokenized over-cap prefixes force the close tag instead of killing the vLLM worker, added tests for strict vs replay limit handling, and moved native vLLM/Triton/Inductor/CUDA/XDG caches to node-local `/tmp` after the first nextgen `grammar_temp` job hit a GPFS `Stale file handle`. Cleaned only the failed scratch output dir for `vllm-exp-grammar-nextgen-20260626`; Slurm logs were preserved. Cancelled the pending debug comparator backlog after jobs `6139051` and `6139052` completed, then reran the full matched grid on `nextgen` A100-PCIE-40GB: comparator jobs `6139149`-`6139193` all completed, native on-policy job `6139195` completed 15/15 rows, native off-policy job `6139196` completed 15/15 rows, and restarted native grammar job `6139204` completed 15/15 rows. The summary at `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/comparison_summaries/nextgen_20260626/` contains `45` hardware-matched comparisons and rejected older A100-80GB rows by GPU name/memory; average generated-token throughput over the matched rows was `grammar_temp` `263.56 tok/s` vs `structured_baseline` `9.98 tok/s`, `eps_on_policy_diverse` `60.50 tok/s` vs `epsilon_greedy` `9.57 tok/s`, and `eps_off_policy_verbalized` `62.87 tok/s` vs `epsilon_greedy_off_policy` `9.19 tok/s`. Validation passed: `vLLM_Experimental` pytest (`32 passed`), `vLLM_Experimental` pyright (`0 errors`), shell syntax checks, Slurm `--test-only` for nextgen comparator/native shapes, completed Slurm jobs above, and comparison summary generation. Remaining gap: branch modes still use the iterative vLLM prefix-cache implementation (`native_scheduler_kv_fork=false`), not the true V1 scheduler KV fork/promote/free patch.
- Sped up the `vLLM_Experimental` branch-mode fallback at `2026-06-26 09:05 EDT`: branch rows now advance each request chunk with batched vLLM calls and per-request `SamplingParams`, instead of serially decoding every prompt inside `request_prompt_batch_size`. Enabled vLLM prefix caching for experimental LLMs, added an unmeasured warmup decode before timed rows, made manifests record `enable_prefix_caching=true`, and set `VLLM_WORKER_MULTIPROC_METHOD=spawn` explicitly in the native Slurm wrapper. Validation passed locally with `vLLM_Experimental` pytest (`33 passed`), pyright (`0 errors`), and shell syntax checks. One-row nextgen A100-PCIE-40GB smoke job `6139235` validated the batched branch runner before prefix caching (`36.09 generated tok/s`, `6` boundary fires, `12` branches); follow-up job `6139245` confirmed `enable_prefix_caching=True` in vLLM args/EngineCore and improved the same shape to `82.38 generated tok/s`, `8` boundary fires, and `16` branches on `a0153`. Logs still show vLLM's experimental Mamba cache `align` warning when prefix caching is enabled, so keep this visible in future benchmark interpretation. Remaining gap: branch modes still report `native_scheduler_kv_fork=false`; native V1 scheduler KV fork/promote/free is still the next implementation target.
- Corrected `vLLM_Experimental` nextgen comparator throughput interpretation at `2026-06-26 EDT`: the original `nextgen_20260626` summary divided comparator SQLite token counts by whole-job Slurm `ElapsedRaw`, which includes one vLLM server startup per tiny comparator job and made the branching engine look like ~9-10 tok/s. Updated `vLLM_Experimental/src/vllm_experimental/compare_benchmarks.py` to also emit request-active comparator timing from SQLite `vllm_request`/`vllm_response` timestamps, added an explicit `--native-run-names` filter to avoid mixing older native rows, and regenerated the 45-row summary for native runs `vllm-exp-grammar-nextgen-20260626b`, `vllm-exp-onpolicy-nextgen-20260626`, and `vllm-exp-offpolicy-nextgen-20260626`. Corrected comparator active averages are `structured_baseline=225.34 tok/s`, `epsilon_greedy=233.50 tok/s`, and `epsilon_greedy_off_policy=216.38 tok/s`; the old Slurm-elapsed columns remain in the CSV for audit only. Validation passed: focused `vLLM_Experimental` pytest (`2 passed`) and pyright (`0 errors`).
- Reran optimized `vLLM_Experimental` branch fallback sweeps on `nextgen` at `2026-06-26 09:25 EDT`: initial full jobs `6139359`/`6139360` were misconfigured because comma-valued env vars were passed inside Slurm `--export`, so only one doc/load row reached the jobs; cleaned only those two scoped scratch output dirs and preserved Slurm logs. Corrected submissions `6139761` (on-policy diverse) and `6139762` (off-policy verbalized) inherited comma env vars from the shell, completed all `15` rows each on A100-PCIE-40GB, and confirmed `enable_prefix_caching=True` with no vLLM multiprocessing warning. The optimized fallback improved native branch-mode averages versus the prior native rows: on-policy `60.50 -> 77.80 generated tok/s`, off-policy `62.87 -> 67.63 generated tok/s`; on-policy fired `32` boundaries / `1600` candidate branches, off-policy fired `26` boundaries / `52` selected branches. Regenerated `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/comparison_summaries/nextgen_batched_20260626/` using native runs `vllm-exp-grammar-nextgen-20260626b`, `vllm-exp-onpolicy-batched-nextgen-20260626`, and `vllm-exp-offpolicy-batched-nextgen-20260626`: hardware-matched active-timing averages are `grammar_temp=263.56 tok/s` vs `structured_baseline=225.34 tok/s`, `eps_on_policy_diverse=77.80 tok/s` vs existing `epsilon_greedy=233.50 tok/s`, and `eps_off_policy_verbalized=67.63 tok/s` vs existing `epsilon_greedy_off_policy=216.38 tok/s`. This confirms the optimized fallback is healthier under load but still slower than the existing branch engine for branch modes; native scheduler KV fork/promote remains the real speed target.
- Added the first native-speed vLLM runtime primitive at `2026-06-26 09:40 EDT`: patch `vLLM_Experimental/patches/0002-add-vllm-experimental-kv-fork-primitive.patch` adds `fork_blocks` through `SingleTypeKVCacheManager`, `KVCacheCoordinator`, and `KVCacheManager`, sharing parent KV block objects with child requests through `BlockPool.touch()` refcount increments and copying Qwen3.5/Mamba `align` side state (`_allocated_block_reqs`, `last_state_block_idx`). Materialized a new patched runtime at `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/runtimes/vllm_0.22.0_19ef0b6fd88c823d_5c22bfcd296d5fec` with patch files `0001` and `0002`, and import-checked the patched fork method signatures under the RL vLLM `0.22.0` env. This is a native KV ownership/refcount primitive only; hidden branch request scheduling, partial-tail device copy, winner promotion, and loser cleanup still need to be wired before setting `native_scheduler_kv_fork=true`.
- Tightened the `vLLM_Experimental` native-speed fallback at `2026-06-26 EDT` after correcting the required added-token prefix to `<think>\n<steer>`: grammar replay now allows the newline token only between `<think>` and `<steer>`, the vLLM plugin passes that newline token id to the logits processor, and branch warmup uses a full valid `<think>\n<steer>...` prefix. On-policy candidate-pool fires are now batched across all active prompts in a chunk with one vLLM `generate` call carrying per-request `SamplingParams(n=candidate_count)`, instead of one candidate-pool call per fired prompt. Failed/cancelled scratch dirs from `6140075` and `6140138` were removed while Slurm logs were preserved. Validation passed: `uv run black src tests`, `uv run pytest -q` (`35 passed`), and `uv run pyright src tests` (`0 errors`; pyright version warning only). Smoke job `6140146` completed on `a0172` (`NVIDIA A100-PCIE-40GB`, `40960` MiB) with the corrected newline prefix, `FIRE_RATE=1.0`, `candidate_count=50`, `8` boundary fires, `400` branches, and `47.30 generated tok/s`. Full nextgen sweep job `6140163` completed all `15` on-policy rows on `a0172` with the base 12ep final model, `MAX_MODEL_LEN=17408`, `MAX_NUM_BATCHED_TOKENS=65536`, `FIRE_RATE=0.10`, `candidate_count=50`, and `native_scheduler_kv_fork=false`: average `79.68 generated tok/s`, `27` total fires, and `1350` candidate branches. Regenerated `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/comparison_summaries/nextgen_newline_batched_20260626/` with `45` hardware-matched A100-40GB rows: `grammar_temp=263.56 tok/s` vs `structured_baseline=225.34 tok/s`, corrected on-policy `eps_on_policy_diverse=79.68 tok/s` vs existing `epsilon_greedy=233.50 tok/s`, and `eps_off_policy_verbalized=67.63 tok/s` vs existing `epsilon_greedy_off_policy=216.38 tok/s`. This improves the fallback but still confirms branch modes need the true V1 scheduler fork/promote/free path for native speed.
- Continued `vLLM_Experimental` native-speed work at `2026-06-26 11:30 EDT`: batched verbalized off-policy branch enumeration/selection across all fired active prompts in the chunk, added deterministic forced-fire branch warmup for future runs, and tightened the tracked vLLM KV fork primitive so it shares only a full-block prefix (`fork_blocks(..., num_computed_tokens)`) instead of unsafe partial-tail blocks; fake KV accounting now covers both recompute-tail and future partial-tail-copy paths. Materialized patched vLLM runtime `0.22.0` at `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/runtimes/vllm_0.22.0_19ef0b6fd88c823d_72175d972073eac0` and import-checked the new fork signatures under the RL env. Jobs `6140360` and `6140361` completed the 131072-token-budget sweeps on A100-PCIE-40GB, but 131k did not improve throughput (`73.92 tok/s` on-policy, `70.04 tok/s` off-policy) versus 65k. Job `6140649` completed the new off-policy batched-code 65k sweep on `a0203` (`NVIDIA A100-PCIE-40GB`, `40960` MiB), all `15` rows, average `84.73 generated tok/s`, `41` fires, `82` selected branches; warmup-fixed replacement job `6140825` completed on `a0167` at `82.96 tok/s`, so the faster `6140649` row set remains the best off-policy fallback. Regenerated `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/comparison_summaries/nextgen_native_speed_20260626/` with `45` hardware-matched rows: `grammar_temp=263.56 tok/s` vs `structured_baseline=225.34 tok/s`, `eps_on_policy_diverse=79.68 tok/s` vs `epsilon_greedy=233.50 tok/s`, and `eps_off_policy_verbalized=84.73 tok/s` vs `epsilon_greedy_off_policy=216.38 tok/s`. Validation passed: `uv run black src tests`, `uv run pytest -q` (`37 passed`), `uv run pyright src tests` (`0 errors`; version warning only), shell syntax checks, function/file line-limit scan, `git diff --check`, Slurm `--test-only` before submissions, completed jobs above, and comparison summary generation. Remaining gap: branch modes still record `native_scheduler_kv_fork=false`; scheduler hidden-branch insertion, winner promotion, loser cleanup, and output suppression are not yet enabled.
- Continued the `vLLM_Experimental` native scheduler path at `2026-06-26 12:09 EDT`: regenerated the tracked native scheduler patch set so `0003-add-vllm-experimental-native-branch-scheduler.patch` no longer asserts `use_v2_model_runner` and skips native branch launch when no full KV block is available. Materialized reproducible vLLM `0.22.0` runtime `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/runtimes/vllm_0.22.0_19ef0b6fd88c823d_6f694be377c5ef9f` (`patch_hash=6f694be377c5ef9f`) and import-checked `fork_blocks`, `promote_blocks`, and scheduler helper availability under the RL env. The prior native smoke job `6141218` failed during warmup on A100-PCIE-40GB because the stale runtime still asserted `native branches require v2 model runner`; cleaned only its scoped scratch benchmark dir and preserved Slurm logs. Validation passed locally: `uv run pytest -q` (`37 passed`), `uv run pyright src tests` (`0 errors`; version warning only), and fresh runtime materialization. Submitted replacement one-doc nextgen native smoke `6141357` (`vllm-native-smoke2`, one A100, `NATIVE_SCHEDULER_KV_FORK=1`, `FIRE_RATE=1.0`, `candidate_count=2`, `branch_max_tokens=64`, run name `vllm-exp-onpolicy-native-smoke2-20260626`); duplicate pending job `6141358` was cancelled immediately to avoid duplicate artifacts. Remaining gap: native scheduler branch metrics are not yet surfaced in benchmark rows, and faithful native verbalized off-policy still needs a scheduler-internal enumeration request before option children rather than a direct-prefix shortcut.
- Corrected the `vLLM_Experimental` verbalized off-policy native path at `2026-06-26 13:15 EDT`: `eps_off_policy_verbalized` no longer routes to the iterative prefix-cache fallback when `NATIVE_SCHEDULER_KV_FORK=1`. The benchmark payload now carries tokenized verbalized scripts for `Enumerate {candidate_count} distinct options for the immediate next decision/step`, `Proceed with option {option_number}`, and `Let's do option {option_number}:`; the grammar logits processor can force an initial script and a follow-up script after the next generated `</exec>`. Regenerated `0003-add-vllm-experimental-native-branch-scheduler.patch` so the native scheduler accepts both branch modes, deterministically samples off-policy option counts/options, and injects those forced-token scripts into hidden child requests. Fixed the runtime materializer to retry stale partial-runtime removal and keep patch progress on stderr so the Slurm wrapper can parse the JSON manifest. Materialized vLLM `0.22.0` runtime `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/runtimes/vllm_0.22.0_19ef0b6fd88c823d_1fcf1631295d18ed` (`patch_hash=1fcf1631295d18ed`) and import-checked `fork_blocks`, `promote_blocks`, `_vllm_exp_branch_plan`, `_vllm_exp_option_numbers`, `_vllm_exp_set_child_forced_scripts`, and the forced-script logits helper. Validation passed: `uv run black src tests`, `uv run pytest -q` (`47 passed`), `uv run pyright src tests` (`0 errors`; version warning only), shell syntax checks, `git diff --check`, fresh runtime materialization, and runtime import smoke. Submitted forced native off-policy Slurm smoke job `6142246` on `nextgen` A100-PCIE-40GB with `FIRE_RATE=1.0`, fixed `3` off-policy options, `BRANCH_FANOUT=2`, and prompt padding to ensure a full KV block; it completed successfully in `00:07:12`. Artifacts under `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/benchmarks/vllm-exp-offpolicy-native-forced-script-smoke3-20260626/eps_off_policy_verbalized/` show `implementation_surface=native_vllm_scheduler_kv_fork`, `native_scheduler_kv_fork=true`, `generated_tokens_s=100.06`, `boundary_fire_count=1`, `branch_count=2`, `prefill_tokens_avoided=1056`, `kv_blocks_allocated=2`, `kv_blocks_freed=2`, and `native_events_pc1_bs1.jsonl` records one `branch_start` (`fork_tokens=528`, `shared_blocks=1`) plus one `branch_promote` (`selected_candidate_id=1`, `children_freed=2`, `suffix_tokens=95`). Failed setup-only smoke jobs `6142227` and `6142234` preserved Slurm logs and produced no retained benchmark output.
- Completed the native-speed `vLLM_Experimental` nextgen scheduler benchmark pass at `2026-06-26 14:25 EDT`: kept the grammar prefix as the required `<think>\n<steer>` token sequence, fixed native promotion so resumed parents cap `num_computed_tokens` at `num_tokens - 1`, and expanded hidden off-policy child budgets to include forced enumeration/selection script tokens. Materialized fixed vLLM runtime `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/runtimes/vllm_0.22.0_19ef0b6fd88c823d_eb50a67601f3a64b`; cleaned the failed partial runtime tree from the malformed patch attempt. Native on-policy full sweep job `6142508` completed 15/15 rows on `a0128` (`NVIDIA A100-PCIE-40GB`, `40960` MiB), averaging `164.92` generated tok/s with `42` fires, `2100` child branches, and `2,244,000` prefill tokens avoided. Native off-policy job `6142511` wrote 13/15 rows before the old promotion bug hit `assert num_new_tokens > 0`; fixed rerun job `6142688` completed the missing `(16,4)` and `(16,8)` rows on `a0203` with the new runtime, producing `333.27` and `608.79` generated tok/s and normal `branch_start`/`branch_promote` event logs. Regenerated `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/comparison_summaries/nextgen_native_scheduler_20260626/` with exactly `45` hardware-matched A100-40GB comparisons: `grammar_temp=263.56` vs `structured_baseline=225.34` tok/s, native `eps_on_policy_diverse=164.92` vs `epsilon_greedy=233.50` tok/s, and native `eps_off_policy_verbalized=261.69` vs `epsilon_greedy_off_policy=216.38` tok/s. Validation passed: `uv run black src tests`, `uv run pytest -q` (`47 passed`), `uv run pyright src tests` (`0 errors`; pyright version warning only), shell syntax checks, `git diff --check`, runtime `py_compile`/import checks, Slurm `--test-only`, completed jobs `6142242`, `6142246`, `6142508`, and `6142688`; failed full off-policy job `6142511` was preserved as partial benchmark evidence, while failed generated runtime artifacts were cleaned and Slurm logs preserved.
- Reduced native on-policy burst pressure at `2026-06-26 15:15 EDT`: confirmed vLLM V1 `SamplingParams.n > 1` already fans out into one scheduler request per sample, so replacing hidden children with `n=50` would not fix thrashing. Added `native_branch_wave_size` to the typed tree-search payload and `MAX_NUM_SEQS` to the benchmark harness/manifests, then changed the native scheduler patch to keep the parent as a suspended KV anchor and launch branch candidates in bounded waves. Materialized vLLM runtime `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/runtimes/vllm_0.22.0_19ef0b6fd88c823d_b95310231dfa7304`. Forced-fire one-doc nextgen smoke job `6142866` completed on `a0123` (`NVIDIA A100-PCIE-40GB`, `40960` MiB) with `NATIVE_BRANCH_WAVE_SIZE=8`, `MAX_NUM_SEQS=64`, `candidate_count=50`, `max_steer_tokens=30`, and `MAX_NUM_BATCHED_TOKENS=65536`: event log shows one `branch_start`, six `branch_wave_start` rows with wave sizes `8,8,8,8,8,8,2`, and one `branch_promote` selecting candidate `31`; metrics show `boundary_fire_count=1`, `branch_count=50`, `prefill_tokens_avoided=26400`, `generated_tokens_s=44.23`, `gpu_memory_high_watermark_mib=32301`, and Slurm `COMPLETED 0:0` in `00:07:39`. Validation passed: `uv run pytest -q` (`47 passed`), `uv run pyright src tests` (`0 errors`), shell syntax checks, `git diff --check`, runtime materialization, Slurm `--test-only`, and completed smoke job `6142866`.
- Added first-pass model-hidden-state diversity sampling to `vLLM_Experimental` at `2026-06-26 16:29 EDT`: introduced `DIVERSITY_VECTOR_SOURCE=model_hidden_state`, `DIVERSITY_MODEL_PATH`, and `DIVERSITY_POOLING={mean,last}` config plumbing, plus `ModelHiddenStateEmbedder`, which loads the configured final HF model lazily and embeds each generated candidate span from the final hidden layer in its `prompt + generated_so_far` context. The iterative Python branch runner now passes those vectors into the existing length-normalized-logprob plus diversity scorer for both on-policy candidate pools and verbalized off-policy option continuations; unit tests use a fake embedder to verify the selector receives the correct context and candidate texts. Native scheduler KV-fork mode now asserts if `model_hidden_state` diversity is requested, because vLLM V1 currently keeps `sample_hidden_states` internal to the GPU model runner and does not export those vectors through `ModelRunnerOutput`; wiring that export is the next native-speed patch. Validation passed: `uv run black src tests`, focused pytest (`19 passed`), full `uv run pytest -q` (`49 passed`), `uv run pyright src tests` (`0 errors`), Slurm script syntax checks, and `git diff --check`.
- Wired native vLLM hidden-state diversity at `2026-06-26 18:39 EDT`: added `0004-add-native-hidden-state-diversity.patch`, which extends V1 `ModelRunnerOutput` with `vllm_experimental_hidden_vectors`, exports normalized sampled-token final hidden vectors from `GPUModelRunner` when hidden branch requests set `return_hidden_state_vector`, accumulates/averages those vectors per hidden child in the scheduler, and uses cosine distance over model hidden vectors for native diversity scoring before falling back to token-set distance. Native `branch_promote` events now record `diversity_vector_source` and `hidden_vector_child_count`, and benchmark metrics surface those fields for runtime evidence. Materialized runtime `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/runtimes/vllm_0.22.0_19ef0b6fd88c823d_8761fbbcb1224fb0` with patch hash `8761fbbcb1224fb0`; import checks confirmed `ModelRunnerOutput.vllm_experimental_hidden_vectors`, `GPUModelRunner._vllm_exp_hidden_vectors`, and scheduler hidden-vector helpers are present. Submitted nextgen one-GPU smoke job `6144060` with `NATIVE_SCHEDULER_KV_FORK=1`, `DIVERSITY_VECTOR_SOURCE=model_hidden_state`, `FIRE_RATE=1.0`, `CANDIDATE_COUNT=2`, and `MAX_TOKENS=128`; it is pending at the time of this note. Validation passed so far: `uv run black src tests`, `uv run pytest -q` (`49 passed`), `uv run pyright src tests` (`0 errors`), corrected shell syntax checks, `git diff --check`, runtime materialization, and patched-runtime import check.
- Wired final-hidden-layer diversity into the native vLLM scheduler path at `2026-06-26 18:15 EDT`: added tracked patch `0004-add-native-hidden-state-diversity.patch`, which extends `ModelRunnerOutput` with opt-in `vllm_experimental_hidden_vectors`, copies normalized `sample_hidden_states` only for hidden branch children carrying `return_hidden_state_vector`, accumulates mean hidden vectors per candidate in the scheduler, and uses cosine-distance diversity for native winner scoring with token-set diversity as fallback. Native `branch_promote` events now include `diversity_vector_source` and `hidden_vector_child_count` to prove whether model-hidden vectors were used. Materialized runtime `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/runtimes/vllm_0.22.0_19ef0b6fd88c823d_8761fbbcb1224fb0` (`patch_hash=8761fbbcb1224fb0`) and import-checked the new `ModelRunnerOutput`, scheduler, and GPU runner hooks under the RL env. Validation passed: `uv run black src tests`, `uv run pytest -q` (`49 passed`), `uv run pyright src tests` (`0 errors`), shell syntax checks, `git diff --check`, runtime `py_compile`, and runtime import smoke. Per user request, cancelled longer pending job `6143893` and submitted replacement nextgen one-GPU smoke `6143896` with `MAX_TOKENS=8192`, `FIRE_RATE=0.33`, `NATIVE_SCHEDULER_KV_FORK=1`, `DIVERSITY_VECTOR_SOURCE=model_hidden_state`, `DOC_IDS=0..7`, prompt concurrency `8`, request batch size `1`, `MAX_NUM_SEQS=384`, and `NATIVE_BRANCH_WAVE_SIZE=8`; `sbatch --test-only` for the shorter shape passed with estimated start on `a0145`, and the real job is pending in `nextgen` with reason `Priority`.
- Proved native final-hidden-layer diversity sampling on `debug-quad` at `2026-06-26 19:25 EDT`: fixed `0004-add-native-hidden-state-diversity.patch` so lexical children do not request hidden vectors and hidden-vector export works with vLLM async scheduling, then materialized runtime `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/runtimes/vllm_0.22.0_19ef0b6fd88c823d_a4a65a5b43111b95`. Cancelled stale pending nextgen proof jobs `6144060`/`6144090`; early debug attempts `6144097`/`6144098` failed on the async assertion and were fixed, while nonproof completed dirs with no branch fire/full-block fork were removed from scratch with Slurm logs preserved. Final proof jobs `6144116` (`DIVERSITY_VECTOR_SOURCE=model_hidden_state`) and `6144117` (`DIVERSITY_VECTOR_SOURCE=lexical`) both completed on `a0002` (`NVIDIA A100-SXM4-80GB`, `81920` MiB), with `PROMPT_PAD_REPEATS=600`, one doc, one prompt, `MAX_TOKENS=256`, `FIRE_RATE=1.0`, `CANDIDATE_COUNT=2`, `BRANCH_MAX_TOKENS=64`, `MAX_NUM_SEQS=64`, and `NATIVE_BRANCH_WAVE_SIZE=2`. Hidden run artifacts show `boundary_fire_count=1`, `branch_count=2`, `prefill_tokens_avoided=1056`, `diversity_vector_source=model_hidden_state`, `hidden_vector_child_count=2`, `pool_hidden_pairwise_diversity=0.5899983826416135`, and `selected_hidden_diversity=0.5899983826416135`; lexical control on the same node shows `diversity_vector_source=token_set`, `hidden_vector_child_count=0`, and matched speed `104.16` tok/s vs hidden `103.55` tok/s (`0.994x`). Validation passed: hidden-state verifier, full `vLLM_Experimental` pytest (`51 passed`), pyright (`0 errors`), shell syntax checks, `git diff --check`, Slurm `--test-only`, runtime import smoke, and completed hardware-matched debug jobs.
- Added decoded 50-candidate diversity audit evidence at `2026-06-26 20:00 EDT`: fixed native `candidate_audit` capture to run before winner KV/output promotion, materialized runtime `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/runtimes/vllm_0.22.0_19ef0b6fd88c823d_59cd5c1382608579`, and ran debug job `6144128` on `a0101` (`NVIDIA A100-PCIE-40GB`, `40960` MiB) with one AIME doc, `FIRE_RATE=1.0`, `CANDIDATE_COUNT=50`, `NATIVE_BRANCH_WAVE_SIZE=8`, and `DIVERSITY_VECTOR_SOURCE=model_hidden_state`. The run completed in `00:05:53` with `boundary_fire_count=1`, `branch_count=50`, `hidden_vector_child_count=50`, `prefill_tokens_avoided=26400`, `generated_tokens_s=27.21`, and `gpu_memory_high_watermark_mib=32301`. Decoded audit artifacts are under `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/benchmarks/vllm-exp-onpolicy-native-hidden50-audit-fixed-20260626/eps_on_policy_diverse/`: `candidate_audit_summary.json` and `candidate_audit_candidates.csv`. The selected candidate `2` was rank `1/50` by the implemented combined score (`length_normalized_logprob + 0.2 * hidden_diversity`) but rank `29/50` by pure hidden-vector diversity; the pure-diversity top candidates were off-distribution strings, so this run verifies top combined-score selection, not top-most-diverse-only selection. Validation passed: focused `vLLM_Experimental` pytest, pyright, shell syntax checks, `git diff --check`, runtime materialization, Slurm `--test-only`, and the completed debug job.
- Investigated the bad decoded native diversity trajectory at `2026-06-26 20:38 EDT`: the tokenizer chat template is correct and ends assistant generation with `<think>\n`, but `PROMPT_PAD_REPEATS` was being appended after chat templating, so debug padding corrupted the assistant prefix as `<think>\n filler ...`. Fixed padding to apply to the raw user prompt before chat templating and added a regression test. Reran job `6144186`; the prompt tail was correct, but hidden branch children still restarted with `<think>/<steer>` because the scheduler child request copied parent output ids only in Python state while the worker/logits processor saw child output as empty. Fixed child construction so hidden children use `prompt_token_ids = parent.prompt_token_ids + parent.output_token_ids`, score/promote/audit child output as new-only, and pass `prefix_output_token_ids` to grammar replay. Materialized runtime `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/runtimes/vllm_0.22.0_19ef0b6fd88c823d_ed6b20effb82cbaf`; validation passed focused pytest/pyright, import checks, shell syntax, and `git diff --check`. Small debug audit job `6144331` completed on `a0169` (`NVIDIA A100-PCIE-40GB`, `40960` MiB) with `candidate_count=8`, `boundary_fire_count=2`, `branch_count=16`, `hidden_vector_child_count=16`, `prefill_tokens_avoided=8448`, and `generated_tokens_s=71.53`. Decoded candidates no longer show filler-language derailment, but tag-order issues remain in some branch candidates (`</exec>` / `<exec>... </steer>`), so native branch grammar replay is improved but not yet fully valid for final 50-way diversity evidence.
- Added no-Slurm native debug checks for `vLLM_Experimental` at `2026-06-26 21:20 EDT`: `vllm_experimental.native_debug` now verifies materialized runtime scheduler/model-runner source invariants, tokenizer control-token atomicity, and native candidate-audit selection/layout-only evidence from JSONL or CSV. Running it against runtime `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/runtimes/vllm_0.22.0_19ef0b6fd88c823d_5077a0a761f60e93` plus the latest debug CSV confirmed all KV/prefix invariants pass, all control tags and newline are atomic tokens, selected candidate ranking matched the max combined score, and the real remaining issue was `7/8` layout-only `<steer>\n</steer>` candidates. Fixed the grammar plugin so newline-only steer/exec spans cannot close by tracking non-layout content tokens, and split branch dataclasses/constants into `branch_models.py` to bring `branch_runtime.py` down to `1000` lines. Validation passed: focused plugin/debug tests, full `vLLM_Experimental` pytest (`57 passed`), pyright (`0 errors`), line-limit scan, and the local native-debug command; no new Slurm job was submitted for this diagnosis.
- Ran the live native on-policy layout-fix smoke at `2026-06-26 21:25 EDT`: debug job `6144378` completed on `debug-nextgen` node `a0101` with one visible GPU (`CUDA_VISIBLE_DEVICES=0`, `NVIDIA A100-PCIE-40GB`, `40960` MiB), base 12ep final model, runtime `vllm_0.22.0_19ef0b6fd88c823d_5077a0a761f60e93`, `candidate_count=8`, `FIRE_RATE=1.0`, `NATIVE_BRANCH_WAVE_SIZE=8`, `DIVERSITY_VECTOR_SOURCE=model_hidden_state`, `PROMPT_PAD_REPEATS=600`, and one AIME doc. Metrics: `boundary_fire_count=1`, `branch_count=8`, `hidden_vector_child_count=8`, `prefill_tokens_avoided=4224`, `generated_tokens_s=82.19`, `wall_time_s=1.95`, selected candidate `5`, and Slurm elapsed `00:05:36`. The native-debug audit on `native_events_pc1_bs1.jsonl` reported `layout_only_steer_count=0`, `invalid_literal_tag_count=0`, and `selected_mismatch_count=0`, so the blank-span fix worked. Decoded candidates are still very short one-content-token steer spans (e.g. `<steer>\n Teixe\n</steer>`), so the next quality fix is a minimum non-layout steer content length rather than KV/prefix changes.
- Fixed and validated the native `vLLM_Experimental` on-policy branch promotion path at `2026-06-26 EDT`: normal grammar decode job `6144865` on debug A100-40GB produced clean `<think>\n<steer>...` structure at `94.85` active generated tok/s, then native on-policy jobs `6144915` (`candidate_count=8`) and `6144992` (`candidate_count=50`, wave size `8`) confirmed the duplicate `<steer>` bug was an async parent placeholder leak. Added generated patch `0007-discard-async-parent-token-on-branch.patch`, which discards pending async placeholders when suspending a parent at `</exec>\n`; materialized runtime `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/runtimes/vllm_0.22.0_19ef0b6fd88c823d_9b3c036efd60d713`. Job `6144992` completed on `debug-nextgen` A100-PCIE-40GB with `boundary_fire_count=1`, `branch_count=50`, `async_tokens_discarded=1`, `prefill_tokens_avoided=26400`, `generated_tokens_s=42.93`, selected candidate `33`, no duplicated tags, and clean native debug audit (`selected_mismatch_count=0`, `layout_only_steer_count=0`, `invalid_literal_tag_count=0`). Also fixed benchmark instrumentation so tree-search manifests inherit actual env-derived model/token caps and metrics emit `async_tokens_discarded`. Validation passed: `uv run pytest -q` (`65 passed`), `uv run pyright src tests` (`0 errors`), shell syntax checks, `git diff --check`, Slurm `--test-only`, and completed debug jobs above.
- Replaced native on-policy winner selection with score-free top-k diverse random sampling at `2026-06-26 23:55 EDT`: the algorithm now deduplicates candidate suffixes, sets `candidate_bound=ceil(log2(n_unique)+1)`, seeds the top-k set with the most common unique candidate, greedily adds maximin-distance candidates, then selects randomly from that set. Removed the old length-normalized-logprob/combined-score path from repo selection code and native scheduler audit, removed `diversity_weight`, and stopped forcing hidden child `logprobs=1` in native vLLM. Generated patch `0008-add-topk-diverse-random-selection.patch` from scratch before/after runtime files and materialized runtime `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/runtimes/vllm_0.22.0_19ef0b6fd88c823d_34422d42a52a2f25`. Debug-nextgen smoke job `6145195` completed on `a0368` (`NVIDIA A100-PCIE-40GB`, `40960` MiB) with one AIME doc, `FIRE_RATE=1.0`, `candidate_count=50`, `NATIVE_BRANCH_WAVE_SIZE=8`, `MAX_MODEL_LEN=4096`, and `MAX_NUM_BATCHED_TOKENS=8192`: metrics show `boundary_fire_count=1`, `branch_count=50`, `unique_candidate_counts=[19]`, `candidate_bounds=[6]`, `top_k_candidate_ids=[[16,19,44,18,36,24]]`, randomly selected candidate `18`, `hidden_vector_child_count=50`, `prefill_tokens_avoided=26400`, `generated_tokens_s=59.18`, and no score/logprob keys in candidate audit. Decoded top-k set: `Perform the division`, `Enumerate 3 distinct options for the immediate next decision/step`, `Divide by b+7`, selected `Analyze divisibility of components`, `Apply the Euclidean algorithm`, and `Simplify the expression`. Validation passed: `uv run pytest -q` (`68 passed`), `uv run pyright src tests` (`0 errors`), shell syntax checks, `git diff --check`, runtime py_compile, `native_debug` runtime/event/token audit (`selected_mismatch_count=0`, `layout_only_steer_count=0`, `invalid_literal_tag_count=0`), Slurm `--test-only`, and completed job `6145195`.
- Removed non-native/non-optimized `vLLM_Experimental` workflows at `2026-06-27 EDT`: deleted the repo-local comparator launcher/summary path, iterative prefix-cache branch runtime, Python-side hidden-embedding helper, legacy scoring helper, and their tests. Branch benchmark modes now default to and assert native scheduler KV-fork, while `grammar_temp` remains the native vLLM logits-processor path. Removed stale Python-side diversity model/pooling knobs and cleaned generated cache/venv files from the experimental tree. Validation passed: `cd vLLM_Experimental && uv run pytest -q` (`49 passed`), `uv run pyright src tests` (`0 errors`), `bash -n` for Slurm/scripts, and `git diff --check`.
- Fixed native `vLLM_Experimental` promoted-parent max-token completion at `2026-06-27 EDT`: added generated patch `0009-finish-promoted-parent-at-token-cap.patch`, so after hidden-branch diversity promotion the scheduler emits the promoted suffix with a finish reason and frees the parent if `max_tokens`/`max_model_len` has been reached, instead of resuming the parent for another scheduler pass. The final materialized runtime is `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/runtimes/vllm_0.22.0_19ef0b6fd88c823d_e73943edeb4e96f9` (`patch_hash=e73943edeb4e96f9`). Added a `native_debug` source invariant for this path. Validation passed: patch dry-run, runtime materialization, runtime source invariant check, runtime scheduler `py_compile`, `cd vLLM_Experimental && uv run pytest -q` (`50 passed`), targeted pyright (`0 errors`), Slurm/script shell syntax checks, and `git diff --check -- vLLM_Experimental`.
- Fixed native `vLLM_Experimental` off-policy branching semantics at `2026-06-27 01:17 EDT`: added patch `0010-inline-off-policy-enumeration.patch`, so `eps_off_policy_verbalized` now forces `Enumerate N distinct options...` inline on the parent at the epsilon boundary, then either forces `Proceed with option k` inline when `BRANCH_FANOUT=1` or forks true hidden child trajectories only after the enumeration exec block closes when fanout is greater than one. The new materialized runtime is `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/runtimes/vllm_0.22.0_19ef0b6fd88c823d_bca667b4fdd46eb7` (`patch_hash=bca667b4fdd46eb7`). Added native source invariants for inline enumeration, inline fanout-one continuation, post-enumeration-only branch plans, and inline fallback if the native fork is skipped. Validation passed: focused pytest (`29 passed`), full `vLLM_Experimental` pytest (`50 passed`), pyright (`0 errors`), runtime scheduler `py_compile`, `native_debug` source invariant check, shell syntax checks, and `git diff --check -- vLLM_Experimental history.md`.
- Implemented native branch-product returns for `vLLM_Experimental` at `2026-06-27 01:45 EDT`: added patch `0011-return-branch-products.patch`, so branch modes keep promote-one behavior only when `BRANCH_FANOUT<=1`; when `BRANCH_FANOUT>1`, off-policy returns all forked option continuations and on-policy returns the requested fanout from the diverse top-k shortlist. Branch products now finish the parent request with a multi-completion `RequestOutput.outputs` payload, while `branch_return` JSONL events preserve candidate audit, returned candidate IDs, top-k IDs, unique counts, candidate bound, hidden-vector counts, and pairwise diversity. The new materialized runtime is `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/runtimes/vllm_0.22.0_19ef0b6fd88c823d_13dd8cf1d802b668` (`patch_hash=13dd8cf1d802b668`). Validation passed: runtime source invariant check, runtime `py_compile`, focused pytest/pyright for native events/debug/hidden-state verification, full `vLLM_Experimental` pytest/pyright, shell syntax checks, and `git diff --check -- vLLM_Experimental history.md`.
- Added arbitrary native branch-product depth support for `vLLM_Experimental` at `2026-06-27 EDT`: introduced `BRANCH_DEPTH`/`branch_depth` with default `4`, recursive native frontier continuation for returned branch products using token prompts plus matching `prefix_output_token_ids`, compact `native_frontier_*.jsonl` branch-path traces, and patch `0012-add-native-branch-depth-cap.patch` so fanout-one native promotion also stops firing after the configured depth. The materialized runtime is `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/runtimes/vllm_0.22.0_19ef0b6fd88c823d_02893c00b145edf3` (`patch_hash=02893c00b145edf3`). Validation passed: runtime source invariant check, runtime scheduler/output `py_compile`, full `vLLM_Experimental` pytest (`53 passed`), full pyright (`0 errors`), shell syntax checks, per-file/function-size checks for touched code, and `git diff --check -- vLLM_Experimental history.md`. No GPU Slurm job was submitted for this side-thread change.
- Fixed the native on-policy high-concurrency stall in `vLLM_Experimental` at `2026-06-27 EDT`: cancelled hung benchmark job `6145356` after it stopped emitting events with many open hidden children, then added generated patch `0013-free-completed-native-branch-children.patch`. Completed hidden branch children now store lightweight candidate products, free their vLLM requests/KV immediately, and the promoted parent appends the selected suffix then replays from the fork boundary instead of retaining winner KV. The new materialized runtime is `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/runtimes/vllm_0.22.0_19ef0b6fd88c823d_3fe305f3aa6f5329` (`patch_hash=3fe305f3aa6f5329`). Validation passed: runtime source invariant check, runtime scheduler `py_compile`, full `vLLM_Experimental` pytest (`53 passed`), pyright (`0 errors`), shell syntax checks, and targeted `git diff --check` for the touched experimental files.
- Added native benchmark progress logging at `2026-06-27 09:29 EDT`: `vLLM_Experimental` now writes per-job stage events (`job_progress.jsonl` for future submissions), per-row/chunk heartbeats (`progress_pc*_bs*.jsonl`), and native frontier batch start/complete traces so incomplete native runs can be diagnosed and partially salvaged from completed outputs/events. Fixed the native on-policy runtime crash from jobs `6146179`/`6146180` (`NameError: payload is not defined` in `_vllm_exp_complete_hidden_child`) by generating `0014-fix-native-child-completion-payload.patch`, materialized runtime `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/runtimes/vllm_0.22.0_19ef0b6fd88c823d_2a8e0484d18ab614`, and added a native-debug invariant for the parent payload load. Quarantined failed artifact dirs under `vLLM_Experimental/benchmarks/failed_runs/6146179-*` and `6146180-*` while preserving Slurm logs. Submitted replacement A100-80GB preemptible-quad jobs `6146209` (`bs16`, `MAX_NUM_SEQS=256`) and `6146210` (`bs32`, `MAX_NUM_SEQS=384`) with the same 16-doc x 8-rollout, 8K-token, on-policy `.33`, candidate-50, fanout-1 shape; live progress files showed 30s/60s heartbeats with increasing native event rows, boundary fires, branch counts, and freed KV counts. Validation passed: runtime source invariant check, runtime `py_compile`, `uv run black src tests`, `uv run pytest -q` (`55 passed`), `uv run pyright src tests` (`0 errors`), shell syntax checks, and `git diff --check -- vLLM_Experimental`.
- Implemented dynamic native branch admission for `vLLM_Experimental` at `2026-06-27 EDT`: added env/config fields for dynamic pool admission, free-block and sequence-slot reserves, branch priority boost, block safety multiplier, and blocked-pool heartbeat cadence. Generated runtime patches `0015-dynamic-native-branch-admission.patch`, `0016-consume-branch-depth-after-successful-start.patch`, and `0017-admit-dynamic-branch-pool-immediately.patch`; the current materialized runtime is `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/runtimes/vllm_0.22.0_19ef0b6fd88c823d_f2edf02688b5f100`. The native scheduler now queues branch pools atomically, admits them only when KV blocks and request slots are available, prioritizes admitted hidden branches as a fast lane, emits `branch_pool_queued`/`branch_pool_blocked`/`branch_pool_admitted` events, and avoids consuming branch depth on skipped no-full-block boundaries. Added progress metrics for queued/admitted/blocked pools, live/queued pool high-water marks, minimum free blocks/seq slots, and optional `NATIVE_WARMUP_ENABLED=0` for debugging startup-independent row progress. Validation passed: runtime scheduler `py_compile`, `native_debug` source invariants including immediate dynamic admission, `cd vLLM_Experimental && uv run black src tests`, `uv run pytest -q` (`56 passed`), `uv run pyright src tests` (`0 errors`), Slurm/script shell syntax checks, and cleanup of generated cache/venv directories. Smoke evidence: job `6146304` completed and exposed no-full-block branch-depth consumption; cancelled debug artifact dirs from jobs `6146314` and `6146385` were quarantined under scratch `benchmarks/failed_runs/` with Slurm logs preserved; final proof job `6146423` completed on `debug-nextgen` node `a0197` (`NVIDIA A100-PCIE-40GB`, `40960` MiB) with one padded AIME prompt, `candidate_count=8`, `FIRE_RATE=1.0`, `MAX_TOKENS=512`, `MAX_MODEL_LEN=4096`, `MAX_NUM_BATCHED_TOKENS=8192`, and `MAX_NUM_SEQS=64`. Final metrics show `boundary_fire_count=1`, `branch_pool_queued_count=1`, `branch_pool_admitted_count=1`, `branch_pool_blocked_count=0`, `branch_count=8`, `kv_blocks_freed=8`, `prefill_tokens_avoided=4224`, `min_branch_free_blocks=1626`, `min_branch_seq_slots=61`, active row speed `59.23` generated tok/s, and the queue-to-admit trace occurred immediately in `native_events_pc1_bs1.jsonl`.
- Submitted the first full dynamic-admission native benchmark at `2026-06-27 EDT`: `sbatch --test-only` accepted the 80GB-class `preemptible-quad` shape, then an initial 128G request (`6146497`) was cancelled before start and replaced with comparator-matched 64G job `6146500`. The pending job uses run name `vllm16x8-8k-native-onp033-dyn-bs128-r1`, `eps_on_policy_diverse`, AIME doc IDs `0..15`, `PROMPT_CONCURRENCY=128`, `REQUEST_PROMPT_BATCH_SIZE=128`, `MAX_TOKENS=8192`, `MAX_MODEL_LEN=12288`, `MAX_NUM_BATCHED_TOKENS=131072`, `MAX_NUM_SEQS=768`, `FIRE_RATE=0.33`, `CANDIDATE_COUNT=50`, `BRANCH_FANOUT=1`, `BRANCH_DEPTH=4`, `NATIVE_BRANCH_WAVE_SIZE=50`, dynamic branch admission enabled, 512 minimum free KV blocks, 5% free-block reserve, 16 request-slot reserve, and 30s progress heartbeats. It is queued behind resources at the time of this note; no result artifacts exist yet.
- Fixed the native high-concurrency full-prefix child admission path at `2026-06-27 EDT`: job `6146500` reached real branch traffic on A100-SXM4-80GB but failed after `426` branch fires and `21,300` hidden children with vLLM's `assert num_new_tokens > 0` while admitting a hidden child whose copied prompt exactly matched its computed prefix. Generated patches `0018-recompute-full-prefix-native-children.patch` and `0019-trigger-eps-after-steer-open.patch`; the current materialized runtime is `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/runtimes/vllm_0.22.0_19ef0b6fd88c823d_f93ae2cad7e90594` (`patch_hash=f93ae2cad7e90594`). Patch `0018` recomputes the last token for full-prefix hidden children, matching vLLM's remote-KV behavior, and patch `0019` moves epsilon branching to the natural post-`<steer>` continuation boundary instead of forcing a duplicate steer-open token. Validation passed: runtime scheduler `py_compile`, `native_debug` source invariants, `cd vLLM_Experimental && uv run pytest -q` (`56 passed`), and `uv run pyright src tests` (`0 errors`). Debug smoke job `6146592` completed on A100-PCIE-40GB with `boundary_fire_count=64`, `branch_count=3200`, `kv_blocks_freed=3200`, `generated_tokens_s=199.31`, and no scheduler crash. Cancelled pending no-warmup rerun `6146658` before start, then submitted warmup-enabled preemptible-quad jobs `6146747` (`eps_on_policy_diverse`, p=.33, 50 candidates, fanout 1), `6146748` (`eps_off_policy_verbalized`, p=.10, uniform 3..10 options, fanout 1), and `6146749` (`grammar_temp`) using 16 AIME docs, 8 rollouts each, `PROMPT_CONCURRENCY=128`, `REQUEST_PROMPT_BATCH_SIZE=128`, `MAX_TOKENS=8192`, `MAX_MODEL_LEN=12288`, `MAX_NUM_BATCHED_TOKENS=131072`, and `MAX_NUM_SEQS=768`; all three passed `sbatch --test-only` on `preemptible-quad` before submission and are pending at the time of this note.
- Reworked queued native benchmark resources at `2026-06-27 EDT` after inspecting all quad node `AllocTRES`: the apparent free quad GPUs were one-GPU holes on `a0009`, `a0013`, `a0020`, and `a0021`, but most had too few schedulable CPUs left for the original 16-CPU request, while `a0005` had three GPU slots but was drained for reboot. `sbatch --test-only` showed host memory was irrelevant (`64G` through `16G` produced the same forecast), but reducing to 8 CPUs let Slurm use a one-GPU hole. Cancelled not-started 16-CPU jobs `6146747`, `6146748`, and `6146749`, then submitted otherwise identical 8-CPU replacements: `6146815` (`eps_on_policy_diverse`, p=.33, 50 candidates, fanout 1), `6146816` (`eps_off_policy_verbalized`, p=.10, uniform 3..10 options, fanout 1), and `6146817` (`grammar_temp`). Job `6146815` started immediately on `a0013` with `NVIDIA A100-SXM4-80GB, 81920 MiB`; `6146816` and `6146817` are pending with planned nodes `a0012` and `a0009`.
- Recovered the full native benchmark queue at `2026-06-27 EDT`: cancelled still-pending jobs `6146816` and `6146817`, then used smaller one-GPU holes to start `6146884` (`eps_off_policy_verbalized`, `7` CPUs, `64G`, `a0020`, A100-SXM4-80GB) and `6146893` (`grammar_temp`, `3` CPUs, `24G`, `a0009`, A100-SXM4-80GB). A nextgen probe job `6146873` confirmed that available nextgen capacity was A100-PCIE-40GB (`40960` MiB), so it was kept out of the 80GB comparison. The original on-policy job `6146815` failed on `a0013` after `192` boundary fires and `9600` hidden branches with vLLM `block_pool.py` asserting `blk.block_hash is None`, indicating stale parent KV blocks after winner suffix replay. Generated patch `0020-trim-parent-blocks-before-replay.patch`, materialized runtime `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/runtimes/vllm_0.22.0_19ef0b6fd88c823d_0f8342a5e75ae72b`, and added a native-debug invariant that parent blocks are trimmed before promoted-suffix replay. Validation passed: runtime `py_compile`, native-debug runtime invariant check, focused `tests/test_native_debug.py` (`7 passed`), and shell syntax checks. Submitted repaired on-policy job `6146943` on `a0013` with the same 16-doc x 8-rollout, 8K-token, p=.33, candidate-50, fanout-1, `PROMPT_CONCURRENCY=128`, `REQUEST_PROMPT_BATCH_SIZE=128`, `MAX_NUM_SEQS=768` shape; Slurm confirms A100-SXM4-80GB (`81920` MiB) and the new patch-hash runtime.
- Fixed the native full-batch grammar replay bottleneck at `2026-06-27 EDT`: completed on-policy full-batch job `6148232` proved the visible load shape works (`PROMPT_CONCURRENCY=128`, `REQUEST_PROMPT_BATCH_SIZE=128`, 16 docs x 8 rollouts, 8K max tokens, A100-SXM4-80GB) with dynamic branch admission capping live hidden pools (`max_live_branch_pools=2`, `boundary_fire_count=179`, `branch_count=8950`), but it only reached `469.12` generated tok/s while GPU utilization stayed low because the logits processor rebuilt grammar state from the full prior output on every token. Replaced that quadratic replay with `IncrementalGrammarReplay` in the repo plugin source, preserving reset behavior when forced off-policy payloads change. Validation passed: focused plugin/native-debug pytest (`22 passed`), focused pyright (`0 errors`), and Slurm wrapper shell syntax. Submitted full-batch rerun job `6149005` on `preemptible-quad` with the same 16-doc x 8-rollout, 8K-token, p=.33, candidate-50, fanout-1 shape, `PROMPT_CONCURRENCY=128`, `REQUEST_PROMPT_BATCH_SIZE=128`, `MAX_NUM_SEQS=256`, dynamic branch admission enabled, and `NATIVE_BRANCH_MAX_LIVE_POOLS=2`; startup confirmed `a0009`, A100-SXM4-80GB (`81920` MiB), and the repo `ThoughtGrammarLogitsProcessor` plugin import path.
- Corrected native dynamic branch admission semantics at `2026-06-27 EDT`: the fast full-batch rerun `6149005` completed in `00:12:09` at about `1417` generated tok/s, but it was invalid for on-policy comparison because pressure-hit epsilon fires could emit `branch_skip_backpressure` and continue the parent without running the candidate pool. Generated patch `0025-queue-pressure-branch-pools-without-skip.patch` from a before/after scheduler source diff, removing all `branch_skip_backpressure` paths and the pending-pool clear helper. A fired branch now suspends the parent, queues the full branch pool, logs `branch_pool_blocked` while waiting under live-pool/KV/sequence pressure, and later emits `branch_pool_admitted`; branch depth is consumed only for accepted queued branch attempts. Materialized runtime `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/runtimes/vllm_0.22.0_19ef0b6fd88c823d_5c12fea7fc6d1862` (`patch_hash=5c12fea7fc6d1862`) and updated native-debug invariants so `branch_skip_backpressure` fails the source report. Validation passed: runtime scheduler `py_compile`, focused native-debug/native-event pytest (`11 passed`), full `vLLM_Experimental` pytest (`60 passed`), pyright (`0 errors`), shell syntax, and `git diff --check`. Debug-quad smoke job `6149123` completed on A100-SXM4-80GB with `FIRE_RATE=1.0`, `PROMPT_CONCURRENCY=8`, `REQUEST_PROMPT_BATCH_SIZE=8`, `NATIVE_BRANCH_MAX_LIVE_POOLS=1`, `branch_pool_queued=16`, `branch_pool_blocked=15`, `branch_pool_admitted=16`, `branch_start=16`, `branch_promote=16`, `branch_count=128`, and `0` `branch_skip_backpressure` events. Submitted corrected full on-policy benchmark job `6149128` with the same 16-doc x 8-rollout, 8K-token, p=.33, candidate-50, fanout-1, full `PROMPT_CONCURRENCY=128` / `REQUEST_PROMPT_BATCH_SIZE=128` shape; it started on `a0020` A100-SXM4-80GB with the no-skip runtime.
- Submitted corrected-runtime native grammar/off-policy reruns at `2026-06-27 EDT` after the no-skip branch-pool patch: materialized runtime `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/runtimes/vllm_0.22.0_19ef0b6fd88c823d_5c12fea7fc6d1862` (`patch_hash=5c12fea7fc6d1862`) from the current patch set, then `sbatch --test-only` accepted both 80GB-class `preemptible-quad` jobs. Submitted job `6150274` (`vllm16x8-8k-grammar-bs128-c3m24-noskip-r1`, `grammar_temp`, node `a0013`) and job `6150275` (`vllm16x8-8k-offp010-bs128-c7-noskip-r1`, `eps_off_policy_verbalized`, node `a0020`) with AIME doc IDs `0..15`, `PROMPT_CONCURRENCY=128`, `REQUEST_PROMPT_BATCH_SIZE=128`, `MAX_TOKENS=8192`, `MAX_MODEL_LEN=12288`, `MAX_NUM_BATCHED_TOKENS=131072`, `MAX_NUM_SEQS=768`, `BRANCH_FANOUT=1`, and one A100-SXM4-80GB each. Off-policy uses `FIRE_RATE=0.10`, uniform `OFF_POLICY_MIN_CANDIDATES=3` to `OFF_POLICY_MAX_CANDIDATES=10`; grammar uses `FIRE_RATE=0.0`. Both jobs started immediately on `preemptible-quad`; logs are under `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/slurm_logs/`.
- Optimized native fanout-one benchmark routing at `2026-06-27 EDT`: generated patch `0026-apply-branch-depth-limit-only-to-fanout-branches.patch` from a before/after scheduler source diff, so native `branch_depth` skip enforcement now applies only when `BRANCH_FANOUT>1`; fanout-one epsilon/off-policy continuations keep sampling at all eligible boundaries. Also split the benchmark harness routing so `BRANCH_FANOUT=1` native modes use the straight shared-`SamplingParams` `llm.generate` path instead of the recursive native frontier wrapper, reserving frontier recursion for branch-product fanout greater than one. Materialized runtime `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/runtimes/vllm_0.22.0_19ef0b6fd88c823d_d549da4b43ba0d82` (`patch_hash=d549da4b43ba0d82`). Validation passed: runtime scheduler `py_compile`, runtime source invariants, focused pytest (`19 passed`), full `vLLM_Experimental` pytest (`61 passed`), pyright (`0 errors`), shell syntax, and `git diff --check`. Submitted speed rerun job `6150554` (`vllm16x8-8k-offp010-bs128-c7-fastpath-r1`) on `preemptible-quad` with one A100-SXM4-80GB on `a0013`, AIME docs `0..15`, 8 rollouts/doc, `MAX_TOKENS=8192`, `PROMPT_CONCURRENCY=128`, `REQUEST_PROMPT_BATCH_SIZE=128`, `BRANCH_FANOUT=1`, `FIRE_RATE=0.10`, and uniform `OFF_POLICY_MIN_CANDIDATES=3` to `OFF_POLICY_MAX_CANDIDATES=10`; startup log confirms the new runtime hash and 80GB hardware.
- Finished the safe fanout-one native speed pass at `2026-06-27 EDT`: cancelled and quarantined `6150554` after Slurm comma export split `DOC_IDS` down to one prompt, then reran the same corrected off-policy shape as job `6150603` on `a0020` A100-SXM4-80GB with all 16 docs x 8 rollouts. Added full-batch unmeasured warmup for native/grammar jobs (`BATCH_SHAPE_WARMUP_ENABLED=1`, default `BATCH_SHAPE_WARMUP_MAX_TOKENS=4`) so first measured rows do not pay the full 128-request JIT/capture cost, and job `6150695` completed the warmed off-policy rerun at `2211.70` generated tok/s overall (`918003` generated tokens, `415.07s`, high watermark `68377` MiB). The earlier same-shape fast-path run `6150603` was `2204.79` generated tok/s, and the grammar comparator row remains `2713.09` generated tok/s on the same 80GB/full-batch shape. Tried a more aggressive scheduler-side bulk replay optimization, but job `6150689` exposed a grammar/KV replay desync (`expected newline after </exec>`), so the generated patch was removed, its failed run was quarantined, and the invalid runtime `vllm_0.22.0_19ef0b6fd88c823d_2ba6627d39035be9` was deleted. The clean runtime remains `/fs/scratch/PAA0201/ollieproudman/DecomposedReasoning/vLLM_Experimental/runtimes/vllm_0.22.0_19ef0b6fd88c823d_d549da4b43ba0d82`; source invariant checks report zero failures and the failed bulk-replay symbols are absent. Validation passed after cleanup: full `vLLM_Experimental` pytest (`63 passed`), pyright over `src tests` (`0 errors`), runtime scheduler `py_compile`, Slurm/script shell syntax, and `git diff --check`.
